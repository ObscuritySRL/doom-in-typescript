# 01-002 inventory-simplified-launcher-imports

- 2026-05-14 start: selected step 01-002 via bun run plan_final/select-step.ts --lane current-state after 01-001 status was written.
- Goal: record every src/main.ts and src/launcher import edge that bypasses the vanilla launch path.
- Write lock: plan_final/current-state/simplified-launcher-imports.json, test/plan_final/current-state/simplified-launcher-imports.test.ts.
- Read-only refs: src/main.ts, src/launcher/.
- Read src/main.ts (5 import edges, all from the simplified-launcher and bootstrap surfaces) and the four src/launcher/*.ts files (gameplayAssets.ts, gameplayRenderer.ts, session.ts, win32.ts).
- All current-launch-path imports skip the missing vanilla doom.ts → D_DoomMain → D_DoomLoop chain; only the legacy `bun run src/main.ts` → createLauncherSession → runLauncherWindow path is wired.
- Will record per-file fingerprints (size, line count, sha256), per-file ordered import edges with module path / kind (value or type) / names, and a top-level bypass summary that names the missing vanilla entrypoint surfaces.
- Wrote `plan_final/current-state/simplified-launcher-imports.json` with five file entries (src/main.ts and the four src/launcher/*.ts), captured every import edge with kind (value, type, mixed) + module path + names, plus a `bypass_summary` that pins the missing vanilla entrypoint chain and the `bypasses_vanilla_launch_path: true` flag.
- Wrote `test/plan_final/current-state/simplified-launcher-imports.test.ts` with seven assertions: inventory existence, canonical identity (step id / lane / title), captured-at parseability, ordered file enumeration, per-file size/line-count/sha256 fingerprint, round-trip parity between recorded import edges and a regex-parsed source-of-truth scan, and bypass_summary content pins.
- `bun run format` formatted 4 files; no fixes applied.
- `bun test test/plan_final/current-state/simplified-launcher-imports.test.ts` passed 7 of 7.
- `bun test` passed 15345 of 15345 (800 files).
- `bun x tsc --noEmit --project tsconfig.json` exited 0.
- Wrote `plan_final/evidence/01-002.json` and `plan_final/status/01-002.json`.
