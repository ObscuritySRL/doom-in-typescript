# 10-003 wire-monster-look-chase

- 2026-05-15 start: selected plan_final step 10-003 (lane ai-specials). Prerequisite 10-002 COMPLETED. No prior 10-003 artifacts — fresh step. Goal: wire look, chase direction, melee range, missile range, and active sounds. Read-only: `src/ai/chase.ts`, `src/ai/meleeRange.ts`, `src/ai/missileRange.ts`. Write lock: `src/ai/`, `src/vanilla/`, `test/plan_final/ai/wire-monster-look-chase.test.ts`.
- 2026-05-15 reads: chase.ts = const enum DirType (NOT re-exported — verbatimModuleSyntax hazard), NUMDIRS 9, OPPOSITE/DIAGS, X_SPEED/Y_SPEED (diagonal component 47000), FLOATSPEED 4*FRACUNIT, move/tryWalk/newChaseDir/chase, ChaseContext/UseSpecialLineFunction/StartSoundFunction. meleeRange.ts = re-exports checkMeleeRange + type TargetingContext from targeting; own MELEERANGE 64*FRACUNIT, MELEE_RADIUS_ADJUST 20*FRACUNIT, MELEE_BASE_CUTOFF=MELEERANGE-ADJUST=44*FRACUNIT. missileRange.ts = re-exports checkMissileRange + type TargetingContext + MISSILE_* (BASE/NO_MELEE/DIST_CLAMP/CYBORG/VILE_MAX/UNDEAD_MIN) from targeting. COLLISION: TargetingContext exported by BOTH meleeRange + missileRange — surface from meleeRange only. Design: `src/vanilla/wireMonsterLookChase.ts` re-export barrel (chase + meleeRange + missileRange; no const enum; TargetingContext from meleeRange) + frozen `VANILLA_MONSTER_LOOK_CHASE_INVARIANTS` (5 rules). Inventory-neutral test (constants + typeof fns + frozen manifest + `../`-filtered specifier set + dynamic identity). chase/meleeRange/missileRange in inventory-ai-and-specials — probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
