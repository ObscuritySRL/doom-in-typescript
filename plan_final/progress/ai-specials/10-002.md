# 10-002 wire-damage-and-targeting

- 2026-05-15 start: selected plan_final step 10-002 (lane ai-specials) — newly eligible after the cb296d9 cycle fix removed the spurious 09-008 back-edge (prereq is now just 08-002, COMPLETED). No prior 10-002 artifacts — fresh step. Goal: wire damageMobj, line attacks, radius attacks, target acquisition, and sound target propagation. Read-only: `src/ai/targeting.ts`, `src/ai/soundPropagation.ts`, `src/world/radiusAttack.ts`. Write lock: `src/ai/`, `src/world/`, `src/vanilla/`, `test/plan_final/ai/wire-damage-and-targeting.test.ts`.
- 2026-05-15 reads: targeting = MISSILE_* cutoffs/clamps (BASE 64*FRACUNIT, NO_MELEE 128*FRACUNIT, DIST_CLAMP 200, CYBORG 160, VILE_MAX 14*64=896, UNDEAD_MIN 196), checkSight (LOS), lookForPlayers (allaround FOV gate), checkMeleeRange, checkMissileRange (+RNG), TargetingContext/PlayerLike. soundPropagation = createSoundState(sectorCount)→{validcount 0, sectorValidcount/sectorSoundTraversed Int32Array(n), sectorSoundTarget Array(n) null}, noiseAlert, SoundState. radiusAttack = radiusAttack(spot,source,damage,blockmap,blocklinks,callbacks), CheckSightFunction, RadiusAttackCallbacks. No const enums; no name collisions. Design: `src/vanilla/wireDamageAndTargeting.ts` re-export barrel + frozen `VANILLA_DAMAGE_TARGETING_INVARIANTS` (5 rules). Inventory-neutral test (real createSoundState + MISSILE constants + frozen manifest + `../`-filtered specifier set + dynamic identity). ai/world modules likely inventory-tracked (inventory-ai-and-specials / map-and-world) — probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
