# 06-002 wire-bsp-wall-renderer

- 2026-05-14 start: selected plan_final step 06-002 (lane render). Goal: wire BSP front-to-back walk, solidsegs, wall clipping, and wall column drawing. Prerequisites 06-001 and 08-001 are both COMPLETED. HEAD = origin/main = `1608037` at session start.
- Read the read-only refs `src/render/solidWalls.ts` (SolidWallSegment, SolidWallRenderContext, renderSolidWall), `src/render/wallColumns.ts` (PreparedWallTexture, prepareWallTexture, getWallColumn, computeTextureWidthMask), and `src/map/nodeTraversal.ts` (pointOnSide, pointInSubsector). These already provide the BSP traversal and wall rendering primitives — this step wires them together against a `MapData` snapshot.
- Design decision: add `src/vanilla/wireBspWallRenderer.ts` exposing `walkBspFromViewpoint(mapData: MapData, viewX: Fixed, viewY: Fixed): readonly number[]` that returns the subsector index list in BSP front-to-back order from the supplied viewpoint. The walk uses the existing read-only `pointOnSide` primitive and the `NF_SUBSECTOR` flag from `bspStructs.ts`. The traversal is iterative (stack-based) rather than recursive to avoid stack overflow on large levels; at each internal node, the viewpoint's near-side child is pushed last so it pops first (front-to-back). No read-only render or map file is modified.
- Wrote `src/vanilla/wireBspWallRenderer.ts` and `test/plan_final/render/wire-bsp-wall-renderer.test.ts` with 9 tests (252 expect() calls — most from the permutation check asserting every subsector index appears in the front-to-back list exactly once).
- Ran `bun run format` → no fixes applied.
- Ran `bun test test/plan_final/render/wire-bsp-wall-renderer.test.ts` → 9 pass / 0 fail / 252 expect() calls.
- Full suite had transient inventory drift on the first run (parallel-agent module landed mid-walk); retry was clean at 15959 pass / 0 fail / 847 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.