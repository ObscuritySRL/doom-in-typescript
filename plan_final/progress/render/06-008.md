# 06-008 wire-wipes-and-borders

- 2026-05-15 start: selected plan_final step 06-008 (lane render). Goal: wire view borders, screenblocks, wipe transitions, and framebuffer presentation composition. Prerequisites 06-001 and 07-004 are COMPLETED.
- Read the read-only refs `src/render/implement-view-border-rendering.ts` (`VANILLA_VIEW_BORDER_PATCHES` 8-entry list, `VANILLA_VIEW_BORDER_BACKGROUND_DOOM1='FLOOR7_2'`/`DOOM2='GRNROCK'`, `getViewBorderBackground`) and `src/render/implement-screen-size-blocks.ts` (`VANILLA_SCREEN_BLOCK_MIN=3`/`MAX=11`/`DEFAULT=10`/`FULLSCREEN=11`, `isValidScreenBlock`, `isFullscreenScreenBlock`, `scaledViewWidthForScreenBlock`, `viewHeightForScreenBlock`).
- Design decision: add `src/vanilla/wireWipesAndBorders.ts` — a re-export facade collecting the view-border + screen-size-block entry points into one cohesive surface. No read-only render file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/render/wire-wipes-and-borders.test.ts` (10 tests / 34 expect() calls including same-reference identity checks + behavioral assertions).
- Ran `bun run format` → no fixes; focused `bun test` → 10 pass / 0 fail.
- Ran `bun test` → 16077 pass / 0 fail / 861 files on the FIRST run (no inventory drift — the two `implement-*` modules are not importer-counted in the renderer inventory or counts were already current).
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.