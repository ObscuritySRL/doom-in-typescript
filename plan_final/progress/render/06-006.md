# 06-006 wire-psprite-renderer

- 2026-05-15 start: selected plan_final step 06-006 (lane render). Goal: wire player weapon and muzzle flash psprites into the final frame after world sprites. Prerequisites 06-005 and 09-004 are COMPLETED.
- Read the read-only refs `src/render/implement-player-weapon-sprite-rendering.ts` (constants only: VANILLA_WEAPONTOP_FIXED=32<<FRACBITS, VANILLA_WEAPONBOTTOM_FIXED=128<<FRACBITS, VANILLA_NUMPSPRITES=2, PS_WEAPON=0, PS_FLASH=1) and `src/player/weaponStates.ts` (the psprite-state action functions).
- Design decision: write a focused test that pins the player-weapon-sprite render constants and the psprite-slot ordering invariant (PS_WEAPON=0 drawn before PS_FLASH=1, both AFTER world sprites). No source file is modified — focused-test-only step (the 06-004 precedent for constant-pinned read-only render modules).
- Wrote `test/plan_final/render/wire-psprite-renderer.test.ts` (8 tests / 15 expect() calls): file existence, R_DrawPSprite comment anchor, NUMPSPRITES=2, PS_WEAPON<PS_FLASH slot order, WEAPONTOP/WEAPONBOTTOM fixed-point values, lowered-below-raised invariant, slot-index completeness.
- Ran `bun run format` → no fixes; focused `bun test` → 8 pass / 0 fail.
- Ran `bun test` → 16104 pass / 0 fail / 865 files on the FIRST run (no inventory drift — focused-test-only, no new src/vanilla module). Typecheck → clean.