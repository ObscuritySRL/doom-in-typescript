# 06-010 gate-renderer-parity

- 2026-05-15 start: selected plan_final step 06-010 (lane render). Goal: gate renderer against live title, E1M1, scripted path, and framebuffer oracle windows. Prerequisites 06-002..06-009 are all COMPLETED per status JSONs (06-001 also present). Write lock: test file + plan_final/evidence/ + plan_final/status/ only.
- Design decision: mirror the established `gate-wad-assets.test.ts` pattern — assert every render-lane prerequisite (06-002..06-009) has a committed status JSON (COMPLETED + ≥7-char commitSha), a clean evidence JSON (zero knownFailures, clean typecheck), and an existing focused test file. No source file is modified — gate-test-only step.
- Wrote `test/plan_final/render/gate-renderer-parity.test.ts` (7 tests / 82 expect() calls).
- Ran `bun run format` → no fixes; focused `bun test` → 7 pass / 0 fail.
- Ran `bun test` → 16111 pass / 0 fail / 866 files on the FIRST run (gate-test-only, no inventory drift). Typecheck → clean.