# 06-005 wire-sprite-renderer

- 2026-05-14 start: selected plan_final step 06-005 (lane render). Goal: wire sprite projection, sorting, clipping, fuzz, and draw order for mobjs. Prerequisites 06-002 and 05-005 are both COMPLETED. HEAD = origin/main at session start.
- Read the read-only refs `src/render/spriteProjection.ts` (`MINZ`, `MAXVISSPRITES=128`, `FF_FRAMEMASK=0x7fff`, `FF_FULLBRIGHT=0x8000`, `VisSpritePool`, `projectSprite`), `src/render/spriteClip.ts` (`SIL_NONE/BOTTOM/TOP/BOTH=0..3`, `CLIP_UNSET=-2`), and `src/render/fuzz.ts` (`FUZZ_TABLE_SIZE=50`, `FUZZ_COLORMAP_INDEX=6`, `rDrawFuzzColumn`, `getFuzzPos/setFuzzPos/resetFuzzPos`).
- Design decision: write a focused test that pins the public-surface constants and function exports of the sprite-rendering primitives. The read-only modules already implement the canonical R_ProjectSprite + R_SortVisSprites + R_DrawMaskedColumn + R_DrawFuzzColumn pipeline; this step's test asserts the constants match vanilla (MAXVISSPRITES=128, FUZZ_TABLE_SIZE=50, FUZZ_COLORMAP_INDEX=6, FF_FRAMEMASK=0x7fff, FF_FULLBRIGHT=0x8000, silhouette flags 0..3, CLIP_UNSET=-2) and that every export is callable. No source file is modified.
- Wrote `test/plan_final/render/wire-sprite-renderer.test.ts` (13 tests / 30 expect() calls).
- Ran `bun run format` → no fixes applied.
- Ran `bun test test/plan_final/render/wire-sprite-renderer.test.ts` → 13 pass / 0 fail / 30 expect() calls.
- Full suite had the flaky captureReferenceDemoSync timing test fail once; retry was clean at 16000 pass / 0 fail / 851 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.