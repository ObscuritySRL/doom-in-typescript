# 05-001 wire-iwad-resource-cache

- 2026-05-14 start: selected plan_final step 05-001 (lane wad-assets). Goal: create a runtime resource cache from WAD directory, lookup, game mode, and lump availability. Prerequisite 03-004 is COMPLETED at commit `6e629ec` per status JSON (work pushed by a parallel worker during this session). Working tree is clean at session start modulo unrelated probe-*.ts scratch files, .claude scratch state, and `plan_final/progress/launch-host-input/03-005.md` (a parallel in-flight start log for a different step). My write lock and the parallel agent's 03-005 lock both list `src/vanilla/` but the file I add (`src/vanilla/iwadResourceCache.ts`) is disjoint from the file 03-005's design notes name (`src/vanilla/win32WindowHost.ts`), so a rebase at push time should merge cleanly.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs `src/wad/header.ts`, `src/wad/directory.ts`, `src/wad/lumpLookup.ts`, and `src/bootstrap/gameMode.ts` (for the LumpChecker contract and identifyGame). Skimmed `src/assets/` for naming conventions and confirmed that nothing under `src/wad/` or `src/assets/` needs modification (the existing parseWadHeader / parseWadDirectory / LumpLookup / identifyGame surfaces cover everything 05-001 needs). Also read `src/vanilla/launchContext.ts` to see what LaunchContext.iwad shape the cache consumes (resolvedPath, gameMode, gameMission).
- Design decision: add `src/vanilla/iwadResourceCache.ts` exposing `buildIwadResourceCache(launchContext, loader)` and a typed `IwadResourceCache` plus a typed `IwadResourceCacheError`. The cache wraps the existing parseWadHeader/parseWadDirectory/LumpLookup pipeline and calls identifyGame to derive a final `GameIdentification`. A caller-supplied `IwadFileLoader` reads the WAD buffer (so the focused test is deterministic — no `Bun.file` calls inside the cache). Empty path, missing file, parse failure, and IWAD-header validation each surface as a typed error with a stable `reason` discriminator.
- Public surface (planned): `IwadResourceCache` (frozen), `IwadResourceCacheError`, `IwadFileLoader`, `buildIwadResourceCache(launchContext, loader)`, plus the LumpChecker contract surfaced via `hasLump` / `getAllIndicesForName` for downstream `identifyGame` callers.
- Wrote `src/vanilla/iwadResourceCache.ts` (already present in working tree from earlier parallel-agent work) and `test/plan_final/wad/wire-iwad-resource-cache.test.ts` (20 tests). The test had a TS7006 implicit-any error on the inline `readFile: (resolvedPath) => ...` callback parameter at line 291; fix: annotate `resolvedPath: string` explicitly.
- Cross-plan inventory rewrite: src/vanilla/iwadResourceCache.ts imports src/wad/directory.ts (transitively via the directory parsing pipeline), src/wad/header.ts (transitively via the WAD header parse), and src/wad/lumpLookup.ts (directly), bumping plan_vanilla_parity inventory importer counts for those three modules. The inventory JSON test (test/vanilla_parity/current-state/inventory-wad-and-asset-modules.test.ts:311) failed against the stored counts (22/66, 2/62, 3) so plan_vanilla_parity/current-state/inventory-wad-and-asset-modules.json was updated locally to the new on-disk counts (23/67, 3/63, 4). This is the canonical fix for cross-plan importer-count drift; the inventory test now passes (21 pass / 0 fail / 399 expect() calls).
- Ran `bun run format` → 2 files formatted, no fixes applied.
- Ran `bun test test/plan_final/wad/wire-iwad-resource-cache.test.ts` → 20 pass, 0 fail, 64 expect() calls.
- First full `bun test` attempt failed with 2 failures: (1) the inventory-wad-and-asset-modules importer-count drift described above, and (2) one flaky timing assertion in oracle: captureReferenceDemoSync (Expected: ≥3965.23ms, Received: 3959.17ms — a ~6ms wall-clock budget overrun unrelated to 05-001). Fix: updated the inventory JSON; reran the full suite and the demo-sync test passed on rerun.
- Ran `bun test` → 15686 pass, 0 fail across 827 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.
