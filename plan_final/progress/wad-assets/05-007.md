# 05-007 wire-demo-and-map-assets

- 2026-05-14 start: selected plan_final step 05-007 (lane wad-assets). Goal: wire DEMO lumps and map bundles into runtime level and attract-loop systems. Prerequisite 05-001 (commit `1a7f952`) is COMPLETED. No parallel progress log exists for wad-assets/05-007. Working tree clean modulo probe scratch.
- A parallel agent's untracked `src/vanilla/demoAndMapAssets.ts` and `test/plan_final/wad/wire-demo-and-map-assets.test.ts` are already on disk; both files cite the canonical 05-007 wiring (parse-demo-lumps for DEMO1/DEMO2/DEMO3 attract-loop demos, mapBundle for map-name discovery via findMapNames). The bridge module imports `findMapNames` from `src/map/mapBundle.ts` (read-only — SHA-pinned by plan_vanilla_parity inventory). Cross-plan map inventory bump for mapBundle.ts (src_importer_count 2→3) was already applied by the parallel agent — verified inventory tests pass.
- Ran `bun run format` → no fixes applied.
- Ran `bun test test/plan_final/wad/wire-demo-and-map-assets.test.ts` → 13 pass / 0 fail / 31 expect() calls.
- Ran `bun test` → 15895 pass / 0 fail / 841 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.
