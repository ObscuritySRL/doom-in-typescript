# 07-008 wire-finale-runtime

- 2026-05-15 start: selected plan_final step 07-008 (lane ui). Prerequisites 08-009 + 11-007 COMPLETED. No prior 07-008 artifacts — fresh step. Goal: wire finale text, art screens, bunny scope, cast scope, timing, music, and transition exits. Read-only: `src/ui/finale.ts` (FinaleMusicCue surveyed in 11-007). Write lock: `src/ui/`, `src/vanilla/`, `test/plan_final/ui/wire-finale-runtime.test.ts` (per step file).
- 2026-05-15 reads: finale.ts = TEXTSPEED 3, TEXTWAIT 250, FINALE_TEXT_START_DELAY 10, FINALE_COMMERCIAL_SKIP_DELAY 50, FINALE_CAST_MAP 30, MAXPLAYERS 4; FinaleMusicCue 'mus_victor'|'mus_bunny'|'mus_read_m' / FinaleGameMode type aliases (not const enums); E1_FLAT..E4_FLAT, E2_ART_LUMP VICTORY2, E4_ART_LUMP ENDPIC, BUNNY_SCROLL_LUMPS ['PFUB1','PFUB2'], E1_TEXT..E4_TEXT; getFinaleScreen / createFinaleState / startFinale / tickFinale / getVisibleCharacterCount; FinaleScreen/Input/State/StartResult/TickResult interfaces. No const enums; single module → no collisions. Design: `src/vanilla/wireFinaleRuntime.ts` re-export barrel + frozen `VANILLA_FINALE_RUNTIME_INVARIANTS` (5 rules). Inventory-neutral test (constants + getFinaleScreen/createFinaleState + typeof fns + frozen manifest + `../`-filtered specifier set + dynamic identity). finale.ts in inventory-ui-modules — probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
- 2026-05-15 verify: `bun run format` (formatted facade + test, no banned `as`/`as unknown`/`as any`); focused `bun test test/plan_final/ui/wire-finale-runtime.test.ts` = 6 pass / 0 fail (40 expect). Probe replicating the inventory scan + countImporters showed only `src/ui/finale.ts` src 2->3 (the new facade); test count unchanged at 2 — reconciled `plan_vanilla_parity/current-state/inventory-ui-modules.json` finale.ts src_importer_count 2->3 (no other field touched). Ran mandatory `bun run plan_final/sync-master-checklist.ts`. Full `bun test` = 16357 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean. Finalizing with the two-commit pathspec pattern (feat: evidence/progress/inventory/facade/test → push → status JSON w/ feat SHA → re-sync → docs: status + MASTER_CHECKLIST.md → push).
