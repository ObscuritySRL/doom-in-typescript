# 07-003 wire-menu-actions

- 2026-05-15 start: selected plan_final step 07-003 (lane ui), unblocked by 07-002 (e365561 cycle fix chain). Goal: wire New Game, Options, Sound, Read This, Save, Load, End Game, and Quit actions into runtime effects. Prerequisite 07-002 is COMPLETED.
- Read the read-only ref `src/ui/menus.ts` — the `MenuAction` discriminated union has 20 kinds plus `MENU_ACTION_NONE`, `handleMenuKey`, `openMessage`. `src/config/` and `src/save/` are read-only refs for the effects each action drives.
- Design decision: add `src/vanilla/wireMenuActions.ts` — a re-export facade collecting `handleMenuKey` + `MENU_ACTION_NONE` + `openMessage` + the `MenuAction` type, plus a frozen ASCIIbetically-sorted `VANILLA_MENU_ACTION_KINDS` manifest pinning the complete 20-discriminant vocabulary. No read-only ui/config/save file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/ui/wire-menu-actions.test.ts` asserting the 20-kind manifest matches the union, is frozen + ASCIIbetically sorted, the producer is callable, and same-reference identity.