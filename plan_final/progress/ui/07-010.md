# 07-010 wire-quit-and-endoom-ui

- 2026-05-15 start: selected plan_final step 07-010 (lane ui) via `bun run plan_final/select-step.ts`. Prerequisite 07-003 is COMPLETED (status JSON, commit 631800a). No prior 07-010 artifacts exist — fresh step. Goal: wire quit confirmation, randomized text, and ENDOOM display to clean quit. Read-only paths: `src/ui/endoom.ts`, `src/bootstrap/quitFlow.ts`. Write lock: `src/ui/`, `src/bootstrap/`, `test/plan_final/ui/wire-quit-and-endoom-ui.test.ts`. Following the established 07-001..07-006/07-009 convention: the wiring facade lives at `src/vanilla/wireQuitAndEndoomUi.ts` (07-004 set precedent for placing the facade in src/vanilla/ even when its write lock listed only src/ui/+src/render/). About to read the two read-only source modules.
- 2026-05-15 reads: read read-only `src/ui/endoom.ts` (parseEndoom, ENDOOM_COLUMNS/ROWS/CELL_COUNT/BYTES_PER_CELL/SIZE, EndoomCell/EndoomScreen — 80x25 VGA text, 4000 bytes, 2000 frozen cells) and `src/bootstrap/quitFlow.ts` (QuitFlow LIFO I_AtExit model, CANONICAL_REGISTRATION_ORDER, CANONICAL_QUIT_ORDER = [I_ShutdownGraphics, D_Endoom, ...], CLEANUP_STEP_COUNT=8, CleanupStepName/CleanupRegistration). Also read sibling facade `src/vanilla/wireMenuActions.ts` for the established barrel+frozen-manifest convention.
- 2026-05-15 edits: created `src/vanilla/wireQuitAndEndoomUi.ts` (pure re-export barrel of the two read-only modules + frozen `VANILLA_QUIT_ENDOOM_INVARIANTS` pinning 4 parity rules: randomized yes/no quit confirmation gate, LIFO I_AtExit drain, ENDOOM shown right after I_ShutdownGraphics, ENDOOM = 80x25/4000-byte VGA text) and focused test `test/plan_final/ui/wire-quit-and-endoom-ui.test.ts` (exists/cites-step, re-export source check, frozen 4-rule manifest, callable symbols, real parseEndoom decode + RangeError, LIFO executeQuit behavior, same-reference checks). No read-only file modified. Anticipate inventory drift: facade adds an src importer to src/ui/endoom.ts and the test adds a test importer via the `from '../ui/endoom.ts'` assertion literal — will reconcile after the full suite reports exact counts. Running verification.
- 2026-05-15 verify run 1: format fixed 1 file (test import wrap, no fixes since); focused 7 pass / 0 fail; full `bun test` 16185 pass, 1 fail = `inventory-ui-modules.test.ts:330` `src/ui/endoom.ts` src_importer_count manifest 0 vs live 1. Replicated the test's exact scan+countImporters across all 9 UI modules + checked `inventory-src-bootstrap-surface.test.ts` (no importer-count assertions, and the full run had exactly 1 fail across 877 files, so quitFlow drift is not enforced anywhere). Only endoom drifts: src 0→1 (facade `export ... from '../ui/endoom.ts'`), test 1→2 (focused test's `expect(fileText).toContain("from '../ui/endoom.ts'")` assertion literal). Reconciled `plan_vanilla_parity/current-state/inventory-ui-modules.json` endoom src 0→1, test 1→2 (manifest==reality maintained — documented bump-on-drift, not a weakening). Probe reconfirms all 9 UI modules consistent; removed throwaway probe. Rerunning full verification from the beginning.
