# 07-004 wire-status-bar-drawing

- 2026-05-15 start: selected plan_final step 07-004 (lane ui). Goal: wire status bar background, numbers, percent, keys, ammo, arms, and face widgets to the framebuffer. Prerequisites 05-004 and 09-001 are COMPLETED.
- Read the read-only refs `src/ui/statusBar.ts` (`createStatusBarState`, `tickStatusBar`, `tickFaceWidget`, `calcPainOffset`, `updateKeyBoxes`, `computeStatusBarValues`; the ST_* face-priority + timing constants) and `src/ui/implement-status-bar-background.ts`.
- Design decision: add `src/vanilla/wireStatusBarDrawing.ts` — a re-export facade collecting the status-bar state + per-tic widget functions into one cohesive surface. No read-only ui file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/ui/wire-status-bar-drawing.test.ts` (6 tests / 22 expect() calls including a same-reference identity check).
- Ran `bun run format` → no fixes; focused `bun test` → 6 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/ui/statusBar.ts` src 0→1, test 1→2 in `inventory-ui-modules.json`. Inventory test: 22 pass / 0 fail; diff is exactly 2 count lines.
- Full suite: one flaky captureReferenceDemoSync timing fail on the first run; clean retry at 16067 pass / 0 fail / 860 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.