# 07-011 gate-ui-parity

- 2026-05-15 start: selected plan_final step 07-011 (lane ui) as first eligible step. Prerequisites 07-001..07-010 all COMPLETED (verified via status JSON). Goal: gate title, menu, HUD, automap, intermission, finale, and quit UI against the ten ui-lane wiring steps. Write lock: `test/plan_final/ui/gate-ui-parity.test.ts`, `plan_final/evidence/`, `plan_final/status/`. Read-only: `src/ui/`, `test/oracles/fixtures/`. Research: AGENTS.md (binding rules, in force all session), plan_final/README.md.
- 2026-05-15 reads: step file 07-011; canonical lane-gate pattern `test/plan_final/render/gate-renderer-parity.test.ts` (prereq status COMPLETED + stepId match + commitSha >=7 + evidence exists + zero knownFailures + clean typecheck + focused test exists + exact prereq-set assertion). Step-file→title map: 07-001 wire-title-loop-rendering, 07-002 wire-menu-rendering, 07-003 wire-menu-actions, 07-004 wire-status-bar-drawing, 07-005 wire-hud-messages, 07-006 wire-automap-runtime, 07-007 wire-intermission-runtime, 07-008 wire-finale-runtime, 07-009 wire-pause-and-help-overlays, 07-010 wire-quit-and-endoom-ui. All ten focused tests present under test/plan_final/ui/. Design: mirror gate-renderer-parity.test.ts exactly for the ten ui prerequisites; no src changes (gate is read-only over status/evidence/test artifacts) → inventory-neutral.
- 2026-05-15 verify: dropped the reference file's `JSON.parse(...) as PrerequisiteStatus` casts (AGENTS.md L201/L222 ban "any cast that bypasses the type system") — parse into `Record<string, unknown>` (any→Record is cast-free) and field-narrow via String()/Array.isArray with a poison sentinel for malformed evidence (defensive, not weakening: malformed prereq evidence fails the gate). `bun run format` no fixes; grep for ` as unknown| as any| as [A-Z]` clean. Focused `bun test test/plan_final/ui/gate-ui-parity.test.ts` = 7 pass / 0 fail. Ran mandatory `bun run plan_final/sync-master-checklist.ts`. Full `bun test` = 16364 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean. No `../`-prefixed src import in the test → no inventory module affected (inventory-neutral, no probe needed). Finalizing with the two-commit pathspec pattern.
