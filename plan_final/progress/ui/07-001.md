# 07-001 wire-title-loop-rendering

- 2026-05-15 start: selected plan_final step 07-001 (lane ui) after the prereq-cycle fix (commit `e365561`) reopened the frontier. Goal: wire clean launch to TITLEPIC, CREDIT, help pages, demo pages, music cues, and page timing. Prerequisite 05-004 is COMPLETED (07-001's prior spurious dep on 04-005 was removed as part of the cycle fix).
- Read the read-only refs `src/ui/frontEndSequence.ts` (`createFrontEndSequence`, `tickFrontEnd`, `handleFrontEndKey`, `notifyDemoCompleted`, `setMenuActive`, `getInitialHelpLump`; FRONTEND_KEY_* + FRONTEND_TICK_IDLE constants) and `src/bootstrap/titleLoop.ts` (`TitleLoop` class; CYCLE_LENGTH=6, TITLEPIC_PAGETIC=170, COMMERCIAL_TITLEPIC_PAGETIC=385, INTERLUDE_PAGETIC=200).
- Design decision: add `src/vanilla/wireTitleLoopRendering.ts` — a re-export facade collecting the front-end attract-loop sequence + the TitleLoop page-timing primitive into one cohesive surface. No read-only ui/bootstrap file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/ui/wire-title-loop-rendering.test.ts` (7 tests / 25 expect() calls including a deterministic tick-action assertion + same-reference identity). Fixed one tsc error: `TitleLoop` constructor requires a `gameMode` arg — used `new TitleLoop('shareware')`.
- Ran `bun run format` → 1 fix (test wrapping); focused `bun test` → 7 pass / 0 fail; typecheck → clean.
- Cross-plan inventory diagnostic: `src/ui/frontEndSequence.ts` src 20→21 test 20→21 in `inventory-ui-modules.json` (titleLoop.ts is in bootstrap, not importer-counted in the ui inventory). Inventory test: 22 pass / 0 fail; diff is exactly 2 count lines.
- Ran `bun test` → 16143 pass / 0 fail / 871 files. Typecheck → clean.