# 07-005 wire-hud-messages

- 2026-05-15 start: selected plan_final step 07-005 (lane ui). Goal: wire pickup, secret, system, save, and chat macro messages with vanilla timing. Prerequisites 07-004 and 09-002 are COMPLETED.
- Read the read-only ref `src/ui/hudMessages.ts` (`createHudMessageState`, `hudMessageStart`, `requestHudMessageRefresh`, `tickHudMessages`, `getDoom1MapName`; TICRATE=35, HU_MSGTIMEOUT=4*TICRATE, HU_MSGX/Y=0, HU_MAXLINELENGTH=80, HUSTR_E1M1.. map titles).
- Design decision: add `src/vanilla/wireHudMessages.ts` — a re-export facade collecting the HUD-message state + per-tic functions + Doom 1 map-name resolver into one cohesive surface. No read-only ui file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/ui/wire-hud-messages.test.ts` (7 tests / 21 expect() calls including a getDoom1MapName('E1M1: Hangar') behavioral check + same-reference identity).
- Ran `bun run format` → 1 fix (test wrapping); focused `bun test` → 7 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/ui/hudMessages.ts` src 0→1, test 1→2 in `inventory-ui-modules.json`. Inventory test: 22 pass / 0 fail; diff is exactly 2 count lines.
- Ran `bun test` → 16091 pass / 0 fail / 863 files. Typecheck → clean.