# 07-002 wire-menu-rendering

- 2026-05-15 start: selected plan_final step 07-002 (lane ui), unblocked by 07-001 (e365561 cycle fix chain). Goal: wire menu patch drawing, text drawing, cursor drawing, and menu overlay composition. Prerequisites 07-001 + 05-004 are COMPLETED.
- Read the read-only refs `src/ui/menus.ts` (`createMenuState`, `openMenu`, `closeMenu`, `openMessage`, `tickMenu`, `handleMenuKey`, mouse/joy gating; LINEHEIGHT=16, SKULLXOFF=-32, SKULL_ANIM_TIME=8, SAVESTRINGSIZE=24, MAX_LOAD_SAVE_SLOTS=6, HU_FONTSTART=0x21, HU_FONTEND=0x5f) and `src/ui/assets.ts` (`huFontLumpName`, `statusBarFaceLumpName`).
- Design decision: add `src/vanilla/wireMenuRendering.ts` — a re-export facade collecting the menu state machine + key/tick handlers + the HU-font lump-name resolver into one cohesive surface. No read-only ui file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/ui/wire-menu-rendering.test.ts` (7 tests / 27 expect() calls including the layout-constant pins, a huFontLumpName STCFN behavioral check, and same-reference identity).
- Ran `bun run format` → no fixes; focused `bun test` → 7 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/ui/assets.ts` src 2→3 test 3→4; `src/ui/menus.ts` src 20→21 test 21→22 in `inventory-ui-modules.json`. Inventory test: 22 pass / 0 fail; diff is exactly 4 count lines.
- Ran `bun test` → 16167 pass / 0 fail / 874 files. Typecheck → clean.