# 07-009 wire-pause-and-help-overlays

- 2026-05-15 start: selected plan_final step 07-009 (lane ui), unblocked by 07-002 + 04-005 (e365561 cycle fix chain). Goal: wire pause overlay, help routing, and menu/demo interaction without pausing page timers incorrectly. Prerequisites 07-002 + 04-005 are COMPLETED.
- Read the read-only refs `src/ui/frontEndSequence.ts` (`HelpLump` = 'HELP'|'HELP1'|'HELP2', `getInitialHelpLump`, `notifyDemoCompleted`, `setMenuActive`, `OpenHelpKeyAction`/`OpenMenuKeyAction`/`FrontEndKeyAction`) and `src/ui/menus.ts` (`openMenu`/`closeMenu`/`openMessage`, menu-state machine).
- Design decision: add `src/vanilla/wirePauseAndHelpOverlays.ts` — a re-export facade collecting the help-lump resolver + the `setMenuActive` page-timer-pause hook + the demo-completed transition into one cohesive surface, plus a frozen `VANILLA_PAUSE_HELP_INVARIANTS` manifest pinning the two parity rules: (1) opening the menu/help overlay sets `menuActive` which PAUSES the GS_DEMOSCREEN page timer (so the title page does not advance under the menu), and (2) PAUSE during a level (`gamestate===GS_LEVEL`) freezes the tic but NOT the front-end page timer (which is not running during a level). No read-only ui file is modified — pure barrel + frozen manifest.
- 2026-05-15 resume: continuing prior session's unfinished work (impl + test + log present, no status/evidence/commit). Re-read read-only `src/ui/frontEndSequence.ts:94` `getInitialHelpLump`: vanilla returns `HELP1` for `retail` only, `HELP` for `commercial`, `HELP2` for `shareware`/`registered`/`indetermined` (ReadDef1/ReadDef2 drawer selection per m_menu.c). Prior session's focused test asserted `shareware→HELP1` / `registered→HELP1` and the facade docstring/@example claimed the same — both contradict the SHA-pinned read-only source and would fail. `src/ui/frontEndSequence.ts` is read-only and authoritative, so the fix is to correct the test expectations and the facade docstring to the true vanilla mapping (not weaken them).
- 2026-05-15 edits: corrected focused test `getInitialHelpLump` expectations to the true vanilla mapping (retail→HELP1, commercial→HELP, shareware/registered/indetermined→HELP2) and fixed the facade docstring + @example to match. No read-only ui file modified.
- 2026-05-15 verify run 1: `bun run format` clean (2 files, no fixes). Focused `bun test test/plan_final/ui/wire-pause-and-help-overlays.test.ts` → 6 pass / 0 fail. Full `bun test` → 16178 pass, 1 fail: `test/vanilla_parity/current-state/inventory-ui-modules.test.ts:330` `src_importer_count` for `src/ui/frontEndSequence.ts` expected (live) 23 vs manifest 22.
- 2026-05-15 diagnosis: drift caused by THIS step's expected new file `src/vanilla/wirePauseAndHelpOverlays.ts`, which `export ... from '../ui/frontEndSequence.ts'` and `'../ui/menus.ts'`, adding exactly one src importer to each. Verified: importer count without the new file == manifest 22; with it == 23. Fix: bump `src_importer_count` 22→23 for both `src/ui/frontEndSequence.ts` and `src/ui/menus.ts` in `plan_vanilla_parity/current-state/inventory-ui-modules.json` so the manifest stays accurate to on-disk reality (manifest==reality is exactly what the inventory test enforces — this is the documented bump-on-drift workflow, not a weakening). `test_importer_count` left unchanged (the new test file uses dynamic `await import(...)` for the ui modules, which `countImporters`'s `from`-anchored regex does not match). Rerunning full verification from the beginning.
- 2026-05-15 verify run 2: format+focused passed; full `bun test` still 1 inventory fail but now at line 331 `test_importer_count` for `src/ui/frontEndSequence.ts` (manifest 22 vs live 23) — pre-existing drift that was masked by the earlier src_importer_count failure stopping the per-module loop.
- 2026-05-15 diagnosis 2: replicated the test's exact `scanTypeScriptFiles`+`countImporters` algorithm in a throwaway probe across all 9 UI modules. Only two cells drift, both caused by THIS step's new test file: `countImporters` is a textual heuristic and the new test's assertion `expect(fileText).toContain("from '../ui/frontEndSequence.ts'")` / `"from '../ui/menus.ts'"` (verifying the facade re-exports) contains literal `from '...frontEndSequence.ts'` / `from '...menus.ts'` substrings the regex matches. The assertions are legitimate and correct; per the documented bump-on-drift workflow the manifest is reconciled to the true on-disk reality (manifest==reality is exactly what the inventory test enforces — not a weakening, skip, or quarantine). Bumped `test_importer_count` frontEndSequence 22→23, menus 23→24. Probe reconfirms all 9 UI modules consistent for src and test counts. Removed my 3 throwaway probes (pre-existing probe-*.ts from earlier sessions left untouched). Rerunning full verification from the beginning.
- 2026-05-15 verify run 3 PASS (canonical order): `bun run format` clean (no fixes) -> focused `bun test test/plan_final/ui/wire-pause-and-help-overlays.test.ts` 6 pass / 0 fail -> full `bun test` 16179 pass / 0 fail across 876 files -> `bun x tsc --noEmit --project tsconfig.json` exit 0. Wrote `plan_final/evidence/07-009.json` (knownFailures: []). Staging explicit paths for the feat commit (status JSON deferred to the post-push docs commit, mirroring the 07-003 two-commit SHA-recording pattern).