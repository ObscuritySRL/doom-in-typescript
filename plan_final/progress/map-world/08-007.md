# 08-007 wire-sector-effects-and-animations

- 2026-05-15 start: selected plan_final step 08-007 (lane map-world). Goal: wire damaging sectors, secrets, light changes, animated textures, and animated flats. Prerequisite 08-002 is COMPLETED.
- Read the read-only refs `src/specials/sectorSpecials.ts` (`playerInSpecialSector`, the DAMAGE_* constants, the `SectorSpecial` const enum, IRONFEET_BYPASS_THRESHOLD=5, EXIT_DAMAGE_HEALTH_THRESHOLD=10) and `src/specials/animations.ts` (`initPicAnims`, `updateAnimTranslation`, `ANIMDEFS`, `MAXANIMS=32`).
- Design decision: add `src/vanilla/wireSectorEffectsAndAnimations.ts` — a re-export facade collecting `playerInSpecialSector`, `initPicAnims`, `updateAnimTranslation` plus the canonical constants into one cohesive surface. No read-only specials file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/map/wire-sector-effects-and-animations.test.ts` (7 tests / 20 expect() calls including a same-reference identity check).
- Ran `bun run format` → no fixes.
- Ran focused `bun test` → 7 pass / 0 fail.
- Cross-plan inventory diagnostic identified 2 specials modules to bump in `inventory-ai-and-specials-modules.json`: animations.ts src 0→1 / test 1→2; sectorSpecials.ts src 0→1 / test 2→3. Inventory test: 23 pass / 0 fail; diff is exactly 4 changed count lines.
- Ran `bun test` → 16045 pass / 0 fail / 857 files (one transient parallel-agent inventory failure on the first run; clean on retry).
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.