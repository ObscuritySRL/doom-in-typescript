# 08-003 wire-collision-and-movement

- 2026-05-14 start: selected plan_final step 08-003 (lane map-world). Goal: wire check-position, try-move, slide, XY, Z, sector change, crush, and teleport movement. Prerequisite 08-002 is COMPLETED.
- Read the read-only refs `src/world/checkPosition.ts` (`checkPosition`), `src/world/tryMove.ts` (`tryMove`), `src/world/xyMovement.ts` (`xyMovement`, `explodeMissile`), `src/world/zMovement.ts` (`zMovement`, `approxDistance`). Also surveyed `src/world/slideMove.ts` (`slideMove`), `src/world/sectorChange.ts` (`changeSector`, `thingHeightClip`), `src/world/teleport.ts` (`teleportMove`, `evTeleport`, `pitStompThing`).
- Design decision: add `src/vanilla/wireCollisionAndMovement.ts` — a runtime facade that re-exports the canonical collision/movement entry points as a single cohesive surface so the per-tic mobj ticker and the special scheduler resolve them from one module. No read-only world file is modified — the facade is a pure re-export barrel plus a frozen `VANILLA_MOVEMENT_ENTRY_POINTS` manifest.
- Wrote `src/vanilla/wireCollisionAndMovement.ts` + `test/plan_final/map/wire-collision-and-movement.test.ts` (5 tests / 26 expect() calls including a same-reference identity check).
- Ran `bun run format` → 1 file fixed (import wrapping).
- Ran `bun test test/plan_final/map/wire-collision-and-movement.test.ts` → 5 pass / 0 fail.
- Cross-plan inventory: a precise diagnostic (replicating the inventory test's countImporters over the live src/ + test/ globs) identified exactly 7 read-only world modules whose importer counts the new facade + focused test bump by 1 each. Applied via line-targeted edits to `plan_vanilla_parity/current-state/inventory-map-and-world-modules.json`: checkPosition 9→10/9→10; sectorChange 0→1/1→2; slideMove 0→1/1→2; teleport 0→1/1→2; tryMove 4→5/1→2; xyMovement 4→5/1→2; zMovement 7→8/3→4. Inventory test: 23 pass / 0 fail; diff is exactly 14 changed count lines.
- Ran `bun test` → 16038 pass / 0 fail / 856 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.