# 08-009 wire-death-reborn-and-transitions

- 2026-05-15 start: selected plan_final step 08-009 (lane map-world). Prerequisites 08-008 (just completed) + 09-006 COMPLETED. No prior 08-009 artifacts — fresh step. Goal: wire player death, reborn, map reload, intermission entry, finale entry, and title return. Read-only: `src/player/playerSpawn.ts`, `src/player/implement-death-and-reborn-flow.ts`. Write lock: `src/world/`, `src/vanilla/`, `test/plan_final/map/wire-death-reborn-and-transitions.test.ts` (per step file).
- 2026-05-15 reads: implement-death-and-reborn-flow.ts = VANILLA_DEAD_VIEWHEIGHT_FIXED 6*FRACUNIT, VANILLA_DEAD_VIEWHEIGHT_DROP_FIXED 1*FRACUNIT, VANILLA_DEATHMATCH_RESPAWN_DELAY_TICS 10, PLAYER_STATE_LIVE 0/DEAD 1/REBORN 2, PlayerState type alias, DeathThinkInput{viewHeightFixed,useButtonPressed}/DeathThinkResult{viewHeightFixed,deltaViewHeightFixed,transitionsToReborn}, stepVanillaDeathThink (drops view by 1 unit/ tic until clamped at 6, transitionsToReborn=useButtonPressed). playerSpawn.ts = playerReborn/createPlayer/setupPsprites + NUMWEAPONS 9/NUMAMMO 4/NUMCARDS 6/NUMPOWERS 6/NUMPSPRITES 2; const enums PlayerState/WeaponType/AmmoType/PowerType/CardType/PsprNum (NOT re-exported). COLLISION: PlayerState exported by BOTH (playerSpawn const enum vs death-flow type alias) — surface from death-flow only. Design: `src/vanilla/wireDeathRebornAndTransitions.ts` re-export barrel (death-flow surface + playerReborn/createPlayer + NUM* constants from playerSpawn; no const enums) + frozen `VANILLA_DEATH_REBORN_INVARIANTS` (5 rules). Inventory-neutral test (real stepVanillaDeathThink view-drop/clamp/reborn-transition + constants + frozen manifest + `../`-filtered specifier set + dynamic identity). playerSpawn in inventory-player-modules, implement-death-and-reborn-flow likely too — probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
