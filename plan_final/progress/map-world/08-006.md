# 08-006 wire-floor-ceiling-platform-specials

- 2026-05-15 start: selected plan_final step 08-006 (lane map-world) via `bun run plan_final/select-step.ts`. Prerequisite 08-004 is COMPLETED (commit 2835dad). No prior 08-006 artifacts — fresh step. Goal: wire floors, ceilings, platforms, stairs, donuts, crushers, and active special serialization hooks. Read-only: `src/specials/floors.ts`, `src/specials/ceilings.ts`, `src/specials/platforms.ts`, `src/specials/stairsDonut.ts`. Write lock: `src/specials/`, `src/vanilla/`, `test/plan_final/map/wire-floor-ceiling-platform-specials.test.ts`.
- 2026-05-15 reads: extracted the export surfaces of the four read-only modules. Notes: all of floors/ceilings/platforms re-export shared `PlaneMoveResult` (from doors.ts) and the shared `SFX_STNMOV`/`SFX_PSTOP` constants; `FloorType`/`FloorDirection`/`CeilingType`/`CeilingDirection`/`PlatStatus`/`PlatType`/`StairType` are all `const enum`. Design: `src/vanilla/wireFloorCeilingPlatformSpecials.ts` re-export barrel that (a) takes each shared constant from exactly one module (SFX_STNMOV/SFX_PSTOP from floors, SFX_PSTART from platforms) to avoid TS duplicate-export errors, (b) does NOT re-export `PlaneMoveResult` (4-way duplicate) nor the `const enum`s (verbatimModuleSyntax cross-module const-enum hazard, per the 08-004 decision), (c) splits value `export {}` vs `export type {}`. Frozen `VANILLA_FLOOR_CEILING_PLAT_INVARIANTS` pins the parity speeds/waits/caps/sfx + EV_* return-on-success + active-special stasis-restore hooks. Focused test asserts the vanilla parity CONSTANTS (the parity-critical values), frozen/sorted manifest, line-anchored re-export specifier set, and dynamic same-reference identity for the EV_* functions + ActiveCeilings/ActivePlats (the EV_* dispatchers need const-enum params + heavy ThinkerList/sector fixtures, so behavior is proven via the constants + identity, consistent with the established barrel+manifest pattern and the "behavior OR inventory result" acceptance). Expect src-side drift on the four modules in inventory-ai-and-specials-modules.json; will probe + reconcile.
- 2026-05-15 verify run 1: format wrapped 2 files; focused 8 pass / 0 fail; no banned casts; full `bun test` 16215 pass, 1 fail = `inventory-ai-and-specials-modules.test.ts`. Probed with the test's exact algorithm: four src-only drifts from the new facade's re-exports — ceilings src 3→4, floors src 3→4, platforms src 3→4, stairsDonut src 1→2 (all test counts unchanged; line-anchored/dynamic-import test design held). Reconciled all four entries in inventory-ai-and-specials-modules.json (anchored on the unique exported_const_enums line since the three plane modules share an identical external_imports line). Probe reconfirms zero drift; removed throwaway probe. Rerunning full verification from the beginning.
