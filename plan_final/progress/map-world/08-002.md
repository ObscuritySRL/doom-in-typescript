# 08-002 wire-thinker-ticker

- 2026-05-14 start: selected plan_final step 08-002 (lane map-world). Goal: wire thinker add/remove order, mobj ticks, and special thinker ticks into P_Ticker. Prerequisites 08-001 and 04-004 are both COMPLETED.
- Read the read-only ref `src/world/thinkers.ts` (the `ThinkerList` ring: `init` = P_InitThinkers, `add` = P_AddThinker tail-insert, `remove` = P_RemoveThinker deferred-mark via the `REMOVED` symbol, `run` = P_RunThinkers which unlinks REMOVED nodes and invokes live actions, capturing `next` AFTER the action so same-tic insertions are visited).
- Design decision: add `src/vanilla/wireThinkerTicker.ts` exposing `runVanillaPTicker(thinkerList: ThinkerList): void` that mirrors the P_Ticker call into P_RunThinkers. The focused test exercises the parity-critical semantics: deferred removal, same-tic insertion, self-removal from inside an action, and empty-list no-op. No read-only world/specials file is modified.
- Wrote `src/vanilla/wireThinkerTicker.ts` + `test/plan_final/map/wire-thinker-ticker.test.ts` (7 tests / 19 expect() calls). Dropped a `doomedNode.action === REMOVED` assertion because TS narrows `action` to ThinkFunction after assignment (TS2367); the action-not-invoked assertion already covers the behavior.
- Ran `bun run format` → no fixes.
- Ran `bun test test/plan_final/map/wire-thinker-ticker.test.ts` → 7 pass / 0 fail.
- Cross-plan inventory: `src/world/thinkers.ts` test_importer_count bumped 21 → 22 in `plan_vanilla_parity/current-state/inventory-map-and-world-modules.json` (the new focused test imports thinkers.ts). src_importer_count stays 21 (a diagnostic confirmed 21 is the stable committed value; the 21↔22 flip earlier was a parallel-agent untracked-file race). Inventory test: 23 pass / 0 fail after the bump.
- Ran `bun test` → 16033 pass / 0 fail / 855 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.