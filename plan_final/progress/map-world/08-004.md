# 08-004 wire-line-crossing-and-use

- 2026-05-15 start: selected plan_final step 08-004 (lane map-world) via `bun run plan_final/select-step.ts`. Prerequisites 08-003 (commit f9b9dd9) and 09-003 (commit 10fd45f) are COMPLETED. No prior 08-004 artifacts — fresh step. Goal: wire line crossing, line use, spechit ordering, trigger repeat rules, and switch activation. Read-only: `src/world/useLines.ts` (already read for 09-003), `src/specials/lineTriggers.ts`, `src/specials/switches.ts`. Write lock: `src/world/`, `src/specials/`, `src/vanilla/`, `test/plan_final/map/wire-line-crossing-and-use.test.ts`.
- 2026-05-15 reads: read read-only `src/specials/lineTriggers.ts` (`pCrossSpecialLine` TRIGGERS clear special after dispatch except exit 52/124, RETRIGGERS never clear, monster-eligibility sets, `pUseSpecialLine` back-side gate=only 124, MANUALS/SWITCHES(useAgain=0)/BUTTONS(useAgain=1), changeSwitchTexture guarded on EV_* return except 11/51/138/139, `pShootSpecialLine` unconditional flip; `GameVersion`/`MobjType` const enums) and `src/specials/switches.ts` (`changeSwitchTexture` top→mid→bottom cascade + `i ^ 1` swap + buttons[0].soundorg quirk, `startButton`/`updateButtons`/`createButtonList`/`resetButton`, `initSwitchList`/`switchEpisodeForGameMode`, BUTTONTIME=35, SFX_SWTCHN/SWTCHX, `ButtonWhere` const enum). Inspected 08-001/08-002/08-003 evidence: facades at `src/vanilla/wireXxx.ts`, tests `test/plan_final/map/`; 08-002/08-003 required crossPlanInventory bumps. tsconfig has `verbatimModuleSyntax: true`.
- 2026-05-15 design: `src/vanilla/wireLineCrossingAndUse.ts` = re-export barrel of the three read-only modules (split value `export {}` vs `export type {}` for verbatimModuleSyntax; do NOT re-export the `GameVersion`/`ButtonWhere` const enums to dodge the cross-module const-enum hazard) + frozen `VANILLA_LINE_CROSSING_USE_INVARIANTS` pinning 6 parity rules. Focused test exercises the REAL re-exported functions with plain-object fixtures (LineTriggerLine/SwitchSide/Button — no heavy Mobj), deriving a valid `MobjType` value from re-exported `NON_TRIGGER_PROJECTILE_TYPES[0]` and using line-anchored specifier parsing + dynamic identity imports so the test adds no inventory importer. Expect src-side inventory drift for the three read-only modules; will probe + reconcile per the 08-002/08-003 precedent.
- 2026-05-15 edits: created `src/vanilla/wireLineCrossingAndUse.ts` (re-export barrel split value/type for verbatimModuleSyntax, no const-enum re-export; frozen `VANILLA_LINE_CROSSING_USE_INVARIANTS` 6 rules) and focused test `test/plan_final/map/wire-line-crossing-and-use.test.ts` (real-function behavior: one-shot vs exit-52 special clearing, retrigger non-clearing, back-side use gate, spechitOverrun, changeSwitchTexture flip + BUTTONTIME button revert, line-anchored specifier set, dynamic same-reference identity). No banned casts (used non-null `!` + `unknown` rest params, not `as`). Running verification; will probe+reconcile inventory after the full suite.
- 2026-05-15 verify run 1: format wrapped 2 files; focused 9 pass / 0 fail; no banned casts; full `bun test` 16206 pass, 2 fail = `inventory-ai-and-specials-modules.test.ts` + `inventory-map-and-world-modules.test.ts`. Probed both with the test's exact scan+countImporters: three src-only drifts, all from the new facade's three re-export imports — `src/specials/lineTriggers.ts` src 0→1, `src/specials/switches.ts` src 2→3 (inventory-ai-and-specials-modules.json); `src/world/useLines.ts` src 2→3 (inventory-map-and-world-modules.json). All test counts unchanged (line-anchored/dynamic-import test design held). Reconciled all three manifest entries to live reality (documented bump-on-drift, 08-002/08-003 precedent). Probe reconfirms zero drift across both manifests; removed throwaway probe. Rerunning full verification from the beginning.
