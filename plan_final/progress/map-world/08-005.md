# 08-005 wire-door-specials

- 2026-05-15 start: selected plan_final step 08-005 (lane map-world) as first eligible step. Prerequisites 08-004 + 11-004 COMPLETED (11-004 finalized this session at 3ba85ba/9892125). Goal: wire manual/walk/switch/locked/timed door specials with sounds. Write lock: src/specials/, src/vanilla/, test/plan_final/map/wire-door-specials.test.ts. Read-only: src/specials/doors.ts. Research: AGENTS.md (binding, all session), plan_final/README.md.
- 2026-05-15 reads: src/specials/doors.ts (696 lines). Values: TICRATE 35, VDOORSPEED FRACUNIT*2, VDOORWAIT 150, CLOSE30_TICS 30*35, RAISE_IN_5MINS_TICS 5*60*35, DOOR_CEILING_OFFSET 4<<FRACBITS, SFX_DOROPN 20, SFX_DORCLS 21, SFX_OOF 34, SFX_BDOPN 86, SFX_BDCLS 87, PD_BLUEO/PD_REDO/PD_YELLOWO/PD_BLUEK/PD_REDK/PD_YELLOWK; class VerticalDoor; fns tVerticalDoor, evDoDoor, evDoLockedDoor, evVerticalDoor, spawnDoorCloseIn30, spawnDoorRaiseIn5Mins. Interfaces DoorSector, DoorCallbacks, DoorLineSpecial. CONST ENUMS VerticalDoorType / PlaneMoveResult / DoorDirection — NOT re-exported (verbatimModuleSyntax bans barrel re-export of const enums; consumers import them directly from the read-only module). Cited semantics: blaze doors (types 5/6/7) move at VDOORSPEED*4 but share VDOORWAIT 150 and use SFX_BDOPN/SFX_BDCLS; on close-crush vld_close/vld_blazeClose do NOT reverse while the rest reverse + replay non-blaze sfx_doropn; evDoDoor skips sectors with active specialdata; locked-door key map 99/133→blue 134/135→red 136/137→yellow with -O denial messages vs manual -K; spawnDoorCloseIn30/spawnDoorRaiseIn5Mins clear the sector special once armed. Design: src/vanilla/wireDoorSpecials.ts re-export barrel (split value/type, no const enums) over ../specials/doors.ts + frozen VANILLA_DOOR_SPECIALS_INVARIANTS (5 rules). Inventory-neutral test (constants, typeof fns + VerticalDoor class, frozen manifest, ../-filtered specifier set ['../specials/doors.ts'], dynamic identity). doors.ts in inventory-ai-and-specials — probe + reconcile + sync; pathspec commits.
- 2026-05-15 verify (first pass): `bun run format` fixed 2 (facade + test multiline wrap); grep ` as unknown| as any| as [A-Z]` clean. Focused = 7 pass / 0 fail. Probe replicating the inventory scan + countImporters showed only src/specials/doors.ts src 5->6 (the new facade); test unchanged at 9 — reconciled inventory-ai-and-specials-modules.json doors.ts src_importer_count 5->6. Strengthened the const-enum test (strip the leading JSDoc, then assert the export region omits VerticalDoorType/PlaneMoveResult/DoorDirection) so it survives prettier wrapping. Ran mandatory sync-master-checklist. Full `bun test` reported 1 fail (inventory: demo and replay) + tsc TS2769 — both isolated to a CONCURRENT agent's uncommitted/untracked 12-006 wire-demo-playback-runtime files (NOT in 08-005's write lock). Per the parallel-agent contract did not touch/revert their work; did not commit while the full suite failed; backgrounded a poll for their TS error to clear.
- 2026-05-15 verify (rerun from beginning after concurrent 12-006 cleared): poll exited (their TS2769 fixed + demo inventory reconciled by that agent). Reran the full sequence: `bun run format` 7 files, no fixes; focused = 7 pass / 0 fail; full `bun test` = 16400 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean (zero error TS). Finalizing with the two-commit pathspec pattern, staging ONLY the explicit 08-005 paths (the concurrent agent's 12-006 files are deliberately excluded from every pathspec).
