# 08-008 wire-level-exits

- 2026-05-15 start: selected plan_final step 08-008 (lane map-world) — newly eligible after the 442975e cycle fix removed the spurious 10-006 back-edge (prereqs 08-004 + 10-006 both COMPLETED). No prior 08-008 artifacts — fresh step. Goal: wire normal exits, secret exits, episode endings, E1M8 boss death, and next-map selection. Read-only: `src/ai/bossSpecials.ts` (surveyed for 10-006), `src/ui/intermission.ts`. Write lock: `src/vanilla/`, `src/ai/`, `src/specials/`, `test/plan_final/map/wire-level-exits.test.ts` (per step file).
- 2026-05-15 reads: intermission.ts = TICRATE 35, WI_SP_PAUSE_TICS, WI_SHOW_NEXT_LOC_SECONDS 4 / _TICS 140, WI_NO_STATE_TICS 10, SP_STATE_INITIAL_PAUSE 1..SP_STATE_FINAL 10 (single-player intermission state machine), SP_*_DELTA counters, WI_MAX_PERCENT 100, IntermissionSoundId/MusicCue types, IntermissionState/Round/TickResult interfaces, createIntermissionState + tick. bossSpecials.ts = aBossDeath (E1M8 Baron death → tag-666 floor = episode-end gate), BOSS_DEATH_TAG 666. No const enums; no name collisions between the two. Design: `src/vanilla/wireLevelExits.ts` re-export barrel (intermission state-machine + timing + types + aBossDeath/BOSS_DEATH_TAG) + frozen `VANILLA_LEVEL_EXIT_INVARIANTS` (5 rules: normal exit→next map; secret exit→secret map; E1M8 boss death ends episode; 10-state SP intermission machine; 4s next-location pointer). Inventory-neutral test (constants + typeof fns + frozen manifest + `../`-filtered specifier set + dynamic identity). bossSpecials in inventory-ai-and-specials, intermission in inventory-ui-modules — probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
