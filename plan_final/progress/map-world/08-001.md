# 08-001 wire-level-setup

- 2026-05-14 start: selected plan_final step 08-001 (lane map-world). Goal: wire map bundle setup, player starts, things, blockmap, reject, nodes, subsectors, and sector links into the vanilla runtime. Prerequisites 05-007 (commit `80d64f1`, status `062e53d`) and 04-001 (commit `41ab7c8`, status `0b37e90`) are both COMPLETED. HEAD = origin/main at session start.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs `src/map/mapSetup.ts` (`setupLevel(bundle: MapLumpBundle): MapData` orchestrator that produces vertexes, sectors, sidedefs, linedefs, segs, subsectors, nodes, things, blockmap, reject, subsectorSectors, lineSectors, sectorGroups, validCount in one frozen artifact) and `src/map/mapBundle.ts` (`parseMapBundle(directory, wadBuffer, mapName): MapLumpBundle`, `findMapNames`).
- Design decision: add `src/vanilla/wireLevelSetup.ts` exposing `wireLevelSetup(resourceCache: IwadResourceCache, wadBuffer: Buffer, mapName: string): MapData` that walks the existing read-only pipeline: (1) `parseMapBundle(resourceCache.directory, wadBuffer, mapName)` produces the 10-lump bundle for the named map, (2) `setupLevel(bundle)` produces the final frozen MapData. The wrapper validates `mapName` is non-empty and is one of the names returned by `findMapNames(resourceCache.directory)`; unknown map names throw a typed `WireLevelSetupError` with `reason: 'map-not-found'`. No source file under `src/map/` or `src/world/` is modified.
- Wrote `src/vanilla/wireLevelSetup.ts` (exports WireLevelSetupErrorReason, WireLevelSetupError, wireLevelSetup) and `test/plan_final/map/wire-level-setup.test.ts` with 11 tests covering: file existence + 08-001 citation, import string, public surface arity, WireLevelSetupError shape, live E1M1 + E1M9 load against doom/DOOM1.WAD when present (skipped otherwise), whitespace trim, P_GroupLines invariants, empty-map-name failure, unknown-map-name failure.
- Ran `bun run format` → no fixes applied.
- Ran `bun test test/plan_final/map/wire-level-setup.test.ts` → 11 pass / 0 fail / 34 expect() calls.
- Full suite had intermittent inventory-audio-modules failure under parallel-agent load; retry showed clean 15941 pass / 0 fail / 845 files. The audio inventory passes in isolation; the failure was a race window between parallel commits adjusting importer counts.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.