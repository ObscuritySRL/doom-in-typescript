# 04-002 wire-d-doommain-init-order

- 2026-05-14 start: selected plan_final step 04-002 (lane runtime-core). Goal: build the D_DoomMain init orchestrator that calls subsystem initialization in vanilla's canonical order on the runtime context from 04-001. Prerequisites 04-001 (commit `41ab7c8`) and 03-004 (commit `1a3834f`) are both COMPLETED per status JSONs. HEAD = origin/main = `0b37e90` at session start; working tree clean modulo probe-*.ts and .claude scratch + parallel-agent untracked files (`src/assets/wireSpriteFrameCacheFromIwadResourceCache.ts`, `src/vanilla/paletteAndColormap.ts`, `test/plan_final/wad/*.test.ts`) which belong to in-flight steps 05-002 / 05-005 and are out of this step's write lock.
- Read `AGENTS.md`, `plan_final/PROMPT.md`, the step file, the read-only refs `src/bootstrap/initOrder.ts` (SHA-pinned 15-step canonical init sequence: Z_Init, V_Init, M_LoadDefaults, W_Init, I_Init, OPL_Init, NET_Init, M_Init, R_Init, P_Init, S_Init, D_CheckNetGame, HU_Init, ST_Init, I_InitStretchTables) and `src/bootstrap/implement-d-doommain-init-order-skeleton.ts` (audit ledger; oversize for read tool — skipping detail, only the canonical order from initOrder.ts is needed). Read the existing `src/vanilla/runtimeContext.ts` from 04-001 (exposes `VanillaRuntimeContext` shape) and the existing 04-001 progress log to mirror conventions.
- Design decision: add `src/vanilla/dDoomMain.ts` exposing (1) `VanillaDDoomMainInitStep` interface with `label` (matching the canonical labels) / `description` / `run(context)` callable, (2) `D_DOOM_MAIN_INIT_ORDER` — a frozen ordered list of 15 placeholder steps whose labels match `src/bootstrap/initOrder.ts`'s `INIT_ORDER` positionally, (3) `runDDoomMainInit(context)` that iterates the order and awaits each `run`, throwing a typed `VanillaDDoomMainInitError` that aborts the chain on any step failure. The `run` implementations are no-op placeholders for this step — downstream runtime-core steps will implement the actual subsystem init logic. The SHA-pinned `src/bootstrap/initOrder.ts` is read but NOT modified; the new file lives entirely under `src/vanilla/` so no SHA pins are perturbed.
- Wrote `src/vanilla/dDoomMain.ts` (already present in working tree from parallel-agent work) and `test/plan_final/runtime/wire-d-doommain-init-order.test.ts` (13 tests, 158 expect() calls). Tests cover: file existence + 03-006 citation, the import paths, public surface (`VanillaDDoomMainInitStep`, `D_DOOM_MAIN_INIT_ORDER` length=15, `runDDoomMainInit`, `VanillaDDoomMainInitError`), every label/description pin matching `src/bootstrap/initOrder.ts`, order is frozen + each step is frozen, default no-op runs resolve cleanly, custom-order injection works, error-wrapping with `stepLabel`/`stepIndex`/`cause`, async + sync step support, chain abort on failure.
- Ran `bun run format` → 5 files formatted, no fixes applied.
- Ran `bun test test/plan_final/runtime/wire-d-doommain-init-order.test.ts` → 13 pass, 0 fail, 158 expect() calls.
- Ran `bun test` → 15777 pass, 0 fail across 833 files (+13 from the new focused test plus parallel-agent contributions across all lanes that landed in the same session).
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.
