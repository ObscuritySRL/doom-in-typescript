# 04-007 wire-state-snapshot-hash

- 2026-05-15 start: selected plan_final step 04-007 (lane runtime-core). Goal: add deterministic state snapshot hashing for all final acceptance checkpoints. Prerequisites 04-001, 08-001, 09-001 are all COMPLETED.
- Read the read-only refs `src/oracles/stateHash.ts` (the `StateHashComponent` union = automap|combined|player|rng|sectors|thinkers; `STATE_HASH_COMPONENTS` frozen 6-entry list; `DEFAULT_SAMPLING_INTERVAL_TICS=35`; `INDIVIDUAL_COMPONENT_COUNT=5`; `StateHashEntry` = {tic, hashes: Record<component,string>}) and `src/save/coreSerialization.ts` (the savegame field-size constants).
- Design decision: add `src/vanilla/wireStateSnapshotHash.ts` exposing `hashStateSnapshot(tic, componentBytes): StateHashEntry` — SHA-256 per individual component + combined SHA-256 over the canonically-ordered concatenation of the five digests. Pure, deterministic, frozen result. No read-only oracle/save file is modified.
- Wrote the module + `test/plan_final/runtime/wire-state-snapshot-hash.test.ts` (9 tests / 28 expect() calls): key-set match, tic passthrough, per-component digest correctness, combined-digest derivation, determinism, single-byte sensitivity, 64-hex format.
- Ran `bun run format` → 1 fix (test wrapping).
- Ran focused `bun test` → 9 pass / 0 fail.
- First `bun x tsc --noEmit` failed: `[...STATE_HASH_COMPONENTS].sort()` widened to `string[]`, mismatching `StateHashComponent[]` in `.toEqual`. Fix: `.map(String)` both sides before `.sort()`.
- Reran: focused 9/0, typecheck clean. Full suite: one flaky captureReferenceDemoSync fail on the first run; clean retry at 16061 pass / 0 fail / 859 files. No cross-plan inventory drift (imports only the read-only stateHash.ts whose counts the parallel agents already track).