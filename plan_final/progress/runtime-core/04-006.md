# 04-006 wire-rng-and-demo-streams

- 2026-05-15 start: selected plan_final step 04-006 (lane runtime-core) — newly eligible after the 442975e cycle fix removed the spurious 12-006 back-edge (prereq now just 04-004, COMPLETED). No prior 04-006 artifacts — fresh step. Goal: wire P_Random and M_Random streams and demo ticcmd read/write without cross-stream drift. Read-only: `src/core/rng.ts`, `src/demo/`. Write lock: `src/core/`, `src/demo/`, `src/vanilla/`, `test/plan_final/runtime/wire-rng-and-demo-streams.test.ts` (per step file).
- 2026-05-15 reads: rng.ts = RNG_TABLE (frozen 256-entry rndtable), DoomRandom class (prndindex=P_Random gameplay stream, rndindex=M_Random menu stream — two independent indices over the same table → no cross-stream drift; pRandom()/mRandom() return 0-255). src/demo/: demoFile.ts (DEMO_VERSION_19 109, DEMO_HEADER_SIZE 13, DEMO_TIC_SIZE 4, DEMO_END_MARKER 0x80, DEMO_MAX_PLAYERS 4, DEMO_TIC_RATE 35, parseDemoLump, DemoTicCommand/DemoFile), demoRecord.ts (DemoRecorder + DEMO_RECORD_DEFAULT_MAXIMUM_SIZE 0x20000/HEADROOM 16), demoPlayback.ts (DemoPlayback + DEMO_PLAYBACK_DEFAULT_VERSION), demoParse/compare-* (SKIP — DEMO_END_MARKER/MAX_PLAYERS/TIC_RATE collide with demoFile). No const enums. Design: `src/vanilla/wireRngAndDemoStreams.ts` re-export barrel (rng + demoFile + demoRecord + demoPlayback; shared DEMO_* from demoFile only) + frozen `VANILLA_RNG_DEMO_INVARIANTS` (5 rules). Inventory-neutral test (real DoomRandom determinism + P/M independence, demo constants, frozen manifest, `../`-filtered specifier set, dynamic identity). rng.ts in inventory-core-math-and-timing, demo modules likely a save/demo inventory — probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
