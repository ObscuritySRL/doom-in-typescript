# 04-003 wire-d-doomloop

- 2026-05-14 start: selected plan_final step 04-003 (lane runtime-core). Goal: wire D_DoomLoop setup and per-frame order to live callbacks instead of contract-only calls. Prerequisite 04-001 is COMPLETED at commit `41ab7c8` (status pushed at `0b37e90`). HEAD = origin/main = `5ee37ec` (the 04-002 status SHA I just pushed). Working tree clean modulo unrelated probe-*.ts scratch and parallel-agent untracked files.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs `src/mainLoop.ts` (the existing `MainLoop` class with `setup(preLoopCallbacks)` + `runOneFrame(perFrameCallbacks)`, plus the `MAIN_LOOP_PHASES = ['startFrame', 'tryRunTics', 'updateSounds', 'display']` and `PRE_LOOP_STEPS = ['initialTryRunTics', 'restoreBuffer', 'executeSetViewSize', 'startGameLoop']` constants), and `src/bootstrap/tryRunTics.ts` (the parity-pinned TryRunTics + time-source contracts; only consumed transitively here). Confirmed the existing `MainLoop` already enforces the canonical phase and pre-loop order — this step just needs to wire a runtime-context-driven driver that supplies real callbacks.
- Design decision: add `src/vanilla/runDoomLoop.ts` exposing `runVanillaDoomLoop(runtime, options?)` that constructs a `MainLoop`, builds placeholder callbacks bound to the `VanillaRuntimeContext` (the pre-loop `restoreBuffer` zeros both framebuffers; the per-frame phases push their label into a phase log so the test can prove canonical phase order without depending on real subsystem side effects), runs `setup()` once, then iterates `runOneFrame()` up to `options.maxFrames` (default 60) or until an optional `shouldContinue(runtime, frameIndex)` predicate returns `false`. The driver returns a frozen `VanillaDoomLoopRunResult` with `framesRun`, `preLoopCallOrder`, `perFramePhaseOrder`, and `exitReason: 'maxFrames' | 'predicateRequestedExit'`. The read-only `src/mainLoop.ts` is consumed verbatim — no SHA pins perturbed.
- Wrote `src/vanilla/runDoomLoop.ts` and `test/plan_final/runtime/wire-d-doomloop.test.ts` (15 tests / 40 expect() calls). Tests cover: file existence + 04-003 citation, import string, public surface arity, maxFrames=0/1/3 phase order, default-maxFrames-is-finite invariant, shouldContinue=false-on-first-call exit, shouldContinue-exits-after-N-frames with framesRun=N, restoreBuffer zeros both framebuffers, frozen result + frozen logs, predicate receives same runtime reference and zero-based frame index, and the default-no-options call shape.
- Ran `bun run format` → 7-8 files formatted, no fixes applied.
- Ran `bun test test/plan_final/runtime/wire-d-doomloop.test.ts` → 15 pass, 0 fail, 40 expect() calls.
- First `bun x tsc --noEmit` attempt failed: TS2769 at the focused test line 184 where I used a `let observedRuntime: VanillaRuntimeContext | null = null` and TypeScript narrowed the variable to `null` in the post-loop `expect(observedRuntime).toBe(runtime)`. Fix: capture observations into an array `observedRuntimes: VanillaRuntimeContext[]` and assert `observedRuntimes[0]` against the input runtime, which preserves the type narrowing.
- Reran full verification after the typecheck fix: `bun run format` → no fixes; focused → 15/0; full → 15792 pass / 0 fail / 834 files; typecheck → clean.