# 04-005 wire-game-state-transitions

- 2026-05-15 start: selected plan_final step 04-005 (lane runtime-core), unblocked by completing 07-001 after the e365561 prereq-cycle fix. Goal: implement GS_DEMOSCREEN, GS_LEVEL, GS_INTERMISSION, GS_FINALE, and wipe transition state changes. Prerequisites 04-003 + 07-001 are COMPLETED.
- Read the read-only refs `src/ui/frontEndSequence.ts` (GS_DEMOSCREEN attract-loop primitives), `src/ui/intermission.ts` (GS_INTERMISSION: createIntermissionState/beginIntermission/tickIntermission; TICRATE, WI_* constants), `src/ui/finale.ts` (GS_FINALE: createFinaleState/startFinale/tickFinale/getFinaleScreen). There is no central GameState enum module; the vanilla `gamestate_t` (GS_LEVEL=0, GS_INTERMISSION=1, GS_FINALE=2, GS_DEMOSCREEN=3 — from doomstat.h) is pinned here.
- Design decision: add `src/vanilla/wireGameStateTransitions.ts` exposing `VanillaGameState` (GS_LEVEL=0/GS_INTERMISSION=1/GS_FINALE=2/GS_DEMOSCREEN=3), a frozen `VANILLA_GAME_STATE_TRANSITIONS` 6-entry adjacency manifest, `isWipeTransition` + `isLegalGameStateTransition` predicates, and re-exports of the per-state primitives. No read-only ui file is modified — pure barrel + frozen manifests + predicates.
- Wrote the facade + `test/plan_final/runtime/wire-game-state-transitions.test.ts` (8 tests / 36 expect() calls including the transition adjacency pins, the D_Display wipe rule, legality checks, and same-reference identity).
- Ran `bun run format` → no fixes; focused `bun test` → 8 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/ui/finale.ts` src 0→1 test 1→2; `src/ui/frontEndSequence.ts` src 21→22 test 21→22; `src/ui/intermission.ts` src 0→1 test 1→2 in `inventory-ui-modules.json`. Inventory test: 22 pass / 0 fail; diff is exactly 6 count lines.
- Ran `bun test` → 16160 pass / 0 fail / 873 files. Typecheck → clean.