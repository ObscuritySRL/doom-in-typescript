# 04-001 create-vanilla-runtime-context

- 2026-05-14 start: selected plan_final step 04-001 (lane runtime-core). Goal: create the shared runtime context for game mode, game state, players, assets, maps, audio, UI, and framebuffers. Prerequisites 03-001 (commit `df1882d`) and 05-001 (commit `1a7f952`) are both COMPLETED per status JSONs. Working tree at session start is clean modulo probe-*.ts and .claude scratch files.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs `src/bootstrap/gameMode.ts` (`GameIdentification` interface — already consumed by launchContext + iwadResourceCache), `src/player/playerSpawn.ts` (player spawn/reborn primitives surface), and `src/map/mapSetup.ts` (P_SetupLevel orchestrator that produces a `MapData` snapshot). Also confirmed `MAXPLAYERS = 4` at `src/world/mobj.ts:1283`. The runtime context lives entirely under `src/vanilla/` — no read-only file needs modification.
- Design decision: add `src/vanilla/runtimeContext.ts` exposing a frozen `VanillaRuntimeContext` interface that aggregates the `LaunchContext` (carried verbatim from 03-004), the `IwadResourceCache` (carried from 05-001), flattened `GameIdentification` fields for ergonomic downstream access, a fixed-size MAXPLAYERS=4 `playerSlots` array, an initially-empty `mapState`, an initially-silent `audioState`, a vanilla initial `uiState` (HUD visible, automap/menu closed), and a `framebuffers` pair (`primary` and `back`, each a 64 000-byte indexed buffer matching the vanilla `screens[0]` and `screens[1]` from r_main.c). The whole object is frozen at the top level; nested mutable buffers follow the vanilla in-place-fill pattern with stable object identity.
- 2026-05-14 implementation: wrote `src/vanilla/runtimeContext.ts` exporting `VANILLA_PLAYER_SLOT_COUNT = 4`, the `PlayerSlot`/`RuntimeMapState`/`RuntimeAudioState`/`RuntimeUiState`/`RuntimeFramebuffers`/`VanillaRuntimeContext` interfaces, and the `createVanillaRuntimeContext(launchContext, resourceCache)` factory. Each slot/audio/ui/framebuffers nested object is frozen alongside the top-level context. Framebuffers reuse `createIndexedFramebuffer` from `win32WindowHost.ts` so the 320x200 indexed buffer constant stays sourced from the single host primitives module.
- 2026-05-14 focused test: added `test/plan_final/runtime/create-vanilla-runtime-context.test.ts` covering (1) the wrapper file exists at the canonical path with `04-001`/`createVanillaRuntimeContext` cited in its top comment, (2) the module imports the launch/iwad/win32-host/gameMode wrappers without modifying them, (3) the factory has arity 2 and returns a frozen artifact, (4) the four-slot player roster is frozen with each slot frozen and (`present=false`, `player=null`), (5) gameMode/episodeCount flatten the shareware vs retail IWAD identification correctly, (6) framebuffers are two independent 64 000-byte zero-filled `Uint8Array`s, and (7) two contexts built from the same inputs allocate independent buffers.
- 2026-05-14 verification: `bun run format` formatted runtimeContext.ts (240-column prettier). Focused `bun test test/plan_final/runtime/create-vanilla-runtime-context.test.ts` → 17 pass / 0 fail / 57 expect() calls. Full `bun test` → 15701 pass / 2 fail / 1 error: the two remaining failures are inventory drift in `test/vanilla_parity/current-state/inventory-wad-and-asset-modules.test.ts` caused by parallel-agent untracked files (`src/assets/wireSpriteFrameCacheFromIwadResourceCache.ts` from in-flight step 05-002 in src/assets/, not part of 04-001's write lock) — documented in `memory/plan_vanilla_parity_workflow.md` as the pre-existing inventory drift category. Bumped `plan_vanilla_parity/current-state/inventory-wad-and-asset-modules.json` test_importer_count for src/wad/directory.ts (67→70) and src/wad/header.ts (63→65) to reflect the new test importer that this step adds (one bump for this test plus two bumps that the linter merged in from parallel-agent test additions). `bun x tsc --noEmit --project tsconfig.json` → clean (no output).
