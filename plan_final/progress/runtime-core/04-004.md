# 04-004 wire-try-run-tics

- 2026-05-14 start: selected plan_final step 04-004 (lane runtime-core). Goal: wire TryRunTics to event processing, ticcmd building, game ticker, demo ticker, and render invalidation. Prerequisites 04-003 (commit `cf35f09`, status `d69f2e3`) and 03-006 (commit `8ef34d4`, status `6f9422f`) are both COMPLETED. HEAD = origin/main = `d69f2e3`. Working tree clean modulo unrelated probe scratch and parallel-agent untracked files.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs `src/bootstrap/tryRunTics.ts` (the `TicRunner` class with `gametic` / `maketic` counters, the `BACKUPTICS=12` circular buffer, and the `TicCallbacks` contract with `startTic`/`buildTiccmd`/`ticker`/`advancedemo`/`doAdvanceDemo`) and `src/input/ticcmd.ts` (the `TICCMD_SIZE=8`, `EMPTY_TICCMD`, button-flag constants).
- Design decision: add `src/vanilla/wireTryRunTics.ts` exposing a mutable `VanillaTryRunTicsState` carrying the runtime-driven counters (eventQueueDrainCount, tickerInvocations, advanceDemoInvocations, renderInvalidations, advanceDemoRequested), plus a builder `buildVanillaTicCallbacks(state, eventQueue)` that returns a `TicCallbacks` whose bodies operate on those counters and the queue, plus `runVanillaTryRunTicsDriver(runner, timeSource, state, eventQueue)` that runs one `TicRunner.tryRunTics` and returns the number of tics executed. Each `startTic` drains the event queue into a captured drained-events array; each `ticker` increments a counter and flips the render-invalidated flag; each `doAdvanceDemo` clears `advanceDemoRequested`. The read-only `src/bootstrap/tryRunTics.ts` is consumed verbatim — `TicRunner` is unchanged.
- Wrote `src/vanilla/wireTryRunTics.ts` (exports VanillaTryRunTicsState, createVanillaTryRunTicsState, buildVanillaTicCallbacks, runVanillaTryRunTicsDriver) and `test/plan_final/runtime/wire-try-run-tics.test.ts` with 17 tests / 58 expect() calls. Tests cover: file existence + 04-004 citation, import paths, state seeding, every TicCallbacks slot, EMPTY_TICCMD return value, FIFO event drain, ticker counter + renderInvalidated flag, advancedemo live proxy, doAdvanceDemo clears the flag, single-tic execution, multi-tic execution, zero-tic execution on idle, BACKUPTICS cap, event drain during tryRunTics, demo-advance triggered when requested, demo-advance NOT triggered when not requested.
- Ran `bun run format` → no fixes applied.
- Ran `bun test test/plan_final/runtime/wire-try-run-tics.test.ts` → 17 pass / 0 fail / 58 expect() calls.
- Ran `bun test` → 15809 pass / 0 fail / 835 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.
- Committed at `f8822dcd54bb5f414e1bb1124c2bdf3b04eadb6e` and pushed → `d69f2e3..f8822dc  main -> main`.
- Wrote `plan_final/status/04-004.json` with status COMPLETED and commitSha `f8822dcd54bb5f414e1bb1124c2bdf3b04eadb6e`.