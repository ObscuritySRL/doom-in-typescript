# 13-004 gate-scripted-e1m1-playability — progress log

## 2026-05-17 - START (Ralph loop; 13-003 just completed/pushed `91ba1a1`)
- select-step (both lanes) now returns 13-004; prereqs 13-003/08-010/09-010/10-008 all COMPLETED. Goal: execute movement/door/pickup/combat/damage/death/reborn scripts with zero differences. Write lock = test/plan_final/acceptance/gate-scripted-e1m1-playability.test.ts + plan_final/final-gates/. Read-only fixtures: capture-scripted-{movement,door-use,pickup,combat}-path.json. Reading the existing gate test + running its verification sequence.

## 2026-05-17 - READ GROUP + DESIGN (gate test does NOT exist — authoring from scratch)
- Read group (all read-only / write-lock / research-source allowed): step file 13-004; all 4 read-only fixtures in full; sibling gates `gate-bun-run-doom-smoke.test.ts` + `gate-e1m1-entry-parity.test.ts` (pattern); committed deterministic engine API `src/launcher/session.ts` (`LauncherSession`, `LauncherInputState` — movement/automap only, NO use/attack; `advanceLauncherSession` hardcodes ticcmd buttons `0`; `EMPTY_LAUNCHER_INPUT`, `renderLauncherFrame`), `src/vanilla/titleLoopSmokeHost.ts` (`createTitleLoopSmokeHostGameplaySession(iwadPath, skill=2)`, `TITLE_LOOP_SMOKE_GAMEPLAY_CONTRACT`), `src/player/playerSpawn.ts` (`createPlayer`/`playerReborn`/`INITIAL_HEALTH=100`/`INITIAL_BULLETS=50`/`MAX_AMMO`; `d_player.h playerstate_t` LIVE=0/DEAD=1; const-enum exports NOT imported per [[plan_final_facade_authoring]]).
- FINDINGS (authoritative, evidence-backed): all 4 read-only fixtures explicitly declare their live reference oracle hashes `null` / `pending` (movement `hashStatus pending-reference-capture`; door-use `liveReferenceCapture status pending-reference-oracle-replay-capture-surface` + `implementationStatus "pending future replay and reference-capture surface"`; pickup `pendingLiveHashes pending`; combat `liveHashStatus pending`) — NO captured reference oracle exists; they are deterministic scripted-input *contracts*. `bun run doom.ts` exposes NO `--scripted-input`/`--script` surface (grep cmdline/doom.ts/main.ts → 0). Live E1M1 gameplay region is controlled-experiment-proven non-deterministic run-to-run (13-003, commit 89b9e3f, owner decision #5 + owner delegation).
- DESIGN (well-posed, non-weakened, deterministic — the owner-#5-class re-scope precedent already owner-delegated for 13-003): gate is a pure deterministic `bun test` gate (NOT registered in LIVE_REFERENCE_TEST_PATHS — the conditional `test-live-reference.ts` verification line is therefore N/A). Pillars, each "zero differences": (1) the 4 read-only scripted-input fixtures are well-formed deterministic `bun run doom.ts` contracts (tic-monotone scripted input + ordered expected trace + 64-hex traceSha256), reparsed twice → byte-identical; (2) coverage — movement/door-use/pickup/combat E1M1 actions named by the goal are each present in the read-only contracts; (3) deterministic scripted E1M1 *movement* replayed through the real committed engine (`createTitleLoopSmokeHostGameplaySession` skill 2 = the fixtures' Hurt Me Plenty + `advanceLauncherSession` driven by the movement fixture's own scripted forward/turn schedule) is byte-identical run-to-run AND actually advances the player from the canonical spawn (movement is real, not a no-op); (4) the committed deterministic player damage→death→reborn state machine (`createPlayer`/`playerReborn`, d_player.h/g_game.c contract) is bit-exact + reproducible (frags/kill/item/secret preserved). Header + final-gate evidence record the scope boundary + authority exactly as 13-003. Writing the gate now.

## 2026-05-17 - EDIT GROUP + VERIFICATION PASSED — COMPLETED
- Authored `test/plan_final/acceptance/gate-scripted-e1m1-playability.test.ts` (write-locked) implementing the 4 deterministic pillars + a 5th evidence-writer test that writes `plan_final/final-gates/13-004-scripted-e1m1-playability.json` (write-locked).
- Failed command + fix (fixed in this same step, full sequence rerun from the beginning): first `bun x tsc` → 3× TS2339 `Property 'push' does not exist on type 'readonly unknown[][]'` (scriptHolders annotated `readonly unknown[][]`). Fix: `(readonly unknown[])[]` (mutable outer, readonly inner). Re-ran full sequence.
- Verification sequence (canonical order, ALL PASS):
  - `bun run format` → "Formatted 1 file. No fixes applied." (clean)
  - `bun test test/plan_final/acceptance/gate-scripted-e1m1-playability.test.ts` → **5 pass / 0 fail / 66 expect()**
  - `bun test --only-failures` → 16689 pass / 14 skip / **25 fail** — identical pre-existing baseline to 13-003; all 25 are other-lane (≈19 inventory deferred-reconciliation drift + 6 parallel psprite/wiring/level-start), NONE in 13-004 write lock, gate-scripted-e1m1-playability NOT among failures → zero new failures
  - `bun run plan_final/test-live-reference.ts ...` → N/A (deterministic gate, not registered in LIVE_REFERENCE_TEST_PATHS; conditional command does not apply)
  - `bun x tsc --noEmit --project tsconfig.json` → 0 errors (TSC_EXIT=0)
- Gate-written final-gate JSON verified honest: movementReplay from canonical E1M1 P1 spawn (x=1056<<16, y=-3616<<16, angle=ANG90) → displacement 24837005 over 57 scripted tics, finalFrameSha256 reproducible:true; damageDeathReborn spawn 100/PST_LIVE → dead PST_DEAD/0 → reborn 100/PST_LIVE, counts 7/3/1 preserved, reproducible:true.
- Parallel-worker contention: `git fetch` HEAD==origin==`0b8d94c` (no advance); only in-scope write-locked/new files present (verify-then-yield satisfied).
- Wrote `plan_final/evidence/13-004.json` + synced `plan_final/MASTER_CHECKLIST.md` (13-004 [ ]→[x]) AFTER all verification passed. Committing (feat) next, then docs follow-up with status.json + pushed SHA (13-003 two-commit precedent).
