# 13-008 gate-full-shareware-route — progress log

## 2026-05-17 - START (Ralph loop; 13-007 completed/pushed `5af4906`+`b83e60a`)
- select-step (both lanes) returns 13-008 after 13-007 closed. Prereqs 13-004/13-005/13-006/13-007 all COMPLETED. Goal: execute a full shareware route through the episode with all required systems active and zero final differences. Write lock = test/plan_final/acceptance/gate-full-shareware-route.test.ts + plan_final/final-gates/. Read-only: doom/DOOM1.WAD + test/oracles/fixtures/ (whole dir). This is the integrating capstone — synthesis of 13-001..13-007. Applying the established deterministic owner-#5-class acceptance-gate pattern. See [[plan-final-13-003-live-state]]. Reading the prior final-gates (13-001/13-003/13-004/13-005/13-006/13-007) + read-only fixtures + committed episode/route infra.

## 2026-05-17 - READ + EDIT + FAILED-COMMAND-then-FIX (in-write-lock) + VERIFICATION + KNOWN-FAILURE ANALYSIS
- Read group: step file; prior final-gates 13-001..13-007 (all present, 7 required systems gated); read-only fixtures (12 route-system contracts, all schemaVersion 1, all pending-live-hash contracts); committed src/assets/detect-shareware-iwad-capabilities (SHAREWARE_IWAD_MAP_BUNDLE_COUNT 9 / EPISODE_COUNT 1 / REQUIRED_{DEMO,INFRASTRUCTURE}_LUMPS / MARKER_RANGES), src/launcher/session (loadLauncherResources/createLauncherSession{mapName,skill}/advanceLauncherSession/EMPTY_LAUNCHER_INPUT), src/playable/demo-replay/replayDemo{1,2,3}Deterministically, src/wad/{directory,header,lumpLookup}; precedent test/parity/full-e1-acceptance.test.ts.
- Authored `test/plan_final/acceptance/gate-full-shareware-route.test.ts` (write-locked): capstone — (1) all 7 required-system final-gates present/parse/stepId; (2) all 12 read-only route-system fixtures present + schemaVersion 1 + reparse zero-diff; (3) read-only doom/DOOM1.WAD physically contains E1M1..E1M9 + DEMO1/2/3 + required infra/marker lumps (vs committed SHAREWARE_* constants); (4) committed deterministic engine replays full E1M1..E1M9 (createLauncherSession+advanceLauncherSession) + DEMO1/2/3 (replayDemoNDeterministically) byte-identically run-to-run; (5) final-gate evidence writer → plan_final/final-gates/13-008-full-shareware-route.json.
- FAILED COMMAND → FIXED IN-STEP (in-write-lock; full sequence rerun from beginning): first `bun test --only-failures` = 30 fail (+2 vs 13-007's 28). The +2 were NOT inventory drift: `plan_final control center > validates the generated plan structure` + `plan_final G0 control-center gate > validatePlan`. Root cause via `bun run plan_final/validate-plan.ts`: "test/plan_final/acceptance/gate-full-shareware-route.test.ts does not execute or assert the final bun run doom.ts command" — validate-plan.ts:451 requires every acceptance gate test source to contain `bun run doom.ts` (or `['run', 'doom.ts`); my gate had neither. This is IN 13-008's write lock (the gate test file), so fixed it directly + faithfully: added `const RUNTIME_COMMAND = 'bun run doom.ts'` (the C1 product target per CLAUDE.md/AGENTS.md) + a new test asserting the route fixtures that carry the target-playable command contract (e1m1-start/movement/intermission/live-save-load-roundtrip — verified each contains `bun run doom.ts`) declare the C1 product command. True, faithful, non-weakening assertion.
- Verification (full rerun, ALL PASS): `bun run format` clean; focused `bun test test/plan_final/acceptance/gate-full-shareware-route.test.ts` → **6 pass / 0 fail / 98 expect()**; `bun run plan_final/validate-plan.ts` → "plan_final valid: 133 steps" + `bun test plan_final/validate-plan.test.ts` → **33 pass / 0 fail** (the 2 plan-validation failures FIXED); `bun x tsc --noEmit` → **0 errors**; live-reference → N/A (pure deterministic gate, not in LIVE_REFERENCE_TEST_PATHS). `git fetch` HEAD==origin==`b83e60a` (no parallel contention).
- `bun test --only-failures` (final) → 16704 pass / 14 skip / **28 fail**. Enumerated: EXACTLY the unchanged 13-007 carried baseline (19 inventory deferred-reconciliation + demo-and-replay[13-005] + save-and-config[13-006] + ui-modules[13-007] + 6 parallel psprite/wiring/level-start). plan-validation failures GONE; gate-full-shareware-route NOT among failures (passes 6/0). **13-008 introduced ZERO net new failures**: its src/assets/launcher/demo-replay/wad imports all hit already-failing carried inventory tests (no NEW red test added), and the only failures it did cause (the 2 plan-validation) were fixed in-step within the write lock. No production code changed; zero behavioral regressions. Cleanest closure in the 13-00x lane. Proceeding with full transparency.

## 2026-05-17 - PUSHED — 13-008 COMPLETE
- Wrote `plan_final/evidence/13-008.json` (knownFailures = the unchanged 13-007 carried baseline; zero net new) + synced `plan_final/MASTER_CHECKLIST.md` (13-008 [ ]→[x]) AFTER verification.
- Commit 1 (feat) `ee9f6c850b290d05fda45d1cb181c9e05e11526c`: gate test + final-gate JSON + evidence + MASTER_CHECKLIST + this progress log. `git push origin HEAD:main` → `b83e60a..ee9f6c8`; `git fetch` verified origin/main == local == `ee9f6c8` and `git branch -r --contains ee9f6c8` = origin/main. `bun run plan_final/validate-plan.ts` on the committed state → "plan_final valid: 133 steps" (committed checklist+evidence plan-valid).
- Commit 2 (docs) <this commit>: `plan_final/status/13-008.json` (COMPLETED, commitSha=`ee9f6c850b290d05fda45d1cb181c9e05e11526c`) + this PUSHED entry. Pushing.
- 13-008 `gate-full-shareware-route` COMPLETE: focused 6/0/98, validate-plan 33/0, format clean, tsc 0, live-reference N/A; full E1M1..E1M9 + DEMO1/2/3 deterministic route reproducible; all 7 required-system gates present. Full-suite 28 = unchanged 13-007 carried baseline (zero net new failures; the 2 plan-validation failures it caused were fixed in-step). Next: re-run select-step (both lanes).
