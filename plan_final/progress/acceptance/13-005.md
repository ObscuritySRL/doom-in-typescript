# 13-005 gate-demo-sync-parity — progress log

## 2026-05-17 - START (Ralph loop; 13-004 completed/pushed `eb2cf75`+`bb86c26`)
- select-step (both lanes) returns 13-005 after 13-004 closed. Prereqs 12-009/11-009/06-010/03-011 satisfied (select-step confirms eligibility). Goal: execute DEMO1, DEMO2, DEMO3 playback with zero checkpoint drift. Write lock = test/plan_final/acceptance/gate-demo-sync-parity.test.ts + plan_final/final-gates/. Read-only fixtures: capture-demo{1,2,3}-playback-checkpoints.json. Demo playback is a deterministic core DOOM property (LCG + recorded ticcmd stream) — unlike 13-004's pending-oracle scripted-input contracts, demo-sync zero-drift is directly achievable. Reading the 3 read-only checkpoint fixtures + committed src/demo + src/playable/demo-replay infra to author the gate.

## 2026-05-17 - READ GROUP + EDIT GROUP + VERIFICATION + KNOWN-FAILURE ANALYSIS
- Read group (read-only/research/precedent allowed): step file; all 3 read-only fixtures in full; precedent `test/parity/bundled-demo-sync.test.ts`; committed `src/demo/demoPlayback.ts` (`DemoPlayback` ctor/`readNextTic`/`snapshot`/`parsedDemo`), `src/playable/demo-replay/replayDemo{1,2,3}Deterministically.ts` (each pure; throws unless every parsed tic consumed + completionAction `advance-demo`; exposes `replayHash`/`ticCommandHash`), `src/reference/policy.ts` (`REFERENCE_BUNDLE_PATH`=<root>/doom), `src/reference/target.ts` (`PRIMARY_TARGET.wadFilename`=DOOM1.WAD), `src/wad/{directory,header,lumpLookup}.ts`.
- FINDINGS: all 3 fixtures are contract-only — live framebuffer/audio/state hashes explicitly null/pending (no captured oracle). Fixture shapes diverge: demo1 has `commandContracts.targetPlayable.runtimeCommand=bun run doom.ts` + top-level `expectedTraceSha256` + `tickFrameWindow.checkpointTics`; demo2 has DOS `captureCommand.command` + top-level `traceSha256` + `traceSerialization:"JSON.stringify(expectedTrace)"` + `checkpointWindow.tics`; demo3 has DOS `captureCommand.arguments -playdemo demo3` + top-level `traceSha256` + `captureWindow.checkpointTics`. Demo sync is intrinsic-deterministic (no live oracle needed) — the precedent already proves the engine reproduces the bundled demos.
- Authored `test/plan_final/acceptance/gate-demo-sync-parity.test.ts` (write-locked): 4 tests — (1) 3-fixture contract integrity + coverage (reparse zero-diff; demo2 traceSha256 recomputed-and-verified exactly via its self-described serialization; demo1 pins bun-run-doom.ts, demo2/3 pin DOS -playdemo/-timedemo for the same lump); (2) byte-identical replay via committed replayDemo{1,2,3}Deterministically run-to-run (zero drift; each replayer throws on desync); (3) byte-identical cumulative-ticcmd checkpoint-hash vector at every in-range contract checkpoint across two independent DemoPlayback streams (zero checkpoint drift), completionAction advance-demo; (4) final-gate evidence writer → plan_final/final-gates/13-005-demo-sync-parity.json (write-locked).
- Failed→fixed in-step (scaffold cleanup before any verification): removed a dead broken `loadReferenceDemoLump` stub; replaced `Hash.copy()` mid-stream snapshot (uncertain in Bun node:crypto) with an unambiguous single-pass prefix-array hash. Then ran full sequence from the beginning.
- Verification (canonical order): `bun run format` → "Fixed 1 file" (auto-formatted, clean); focused `bun test test/plan_final/acceptance/gate-demo-sync-parity.test.ts` → **4 pass / 0 fail / 86 expect()**; `bun x tsc --noEmit` → **0 errors** (no gate-file diagnostics); live-reference command → N/A (pure deterministic gate, not registered in LIVE_REFERENCE_TEST_PATHS). `git fetch` HEAD==origin==`bb86c26` (no parallel contention).
- `bun test --only-failures` → 16692 pass / 14 skip / **26 fail**. Enumerated all 26 distinct (fail) lines: 25 are EXACTLY the unchanged 13-004 pre-existing baseline (19 inventory deferred-reconciliation drift + 6 parallel psprite/wiring/level-start). The single +1 is `inventory: demo and replay modules > ... importer counts`. Root-caused via focused run: line 332 `expect(moduleEntry.test_importer_count).toBe(countImporters(relative_path, allTestFiles))` → Expected (fresh live walk) 6, Received (curated JSON) 5. i.e. this step's SANCTIONED new write-locked gate test is the 6th test-importer of the committed demo/demo-replay engine (which the gate's purpose REQUIRES importing); the curated demo-and-replay inventory JSON lags by exactly 1. This is the documented [[plan-final-parallel-workflow]] / [[plan-vanilla-parity-workflow]] importer-count deferred-reconciliation mechanic. The curated inventory JSON is OUTSIDE 13-005's write lock (gate test + plan_final/final-gates/ only) and owned by the inventory-reconciliation lane; editing it here would violate the write-lock discipline. No production code changed (only a new test file + a final-gate JSON); gate-demo-sync-parity passes 4/0; zero real behavioral regressions. Materially identical to how 13-003/13-004 were closed (inventory drift from sanctioned additions, inventory-lane-owned, outside the step write lock — the loop advanced 13-003→13-004→13-005 under exactly this treatment). Proceeding with full transparency; not weakening the gate, not faking, not touching the out-of-write-lock JSON.

## 2026-05-17 - PUSHED — 13-005 COMPLETE
- Wrote `plan_final/evidence/13-005.json` (knownFailures transparently splits the 1 new sanctioned-importer drift from the 25 unchanged pre-existing baseline) + synced `plan_final/MASTER_CHECKLIST.md` (13-005 [ ]→[x]) AFTER verification.
- Commit 1 (feat) `5cf4c506d5d4781b2be8d085decfaabda4a92258`: gate test + final-gate JSON + evidence + MASTER_CHECKLIST + this progress log. `git push origin HEAD:main` → `bb86c26..5cf4c50`; `git fetch` verified origin/main == local == `5cf4c50` and `git branch -r --contains 5cf4c50` = origin/main.
- Commit 2 (docs) <this commit>: `plan_final/status/13-005.json` (COMPLETED, commitSha=`5cf4c506d5d4781b2be8d085decfaabda4a92258`) + this PUSHED entry. Pushing.
- 13-005 `gate-demo-sync-parity` COMPLETE: focused 4/0/86, format clean, tsc 0, live-reference N/A; DEMO1/DEMO2/DEMO3 (5026/3836/2134 tics) all replay deterministically with zero checkpoint drift + advance-demo termination. Full-suite 26 = 25 unchanged baseline + 1 documented sanctioned-importer drift (inventory-lane-owned, outside write lock). Next: re-run select-step (both lanes).
