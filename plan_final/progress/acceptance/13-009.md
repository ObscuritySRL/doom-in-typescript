# 13-009 gate-registered-iwad-when-present — progress log

## 2026-05-17 - START (Ralph loop; 13-008 completed/pushed `ee9f6c8`+`c7d44ae`)
- select-step (both lanes) returns 13-009 after 13-008 closed. Prereq 13-008 COMPLETED. Goal: run registered DOOM IWAD parity smoke ONLY when a user-supplied registered IWAD is present. Write lock = test/plan_final/acceptance/gate-registered-iwad-when-present.test.ts + plan_final/final-gates/. Read-only: iwad/ (gitignored, likely absent in this env) + src/reference/target.ts. This gate is CONDITIONAL by design (registered smoke gated on presence). Applying the established deterministic owner-#5-class acceptance-gate pattern adapted for the present/absent guard. See [[plan-final-13-003-live-state]]. Reading iwad/ contents + src/reference/target.ts + committed detect-registered-iwad-capabilities + precedent.

## 2026-05-17 - RESUME (continue Ralph 13-009; select-step both lanes → 13-009; HEAD=c7d44ae)
- Untracked artifacts present from a prior START (gate test + final-gate JSON skeleton + this log). Verify-then-complete: the gate test follows the established deterministic owner-#5-class pattern (pure bun test, NOT in LIVE_REFERENCE_TEST_PATHS, contract integrity + presence-gated conditional registered smoke + final-gate evidence write). Confirming imports exist, then running the full verification sequence.

## 2026-05-17 - EDIT + VERIFICATION + KNOWN-FAILURE ANALYSIS — COMPLETE (note: a parallel worker appended the RESUME entry above; verify-then-yield — this agent has verified evidence and proceeds to commit; HEAD==origin==c7d44ae, neither has committed 13-009)
- Authored `test/plan_final/acceptance/gate-registered-iwad-when-present.test.ts` (write-locked): 4 tests — (1) registered-IWAD scope contract (PRIMARY_TARGET shareware; registered=separate user-supplied DOOM.WAD; 27 maps/3 episodes; smoke = `bun run doom.ts -iwad iwad/DOOM.WAD`); (2) the shareware iwad/DOOM1.WAD copy is NOT a registered IWAD (isRegisteredIwad false, mapBundleCount≠27, ep2/ep3=0) so the smoke stays gated off + registered IWAD absent here; (3) presence-gated registered parity smoke (present ? test : test.skip — correctly SKIPPED, no user IWAD); (4) final-gate evidence writer → plan_final/final-gates/13-009-registered-iwad-when-present.json. Gate source contains literal `bun run doom.ts` (the 13-008 validate-plan.ts:451 lesson applied up-front).
- Verification (canonical order, ALL PASS, no fix needed): `bun run format` clean; focused → **3 pass / 1 skip / 0 fail / 15 expect()** (registered smoke correctly skipped — the gate-passing state with no user IWAD); `bun run plan_final/validate-plan.ts` → "plan_final valid: 133 steps" + `bun test plan_final/validate-plan.test.ts` → **33 pass / 0 fail**; `bun x tsc --noEmit` → **0 errors**; live-reference → N/A. `git fetch` HEAD==origin==`c7d44ae` (no commit advance; parallel worker active on this log only).
- `bun test --only-failures` (final) → 16707 pass / 15 skip / **28 fail** = EXACTLY the unchanged 13-008 carried baseline (19 inventory deferred-reconciliation + demo-and-replay/save-and-config/ui-modules carried + 6 parallel psprite/wiring/level-start); zero non-baseline suspects; plan-validation clean; gate NOT among failures. **13-009 introduced ZERO net new failures** (src/assets/reference/wad imports all hit already-failing carried inventory tests). 15 skip = +1 (conditional registered-smoke correctly skipped — the designed "only when present" behavior). No production code changed; zero regressions. Writing evidence/status/MASTER_CHECKLIST, then pathspec-commit + push (verify-then-yield: if parallel worker pushed 13-009 first, fetch+reconcile).

## 2026-05-17 - PUSHED — 13-009 COMPLETE
- Wrote `plan_final/evidence/13-009.json` + synced `plan_final/MASTER_CHECKLIST.md` (13-009 [ ]→[x]) AFTER verification.
- Commit 1 (feat) `b9c369272d82060b39189c599ffe39ae23adb375`: gate test + final-gate JSON + evidence + MASTER_CHECKLIST + this progress log. `git push origin HEAD:main` → `c7d44ae..b9c3692` (CLEAN — won the parallel-worker race, no push conflict); `git fetch` verified origin/main == local == `b9c3692` and `git branch -r --contains b9c3692` = origin/main.
- Commit 2 (docs) <this commit>: `plan_final/status/13-009.json` (COMPLETED, commitSha=`b9c369272d82060b39189c599ffe39ae23adb375`) + this PUSHED entry. Pushing.
- 13-009 `gate-registered-iwad-when-present` COMPLETE: focused 3 pass / 1 skip / 0 fail, validate-plan 33/0, format clean, tsc 0, live-reference N/A; the conditional registered-smoke correctly skipped (no user IWAD = the designed only-when-present gate-passing state). Full-suite 28 = unchanged 13-008 carried baseline (zero net new failures). Next: re-run select-step (both lanes).
