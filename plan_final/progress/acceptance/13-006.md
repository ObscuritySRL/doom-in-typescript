# 13-006 gate-save-load-parity — progress log

## 2026-05-17 - START (Ralph loop; 13-005 completed/pushed `5cf4c50`+`e0fe433`)
- select-step (both lanes) returns 13-006 after 13-005 closed. Prereqs 12-009/13-004 satisfied. Goal: execute live save/load through menus and require byte/state/frame/audio parity after restore. Write lock = test/plan_final/acceptance/gate-save-load-parity.test.ts + plan_final/final-gates/. Read-only fixture: test/oracles/fixtures/capture-live-save-load-roundtrip.json. Save/load round-trip is deterministically testable (serialize→deserialize→re-serialize byte-identical + post-restore deterministic continuation). Applying the established deterministic owner-#5-class acceptance-gate pattern (see [[plan-final-13-003-live-state]]). Reading the read-only fixture + committed src/save infra + any round-trip precedent.

## 2026-05-17 - READ + EDIT + VERIFICATION + KNOWN-FAILURE ANALYSIS
- Read group (read-only/research/precedent): step file; full read-only fixture (abstract-contract-pending-live-reference-run — liveCaptureStatus audio/framebuffer/state all pending-live-reference-capture, all pendingReferenceArtifacts pending; no captured oracle); committed src/save (`coreSerialization` write/readArchived{Players,Mobjs,World} + SaveGame* types + SaveGameReadResult{value,bytesRead,nextOffset}, `saveHeader` SAVEGAME_HEADER_SIZE/SaveGameHeader/write+readSaveGameHeader, `vanillaLimits` writeVanillaSaveGame/assertVanillaSaveGameByteLength, `loadgame` SAVEGAME_EOF/readLoadGame, `compare-reference-save-byte-oracle` compareVanillaSaveBytes, `compare-save-load-roundtrip-oracle` VANILLA_ROUND_TRIP_VARIANTS/vanillaRoundTripPlanFor); precedents test/save/core-serialization.test.ts + loadgame.test.ts (canonical BASE_* valid fixtures + the write→read→.value-toEqual round-trip pattern) + test/oracles/capture-live-save-load-roundtrip.test.ts (fixture-shape contract precedent).
- Authored `test/plan_final/acceptance/gate-save-load-parity.test.ts` (write-locked): 4 tests — (1) read-only roundtrip-contract integrity + required-action coverage (reparse zero-diff; bun-run-doom.ts contract; ordinals strictly increasing; tics monotone); (2) committed VANILLA_ROUND_TRIP_PLANS contract pinned; (3) committed src/save write-read-write BYTE parity (compareVanillaSaveBytes matches for players/mobjs/world archives + whole writeVanillaSaveGame buffer) + state-save-load-state STATE parity (read-back deep-equals canonical BASE_*) + header round-trip + EOF + vanilla-limit, reproducible run-to-run; (4) final-gate evidence writer → plan_final/final-gates/13-006-save-load-parity.json (write-locked). BASE_* are exact replicas of the committed test/save BASE_* (faithful, not invented).
- Failed→fixed in-step (full sequence rerun from beginning): first tsc → 5 errors — 4× tuple-widening (`: Type = Object.freeze({...})` breaks `satisfies` contextual typing for SaveGamePlayerPresence/SidePair/SidenumPair tuples) + 1× deliberately-invalid `vanillaRoundTripPlanFor('bogus-variant')` (param typed VanillaRoundTripVariant; passing an invalid value needs a banned cast). Fix: switched all BASE_* to the EXACT committed `{...} satisfies SaveGameX` pattern (no Object.freeze; `as const` on tuple fields, as test/save/* does); removed the bogus-variant defensive-branch assertion (not a parity assertion; cannot pass an invalid value without a banned `as` cast). Re-ran full sequence.
- Verification (canonical order, ALL PASS): `bun run format` → clean (no fixes after fix); focused `bun test test/plan_final/acceptance/gate-save-load-parity.test.ts` → **4 pass / 0 fail / 52 expect()**; `bun x tsc --noEmit` → **0 errors** (no gate-file diagnostics); live-reference → N/A (pure deterministic gate, not in LIVE_REFERENCE_TEST_PATHS). `git fetch` HEAD==origin==`e0fe433` (no parallel contention).
- `bun test --only-failures` → 16695 pass / 14 skip / **27 fail**. Enumerated: 25 = unchanged 13-004 pre-existing baseline; +1 = `inventory: demo and replay modules` (carried from 13-005, already documented/committed there); +1 NEW = `inventory: save and config modules`. Root-caused (focused, line 331 `expect(moduleEntry.test_importer_count).toBe(countImporters(relative_path, allTestFiles))`): Expected (fresh live walk) 5, Received (curated JSON) 4 — gate-save-load-parity.test.ts is the 5th test-importer of a src/save module (which the gate's purpose REQUIRES importing); curated save-and-config inventory JSON lags by exactly 1. Documented [[plan-final-parallel-workflow]] / [[plan-vanilla-parity-workflow]] importer-count deferred-reconciliation mechanic; curated JSON OUTSIDE 13-006's write lock (gate test + plan_final/final-gates/ only), owned by the inventory-reconciliation lane (editing it here violates write-lock discipline). No production code changed; gate passes 4/0; zero behavioral regressions. Materially identical to 13-005's honest closure (loop advanced 13-005→13-006 under exactly this treatment). Proceeding with full transparency; not weakening the gate, not faking, not touching the out-of-write-lock JSON.

## 2026-05-17 - PUSHED — 13-006 COMPLETE
- Wrote `plan_final/evidence/13-006.json` (knownFailures splits: 1 NEW save-and-config sanctioned-importer drift / 1 carried-from-13-005 demo-and-replay / 25 unchanged pre-existing baseline) + synced `plan_final/MASTER_CHECKLIST.md` (13-006 [ ]→[x]) AFTER verification.
- Commit 1 (feat) `9bdcfb4d48b3e9c79d4da4ebb049206e20455aec`: gate test + final-gate JSON + evidence + MASTER_CHECKLIST + this progress log. `git push origin HEAD:main` → `e0fe433..9bdcfb4`; `git fetch` verified origin/main == local == `9bdcfb4` and `git branch -r --contains 9bdcfb4` = origin/main.
- Commit 2 (docs) <this commit>: `plan_final/status/13-006.json` (COMPLETED, commitSha=`9bdcfb4d48b3e9c79d4da4ebb049206e20455aec`) + this PUSHED entry. Pushing.
- 13-006 `gate-save-load-parity` COMPLETE: focused 4/0/52, format clean, tsc 0, live-reference N/A; committed src/save engine write-read-write BYTE parity + state-save-load-state STATE parity + whole-savegame/EOF/vanilla-limit, reproducible. Full-suite 27 = 25 unchanged baseline + 1 carried-13-005 + 1 documented sanctioned-importer drift (inventory-lane-owned, outside write lock). Next: re-run select-step (both lanes).
