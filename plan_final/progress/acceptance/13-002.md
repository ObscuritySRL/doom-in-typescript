# 13-002 gate-title-menu-parity

## 2026-05-15 - start
- Initial `git status --short` showed only untracked `.claude/*` and root `probe-*.ts` files; they are intentionally ignored unless they become tracked.
- Checked tracked repo skill `.claude/skills/verify-step/SKILL.md`. It partially applies to verification discipline, but this step follows the stricter step-specific command order including serialized live-reference runs.
- Read AGENTS.md, plan_final/PROMPT.md, the 13-002 step file, 13-001 evidence/progress, title smoke host/runtime/menu wiring files, the two oracle fixture manifests, and the reference menu route capture tool.
- Current implementation fact: 13-001 added a live TITLEPIC smoke host for `bun run doom.ts -iwad doom/DOOM1.WAD`, but it does not yet consume posted key messages or render menu overlays. 13-002 needs the live route title -> Escape -> main menu -> Enter -> episode menu, with zero-diff evidence under `plan_final/final-gates/13-002-*`.

## 2026-05-15 - read group and edit plan
- Read plan_final/README.md, plan_final/MASTER_CHECKLIST.md, plan_final/liveReferenceTestConfig.ts, plan_final/test-live-reference.ts, the 13-001 acceptance test, live-reference gate test, 02-004 evidence/status, and nearby front-end/menu rendering helpers.
- Existing `captureReferenceMenuRoute` can supply the live Chocolate Doom title/main/episode normalized frame hashes. The new 13-002 test will run that reference capture first, then run `bun run doom.ts -iwad doom/DOOM1.WAD` in the current smoke host and compare the current normalized frame hashes to the matching reference title, step 0, and step 1 hashes.
- Edit plan: extend `src/vanilla/titleLoopSmokeHost.ts` to process `WM_KEYDOWN` messages, open the main menu on Escape, open the episode menu on Enter, and render menu title/items/skull patches over the frozen TITLEPIC frame. Then add/register the focused live acceptance test and final-gate evidence writer.

## 2026-05-15 - edit group and focused headless check
- Updated `src/vanilla/titleLoopSmokeHost.ts` to drain posted Win32 key messages, route Escape/Enter through the existing title/menu state machine, and draw M_DOOM/M_EPISOD menu overlays over the TITLEPIC frame.
- Added `test/plan_final/acceptance/gate-title-menu-parity.test.ts`, registered it in `plan_final/liveReferenceTestConfig.ts`, and updated the live-reference gate test's pinned path list.
- `bun run format` passed.
- Focused headless `bun test test/plan_final/acceptance/gate-title-menu-parity.test.ts` passed: 2 pass, 1 live skip.

## 2026-05-15 - failed focused live command
- `bun run plan_final/test-live-reference.ts test/plan_final/acceptance/gate-title-menu-parity.test.ts` failed in the live assertion. The current capture returned the same normalized hash for title, main-menu, and episode-menu, so the posted Escape/Enter messages are not reaching the smoke host yet.
- The live failure also showed the current title normalized hash differs from the reference title normalized hash; first fix target is input delivery, then the remaining frame-diff source.

## 2026-05-15 - parity fixes
- Fixed the smoke host input path by polling a serialized control file from the live test and bounding the Win32 message drain so the Bun loop continues to observe test commands.
- Replaced direct GDI 320x200 -> 640x480 stretching in the smoke host with Chocolate Doom's 2x aspect-corrected palette stretch: raw PLAYPAL blend tables for 20%/40% scanline blending, display palette gamma/mask conversion, and zero alpha in captured BGRA.
- Fixed menu overlay details needed for the live route: vanilla skull y offset (`menu.y - 5`) and Chocolate 2.2.1's visible three-row shareware episode menu for the DOOM.EXE reference route.
- After those fixes, the focused live command passed and wrote `plan_final/final-gates/13-002-title-menu-parity.json` with zero framebuffer differences for title-attract, main-menu, and episode-menu. Temporary frame-dump instrumentation was removed before restarting final verification.

## 2026-05-15 - verification restart 1
- Restarted the required verification sequence after cleanup. `bun run format` and the focused headless test passed.
- `bun test --only-failures` then failed the UI inventory importer-count check because `src/vanilla/titleLoopSmokeHost.ts` now imports `src/ui/frontEndSequence.ts` and `src/ui/menus.ts`.
- Updated `plan_vanilla_parity/current-state/inventory-ui-modules.json` to reconcile `src/ui/frontEndSequence.ts` source importers 24 -> 25 and `src/ui/menus.ts` source importers 25 -> 26. Verification must restart from `bun run format`.

## 2026-05-15 - verification restart 2
- Restarted verification after the inventory reconciliation. `bun run format`, the focused headless test, `bun test --only-failures`, the focused live-reference gate, and the full serialized live-reference suite passed.
- `bun x tsc --noEmit --project tsconfig.json` then failed because the new acceptance test typed the scripted key step as full captured `MenuRouteStepEvidence`. Fixed the helper signature to accept `MenuRouteKeyStep`, which is the actual contract consumed there. Verification must restart from `bun run format`.

## 2026-05-15 - final verification pass
- Restarted verification after stabilizing the skull-frame capture and the `MenuRouteKeyStep` type fix.
- Passed in order: `bun run format`; `bun test test/plan_final/acceptance/gate-title-menu-parity.test.ts` (2 pass, 1 live skip); `bun test --only-failures` (16441 pass, 13 skip); focused live reference (3 pass); full live-reference suite (73 pass); `bun x tsc --noEmit --project tsconfig.json`.
- Wrote `plan_final/evidence/13-002.json`, `plan_final/status/13-002.json`, and final zero-diff evidence in `plan_final/final-gates/13-002-title-menu-parity.json`.
