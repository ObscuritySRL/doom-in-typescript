# 00-005 lock-parallel-lane-contract

- 2026-05-14 start: claimed step 00-005 (no in-flight progress log; selector returned it as first eligible).
- Goal: validation must reject lane ownership drift and reject overlapping write locks across plans, except documented shared control files.
- Write lock: plan_final/PARALLEL_WORK.md, plan_final/validate-plan.ts, test/plan_final/validate-plan.test.ts.
- Read-only refs: plan_final/PARALLEL_WORK.md, plan_final/MASTER_CHECKLIST.md.
- About to read read-only references.
- Reads complete: PARALLEL_WORK.md tabulates 14 lane rows with `owns` paths and a `blocked by` column; MASTER_CHECKLIST.md tabulates 132 steps across the same 14 lanes. PARALLEL_WORK.md already documents that `src/specials/` is owned by both `map-world` and `ai-specials`, and `src/demo/` is owned by both `runtime-core` and `save-config-demo`. The current `validateLaneCoverage` only checks lane names are present in PARALLEL_WORK.md; nothing validates ownership uniqueness or cross-lane write-lock overlap.
- Plan:
  - Export `findLaneOwnershipConflicts(lanes, documentedSharedOwns)` flagging duplicate non-shared ownership claims and lanes with empty `owns`.
  - Export `findCrossLaneWriteLockOverlaps(steps, sharedPaths)` flagging any non-shared path that appears in two different lanes' write locks; default shared list is `plan_final/evidence/`, `plan_final/status/`, `plan_final/lane_locks/`, `plan_final/MASTER_CHECKLIST.md`.
  - Wire both checks into `validatePlan()` and add focused tests covering: synthetic duplicate ownership flagged, synthetic empty-owns flagged, synthetic cross-lane overlap flagged, shared-control allowlist allows the overlap, current FINAL_PLAN_LANES and FINAL_PLAN_STEPS pass both checks.
  - PARALLEL_WORK.md left as-is - the documented shared-ownership rows for `src/specials/` and `src/demo/` are already covered by the `documentedSharedOwns` allowlist.
- Edits applied: added FinalPlanLane import, LaneOwnershipViolation + WriteLockOverlap types, DEFAULT_DOCUMENTED_SHARED_OWNS and DEFAULT_SHARED_WRITE_LOCK_PATHS constants, and exported findLaneOwnershipViolations() + findCrossLaneWriteLockOverlaps() pure helpers in plan_final/validate-plan.ts. validatePlan() now iterates both checks after acceptance/final-gates validation.
- Edits applied: imported the two new helpers in test/plan_final/validate-plan.test.ts and appended a `plan_final parallel lane contract` describe block with 8 cases: current FINAL_PLAN_LANES has no ownership violations, duplicate ownership flagged, shared-ownership accepted, empty owns flagged, current FINAL_PLAN_STEPS has no cross-lane overlaps, synthetic cross-lane overlap flagged, same-lane overlap allowed, shared-control overlap allowed.
- Verification 1: bun test test/plan_final/validate-plan.test.ts -> 33 pass, 0 fail, 568 expect() calls in 72.00ms.
- Verification 2: bun run format -> 2 files formatted, no fixes applied.
- Verification 3: bun test -> 15408 pass, 0 fail across 804 files in 23.06s.
- Verification 4: bun x tsc --noEmit --project tsconfig.json -> clean.
- About to write evidence/status, stage explicit paths, commit, and push.
- Commit: a1f1839d5c191cd99853421be82c6961d1870141 (feat(plan_final): lock parallel lane contract with ownership and write-lock overlap checks).
- Push: git push -> ca34beb..a1f1839 main -> main (remote now contains the commit).
