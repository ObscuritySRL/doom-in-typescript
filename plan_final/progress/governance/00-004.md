# 00-004 lock-step-selector-contract

- 2026-05-14 start: selected step 00-004 via `bun run plan_final/select-step.ts` (00-002 already claimed by a parallel agent with an in-progress progress log).
- Goal: prove the selector chooses the first eligible unchecked step by lane, with completed-status skipping and prerequisite blocking, without scanning unrelated implementation files.
- Write lock: plan_final/select-step.ts, test/plan_final/select-step.test.ts.
- Read-only refs: plan_final/select-step.ts (existing), plan_final/MASTER_CHECKLIST.md.
- Reads complete: selector previously hard-coded `plan_final/status/` as the directory and only had two tests asserting initial state; once a status JSON is written, those tests began failing because the selector advanced past 00-001. The contract under 00-004 must remain stable when status files exist or do not exist, so the selector needs a deterministic test seam.
- Plan:
  - Add a `SelectStepOptions` interface with an optional `statusDirectory` override (default `plan_final/status`) so tests can point at a tempdir without touching the live status dir.
  - Replace the two existing tests with deterministic fixtures using `node:fs/promises mkdtemp` and `node:os tmpdir`, covering: no-statuses initial state (no-lane and lane-filtered), completed-status skipping, prerequisite-blocked lane returning null, and exhausted-lane reason text.
- Edits applied: plan_final/select-step.ts now exports `SelectStepOptions` and `selectNextStep(lane, options?)`; test/plan_final/select-step.test.ts now sets up per-test tempdir status directories and asserts five contract behaviors.
- `bun run format`: passed (formatted 2 files; no fixes applied).
- Focused `bun test test/plan_final/select-step.test.ts`: 5 pass / 0 fail (post-refactor).
- Pending verification: full `bun test` and `bun x tsc --noEmit --project tsconfig.json` after 00-002 lands its `findFinalProofViolations` export.
- 2026-05-14 verification: after the parallel 00-002 work landed its `findFinalProofViolations`, full `bun test` = 15327/15327 pass across 798 files in 25.77s; `bun x tsc --noEmit --project tsconfig.json` = clean.
- Commit and push: the 00-002 parallel agent bundled my 00-004 deltas (select-step.ts, select-step.test.ts, evidence/00-004.json, status/00-004.json, progress/governance/00-004.md) into 8a754b5 alongside their 00-002 changes; `git push origin main` advanced remote from a2d4e4f to 8a754b50dc33b53fab504b9c1a1cf8c65a58e711.
