# 00-003 lock-completion-status-schema

- 2026-05-14 start: selected step 00-003 via `bun run plan_final/select-step.ts`.
- Goal: validation must fail when a status file reports `status: "COMPLETED"` without recording the full verification command sequence, without a nonempty commit SHA, and without push evidence.
- Write lock: plan_final/validate-plan.ts, test/plan_final/validate-plan.test.ts.
- Read-only refs: plan_final/README.md, plan_final/STEP_TEMPLATE.md.
- Reads complete: existing status files written by 00-001/00-002/00-004 use the fields `stepId`, `lane`, `status`, `evidence`, `commitSha`. The 00-001 and 00-002 entries have `commitSha: null`; 00-004 has a real SHA. The evidence JSONs contain `commands`, `focusedTest`, `fullSuite`, `typecheck`. The current validator does not enforce a status schema at all.
- Plan:
  - Export a `validateStatusSchema(status, evidence?)` helper plus a `findStatusSchemaViolations` walker the focused test can probe directly.
  - Required fields when `status === 'COMPLETED'`: `stepId` (non-empty string), `lane` (non-empty string), `evidence` (path), `commitSha` (non-empty 40-char hex), and a sibling evidence file whose `commands` array contains all four verification commands listed in the corresponding step.
  - Push evidence is captured by requiring `commitSha` to be a 40-char hex SHA (the convention used after `git push`).
  - Validation surface in `validatePlan`: iterate completed status files in `plan_final/status/`, attach errors when the schema fails, and fold sibling evidence command mismatches into the existing error list.
  - Focused test additions: schema-violation cases (missing commitSha, placeholder commitSha, missing evidence commands, missing focused test command), plus a positive control using the 00-004 status/evidence as fixture data.
- Working on edits now.
- Edits applied: validate-plan.ts now exports `StatusSchemaViolationCategory`, `StatusSchemaViolation`, `CompletionStatusRecord`, `CompletionEvidenceRecord`, `findStatusSchemaViolations`, and `findEvidencePathMismatch`. The schema tolerates both `string[]` and `Array<{ command: string }>` evidence command shapes (since existing 00-002 evidence uses the structured form while 00-001/00-004 use plain strings).
- Test additions: 16 new schema cases covering each violation category, two positive controls (string vs. structured commands), a non-COMPLETED record short-circuit, an uppercase-SHA rejection, and the evidence path mismatch helper.
- `bun run format`: passed (4 files formatted, no fixes).
- Focused `bun test test/plan_final/validate-plan.test.ts`: 25 pass / 0 fail (9 prior final-proof tests + 16 new schema tests).
- Full `bun test`: 15370 pass / 0 fail across 801 files in 24.44s.
- `bun x tsc --noEmit --project tsconfig.json`: clean.
- Note: the live `validatePlan()` deliberately does NOT iterate `plan_final/status/` and run the schema yet, because the existing 00-001 status file was written before this step and records `commitSha: null`. Wiring runtime enforcement requires a backfill that is out of the 00-003 write lock; the schema is locked here as a pure helper that any later step can call.
