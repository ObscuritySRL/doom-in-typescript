# 00-002 reject-manifest-only-final-proof

- 2026-05-14 start: selected step 00-002 via bun run plan_final/select-step.ts.
- Goal: make plan_final validation fail when final gates rely on manifests, pending fixtures, or human attestation alone.
- Write lock: plan_final/validate-plan.ts, test/plan_final/validate-plan.test.ts.
- Read-only refs: plan_final/validate-plan.ts, test/oracles/fixtures/capture-final-side-by-side-replay.json, test/vanilla_parity/acceptance/gate-full-final-side-by-side-proof.json.
- Reads complete: validate-plan.ts already has FINAL_GATE_FORBIDDEN_TOKENS + validateAcceptanceTests but only catches the literal "accept <token>" pattern. The two reference fixtures show concrete patterns to reject: capture-final-side-by-side-replay.json embeds `comparisonStatus: "pending-live-capture"` and `implementationStatus: "pending-unimplemented-side-by-side-surface"`, and gate-full-final-side-by-side-proof.json has `human_attestation_required: true` (paired with a non-empty `oracle_evidence_required` array). The new validation must (a) reject manifest-only proof, (b) reject pending fixtures as the only evidence, and (c) reject human attestation alone (without live oracle evidence) — exposed as a pure helper so the focused test can probe each category without scanning real fixtures.
- Plan: export `findFinalProofViolations(text)` returning categorized issues, scan optional `plan_final/final-gates/` directory in validatePlan(), and add focused-test cases covering each violation category and a positive control.
- Edits applied: extended plan_final/validate-plan.ts with PENDING_FIXTURE_MARKERS, MANIFEST_ONLY_MARKERS, LIVE_CAPTURE_MARKERS, FinalProofViolationCategory, FinalProofViolation, exported findFinalProofViolations(text), added validateFinalGates(errors) scanning plan_final/final-gates/*.json, and wired it after validateAcceptanceTests() in validatePlan(). test/plan_final/validate-plan.test.ts now imports findFinalProofViolations and adds 5 cases covering pending-fixture, manifest-only, human-attestation-alone rejections, plus paired-attestation and live-capture acceptance controls.
- Verification 1: bun run format -> 5 files formatted, no fixes applied.
- Verification 2: bun test test/plan_final/validate-plan.test.ts -> 9 pass / 0 fail (4 prior + 5 new), 543 expect() calls in 84.00ms.
- Verification 3: bun test -> 15327 pass / 0 fail across 798 files in 25.47s.
- Verification 4: bun x tsc --noEmit --project tsconfig.json -> clean.
- Writing plan_final/evidence/00-002.json and plan_final/status/00-002.json before staging and commit.
- Commit: 8a754b50dc33b53fab504b9c1a1cf8c65a58e711 (feat(plan_final): reject manifest-only, pending, and attestation-alone final proof in validate-plan).
- Push: git push -> Everything up-to-date; remote HEAD matches local HEAD at 8a754b50dc33b53fab504b9c1a1cf8c65a58e711.
- Status JSON updated with commit SHA.
