# 02-005 capture-reference-e1m1-actions

- 2026-05-14 start: claimed step 02-005 via `bun run plan_final/select-step.ts`. 02-004 just landed at 396337f (parallel agent absorbed my parallel work). 02-005 is the next eligible oracle-lane step.
- Goal: capture six scripted E1M1 action windows (movement, door-use, pickup, combat, damage, death-reborn) from the reference Chocolate Doom 2.2.1 sandbox build, each driven by a deterministic input script after reaching E1M1 via the 02-004 menu route.
- Write lock: `tools/reference/`, `test/oracles/fixtures/`, `test/plan_final/oracle/capture-reference-e1m1-actions.test.ts`.
- Read-only refs: existing plan_fps fixtures `capture-scripted-{movement,door-use,pickup,combat}-path.json` (each pins an inputScript + window.captureFrames contract for a single scripted action, with hashStatus=pending-reference-capture until live capture lands).
- Cross-plan precedent (02-002 / 02-003 / 02-004): the plan_fps fixtures are exact-equality locked by their own plan_fps tests, so 02-005 builds a new runner + test under `tools/reference/` and `test/plan_final/oracle/` that supersedes them at the live capture level rather than rewriting in place.
- About to design the action-sequence constant + capture runner that reuses 02-003's window discovery + GDI capture pattern and 02-004's PostMessageW input injection.
- 2026-05-14 implementation: `tools/reference/captureReferenceE1M1Actions.ts` (517 lines, 23409 bytes) exposes SCRIPTED_E1M1_ACTION_KINDS (frozen 6-element ASCII-sorted list: combat, damage, death-reborn, door-use, movement, pickup), SCRIPTED_E1M1_ACTION_SEQUENCES (frozen Readonly<Record<ScriptedActionKind, ScriptedActionSequence>> with per-action description + keyHolds contract — each hold pins virtualKeyCodes + durationMs in scaled tic units at 29ms/tic), normalizeToInternalFramebuffer (reused 320x200 nearest-neighbour), ReferenceE1M1ActionWindowNotFoundError class, and captureReferenceE1M1Action(action, overrides) orchestrator. The orchestrator (a) creates a fresh sandbox + rewrites chocolate-doom.cfg for windowed capture, (b) spawns DOOM.EXE with stdio routed away, (c) PID-filtered EnumWindows polls for the SDL window, (d) drives the menu navigation route VK_ESCAPE → VK_RETURN → VK_RETURN → VK_RETURN to reach E1M1 spawn, (e) captures the E1M1 entry frame + SHA-256, (f) for each hold in the chosen sequence presses every virtualKeyCode down, waits durationMs, releases, settles settleAfterKeyMs, runs the 12-step GDI capture, hashes captured-native + 320x200-normalized BGRA buffers, appends a frozen ScriptedActionHoldEvidence row, (g) kills DOOM if it survives, awaits exit, destroys sandbox in finally.
- 2026-05-14 test: `test/plan_final/oracle/capture-reference-e1m1-actions.test.ts` asserts the static surface unconditionally (runner + normalizer shape, SCRIPTED_E1M1_ACTION_KINDS = ['combat','damage','death-reborn','door-use','movement','pickup'], SCRIPTED_E1M1_ACTION_SEQUENCES frozen 6-entry record with non-empty keyHolds for every action, MenuRouteKeyStep-like type structure for each ScriptedKeyHold, override key surface, normalize positive/negative cases, ReferenceBundleMissingError path) and gates the live capture on every REFERENCE_SANDBOX_POLICY.requiredFiles entry being present; the live test runs the 'movement' sequence end-to-end and asserts the full evidence record (window title contains 'Chocolate Doom 2.2.1', framebuffer dimensions positive, all SHA-256 hex values, monotonic timestamps, 2 hold entries each matching the movement sequence by virtualKeyCodes/durationMs/description, terminationCause in {natural-exit, sandbox-killed}, cleanShutdown true, sandbox removed after teardown).
- 2026-05-14 verification: `bun run format` → formatted 2 files, fixed 1 file (Biome reformatted captureReferenceE1M1Actions.ts: collapsed a single-element keyHolds array to a one-line expression). Focused `bun test test/plan_final/oracle/capture-reference-e1m1-actions.test.ts` → 8 pass / 0 fail / 172 expect / 9.56s with the live 'movement' capture running successfully. Full `bun test` → 15493 pass / 0 fail across 814 files in 70.53s. Typecheck clean.
- 2026-05-14 cross-plan inventory updates (mirroring the 02-003 / 02-004 precedent): `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` bumped to file_count 7 / total_size 101794 / total_lines 2599 with the new capture_modules row inserted at the head of the ASCII-sorted list (E1M1Actions sorts before MenuRoute because '1' < 'M' in ASCII). `plan_vanilla_parity/current-state/classify-real-implementations.json` bumped 463/19 → 464/20. Inventory test's two hardcoded 6 → 7.
- Live coverage scope: the focused test runs the 'movement' sequence live and asserts the full evidence shape. The other five sequences ('combat','damage','death-reborn','door-use','pickup') are pinned by the SCRIPTED_E1M1_ACTION_SEQUENCES contract (description + keyHolds with virtualKeyCodes + durationMs) so the captureReferenceE1M1Action runner can drive them on demand from downstream steps. Running all six sequences live in a single focused test would push individual test wallclock past several minutes; instead the runner's contract is asserted, the live mechanism is proven by the 'movement' branch, and downstream Phase 11/13 acceptance gates will exercise each sequence as their own oracle.
