# 02-004 capture-reference-menu-route

- 2026-05-14 start (resumed): selected step 02-004 via `bun run plan_final/select-step.ts`. 02-003 landed at 56bc720 with the SHA-record at d41ead6, 02-004 is the next eligible oracle-lane step. A prior session at 11:16 left a `?? plan_final/progress/oracle/02-004.md` start entry; that file has since been removed (no status, no evidence). Re-claiming the step.
- Goal: capture the deterministic input route from the attract-loop title screen → Main Menu → New Game → Episode 1 (Knee-Deep in the Dead) → Hurt Me Plenty → E1M1 spawn, with per-step framebuffer + menu-state evidence.
- Write lock: `tools/reference/`, `test/oracles/fixtures/capture-new-game-menu-path.json`, `test/plan_final/oracle/capture-reference-menu-route.test.ts`.
- Read-only refs: `test/oracles/fixtures/capture-new-game-menu-path.json` (plan_fps OR-FPS fixture pinning `scriptedInput: ['Escape', 'Enter']` and a 3-tic `expectedTrace`), `doom/` (DOOM.EXE + DOOM1.WAD + cfg).
- Cross-plan precedent (02-002 + 02-003): the plan_fps fixture is also locked by a plan_fps test with exact-equality assertions, so this step builds a new runner + test under `tools/reference/` and `test/plan_final/oracle/` that supersedes the static fixture at the live-capture level rather than rewriting the fixture in place.
- About to read existing infrastructure (captureReferenceTitleFrame.ts) for the spawn + window discovery + GDI capture pattern that 02-004 extends with keyboard input injection.
- 2026-05-14 reads: captureReferenceTitleFrame.ts already exposes the canonical PID-filtered EnumWindows + GDI BitBlt pipeline. The 02-004 runner extends it with a third user32 dlopen for PostMessageW + SetForegroundWindow (input injection) and a 4-step deterministic key sequence. Per the 02-002 precedent, the plan_fps fixture `test/oracles/fixtures/capture-new-game-menu-path.json` stays untouched (it is exact-equality locked by `test/oracles/capture-new-game-menu-path.test.ts` and only captures the first 2 key steps with the DOOMD.EXE DOS binary); 02-004 supersedes it at the live capture level under tools/reference/ and test/plan_final/oracle/.
- 2026-05-14 implementation: `tools/reference/captureReferenceMenuRoute.ts` (505 lines, 19530 bytes) exports MENU_ROUTE_KEY_STEPS (frozen 4-element route: VK_ESCAPE → main-menu, VK_RETURN → episode-menu, VK_RETURN → skill-menu, VK_RETURN → gameplay-e1m1), normalizeToInternalFramebuffer (reused from 02-003's nearest-neighbour resampler), ReferenceMenuRouteWindowNotFoundError class, and captureReferenceMenuRoute(overrides) orchestrator that spawns DOOM.EXE in the sandbox, settles for the title attract, captures the title frame, then for each step does PostMessageW WM_KEYDOWN+WM_KEYUP to the discovered window handle (with SetForegroundWindow before each key burst so SDL 1.2's message pump picks them up), settles settleAfterKeyMs, runs the 12-step GDI capture, and records a frozen MenuRouteStepEvidence row. Returns a frozen ReferenceMenuRouteEvidence record with title-frame and 4-element steps array.
- 2026-05-14 test: `test/plan_final/oracle/capture-reference-menu-route.test.ts` (180 lines) asserts the static export surface unconditionally (runner shape, MENU_ROUTE_KEY_STEPS frozen 4-step contract, MenuRouteKeyStep type, override key set, normalize positive/negative cases, ReferenceBundleMissingError path with sandbox cleanup), then gates the live capture on every REFERENCE_SANDBOX_POLICY.requiredFiles entry being present and asserts the full live evidence record (window title contains 'Chocolate Doom 2.2.1', framebuffer dimensions positive, all SHA-256 hex values, monotonic timestamps, 4 step entries each matching MENU_ROUTE_KEY_STEPS by virtualKeyCode/Name/expectedMenuState, terminationCause in {natural-exit, sandbox-killed}, cleanShutdown true, sandbox removed after teardown).
- 2026-05-14 verification: `bun run format` → 2 files, 0 fixes. Focused `bun test test/plan_final/oracle/capture-reference-menu-route.test.ts` → 9 pass / 0 fail / 111 expect / 11.39s (the live menu route capture pressed Escape → Enter → Enter → Enter into the real Chocolate Doom 2.2.1 window and successfully captured 5 frames). Full `bun test` → 15485 pass / 0 fail across 813 files in 52.99s. `bun x tsc --noEmit --project tsconfig.json` clean.
- 2026-05-14 cross-plan inventory updates (mirroring the 02-003 precedent): `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` bumped to file_count 6 / total_size 78385 / total_lines 2082 with the new capture_modules row inserted at the head of the ASCII-sorted list. `plan_vanilla_parity/current-state/classify-real-implementations.json` bumped 462/18 → 463/19. Inventory test's two hardcoded 5 → 6.
