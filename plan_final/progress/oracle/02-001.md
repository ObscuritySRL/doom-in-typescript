# 02-001 create-reference-sandbox-runner

- 2026-05-14 start: selected step 02-001 from the oracle lane because its prereq 01-010 is COMPLETED in plan_final/status/ and no progress log exists for oracle yet. Write lock (tools/reference/, test/plan_final/oracle/reference-sandbox-runner.test.ts) does not overlap any in-flight step.
- Goal: create a sandbox runner that copies local reference files without mutating doom/ or iwad/.
- Write lock: tools/reference/, test/plan_final/oracle/reference-sandbox-runner.test.ts.
- Read-only refs: doom/, iwad/, src/oracles/referenceSandbox.ts.
- Read src/oracles/referenceSandbox.ts: fully-defined policy module exporting SANDBOX_REQUIRED_FILES (DOOM.EXE, DOOM1.WAD, chocolate-doom.cfg, default.cfg with frozen hashes/sizes/roles), SANDBOX_EXCLUDED_FILES (DOOMD.EXE, DOOMDUPX.EXE, DOOMWUPX.exe, smash.py), and REFERENCE_SANDBOX_POLICY (sourcePath = REFERENCE_BUNDLE_PATH, sandboxParent = .sandboxes, sandboxPrefix = sandbox-, verifyHashesAfterCopy = true, cleanupAfterRun = true).
- Read src/reference/policy.ts: REFERENCE_BUNDLE_PATH = absolute resolve('doom') and CODEX_WORKSPACE_PATH = project root.
- Read doom/: contains DOOM.EXE, DOOM1.WAD, DOOMD.EXE, DOOMDUPX.EXE, DOOMWUPX.exe, README.md, chocolate-doom.cfg, default.cfg, smash.py, stderr.txt — i.e., the local reference bundle.
- Read tools/reference/: contains isolationProbe.ts, windowCaptureProbe.ts (existing probes), but no sandbox runner.
- Plan: write tools/reference/createReferenceSandbox.ts that uses REFERENCE_SANDBOX_POLICY + SANDBOX_REQUIRED_FILES to (a) create a fresh sandbox directory under {workspaceRoot}/{sandboxParent}/{sandboxPrefix}{uuid}/, (b) copy each required file from the source bundle into that directory using Bun.write + Bun.file with explicit per-file size+hash verification, (c) verify the source files in doom/ are byte-identical before AND after the copy (reading the source bytes once and reusing the buffer), (d) return a SandboxDescriptor with the absolute sandbox path, copied file paths, and per-file SHA-256 hashes. Write test/plan_final/oracle/reference-sandbox-runner.test.ts that spawns the runner against a temporary parent directory, verifies all 4 required files are present and hash-matched, verifies the doom/ source files were not mutated, verifies excluded files are absent, and that the sandbox directory is removed when cleanup is called.
- Implementation note: skip the runner if local reference files are missing — gate the test on REFERENCE_BUNDLE presence to keep the suite runnable on CI where doom/ may be absent.
- 2026-05-14 resume: picked up step 02-001 in a follow-on session after the prior agent's working-tree state (tools/reference/createReferenceSandbox.ts, test/plan_final/oracle/reference-sandbox-runner.test.ts, plan_final/progress/oracle/02-001.md) remained uncommitted. Verified the prior agent's runner implementation and companion test against the four-command verification sequence and finalised 02-001 with evidence + status + a drift-fix commit for the matching plan_vanilla_parity tools-reference inventory.
- Inspected the prior agent's `tools/reference/createReferenceSandbox.ts` (112 lines, 4689 bytes, sha256 65437ed3...): exports `createReferenceSandbox(overrides)` returning a `ReferenceSandboxDescriptor` and `destroyReferenceSandbox(sandboxAbsolutePath)`; copies every `REFERENCE_SANDBOX_POLICY.requiredFiles` entry from the read-only doom/ bundle into a freshly created sandbox under `${workspaceRoot}/${sandboxParent}/${sandboxPrefix}${sandboxId}`, verifies each source SHA-256 + size against `expectedHash`/`expectedSize` and re-hashes the destination after copy, and exposes a recursive cleanup helper.
- Inspected the prior agent's `test/plan_final/oracle/reference-sandbox-runner.test.ts`: exercises createReferenceSandbox + destroyReferenceSandbox against a tmpdir-rooted parent, gated on `REFERENCE_SANDBOX_POLICY.sourcePath` availability so the suite stays runnable when doom/ is absent.
- `bun run format`: formatted 4 files; no fixes applied.
- `bun test test/plan_final/oracle/reference-sandbox-runner.test.ts`: 5 pass / 0 fail.
- `bun test` initially FAILED on `test/vanilla_parity/current-state/inventory-tools-reference-capture-surface.test.ts` because the plan_vanilla_parity 01-018 inventory snapshot for `tools/reference/` still recorded only 2 entries (isolationProbe.ts, windowCaptureProbe.ts) but the new createReferenceSandbox.ts brings the on-disk count to 3, AND the test file's hardcoded `toBe(2)` pin had already been updated locally to `toBe(3)` by the prior agent.
- Fix: updated `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` to add the createReferenceSandbox.ts capture_module entry (group `reference_sandbox_capture`, role description, size 4689, line count 112, sha256 65437ed3..., 3 exported interfaces, 2 exported functions, external imports including node:crypto + node:fs/promises + node:path + src/oracles/referenceSandbox.ts, empty companion_test_relative_path since the runner is exercised through its own test/plan_final/oracle/ file), bumped tools_reference_file_count 2→3, replaced tools_reference_filenames_sorted with the three-entry ASCII-sorted list, and bumped total_tools_size_bytes 34546→39235 and total_tools_line_count 939→1051 to reflect the new file. No `weakening` of test logic — only the JSON data was extended to match on-disk truth.
- `bun test` (rerun): 15464 pass / 0 fail across 810 files.
- `bun x tsc --noEmit --project tsconfig.json`: exit 0 (clean).
- Wrote `plan_final/evidence/02-001.json` and `plan_final/status/02-001.json`.
