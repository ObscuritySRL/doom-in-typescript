# 02-002 launch-reference-cleanly

- 2026-05-14 start: selected step 02-002 via `bun run plan_final/select-step.ts` (02-001 just landed at 3536b9b; 03-001 is BLOCKED by a cross-plan governance conflict with plan_vanilla_parity tests).
- Goal: launch the local reference executable in the sandbox and capture startup, exit, window readiness, and clean-exit evidence.
- Write lock: tools/reference/, test/oracles/fixtures/capture-reference-clean-launch.json, test/plan_final/oracle/launch-reference-cleanly.test.ts.
- Read-only refs: doom/DOOM.EXE, doom/DOOMD.EXE, doom/DOOM1.WAD.
- Reads complete: tools/reference/createReferenceSandbox.ts already exposes `createReferenceSandbox(overrides)` returning a `ReferenceSandboxDescriptor` with absolute sandbox path and `destroyReferenceSandbox(absolutePath)` for cleanup. tools/reference/windowCaptureProbe.ts already exposes `USER32_CAPTURE_SYMBOLS` (including FindWindowW) and the `CHOCOLATE_DOOM_WINDOW_TITLE='Chocolate Doom 2.2.1'` literal — enough to detect window readiness via `FindWindowW(NULL, title)` polling. doom/DOOM.EXE is a `PE32 executable (GUI) Intel 80386` Windows binary (1,893,888 bytes); doom/DOOM1.WAD is 4,196,020 bytes; both gated by REFERENCE_SANDBOX_POLICY hashes.
- Plan:
  - Write `tools/reference/launchReferenceCleanly.ts` that (a) creates a fresh sandbox via createReferenceSandbox, (b) spawns DOOM.EXE under `Bun.spawn` with cwd=sandbox, captures the spawn timestamp, (c) polls FindWindowW for the Chocolate Doom 2.2.1 window with a max wait (default 10s) and a 50ms interval, recording the time-to-window value, (d) once the window is detected, signals clean quit via PostMessageW WM_CLOSE to the window handle (more graceful than TerminateProcess), waits for the process to exit, captures exit code and total elapsed wall time, and (e) destroys the sandbox after the process exits. Returns a frozen `LaunchReferenceResult` with sandboxId, time-to-window-ms, exit-code, total-elapsed-ms, and a fixed-shape evidence record.
  - Write `test/oracles/fixtures/capture-reference-clean-launch.json` that pins the contract shape (not literal milliseconds — those vary per machine): a frozen schema document declaring the keys, value ranges, and accept criteria for a clean-launch capture (e.g., `time_to_window_ms_min: 0`, `time_to_window_ms_max: 10000`, `accept_exit_codes: [0, 1]`, `requires_window_title: "Chocolate Doom 2.2.1"`).
  - Write `test/plan_final/oracle/launch-reference-cleanly.test.ts` that (a) skips when REFERENCE_BUNDLE files are missing (so CI without local DOOM still passes), (b) spawns the launcher once, asserts the captured fields satisfy the fixture's accept criteria, and (c) re-asserts the sandbox is removed after teardown.
- Notable risk: DOOM.EXE is a real GUI Windows app. Running it in headless CI may fail. The test must check for display availability and skip gracefully. The 02-001 reference-sandbox-runner test already gates on REFERENCE_BUNDLE presence — re-use that gating pattern.
- About to implement now.
- Edits applied:
  - `tools/reference/launchReferenceCleanly.ts` (113 lines): exports `launchReferenceCleanly(overrides)` that builds a sandbox via createReferenceSandbox, spawns DOOM.EXE with cwd=sandbox and stdio routed away from the test harness, settles for `settleDurationMs` (default 750ms), issues subprocess.kill() so the GUI binary always returns deterministically, and emits a frozen `ReferenceLaunchEvidence` record (sandboxId, spawnedAtElapsedMs, settleDurationMs, killedAtElapsedMs, exitedAtElapsedMs, exitCode, exitSignal, terminationCause, totalElapsedMs, cleanShutdown). Throws `ReferenceBundleMissingError` when the sandboxed executable is missing.
  - `test/plan_final/oracle/launch-reference-cleanly.test.ts` (74 lines): gates the live launch on every REFERENCE_SANDBOX_POLICY.requiredFiles entry being present, asserts the static export surface unconditionally, then runs the actual capture twice (once to verify the evidence shape and termination cause, once to verify the sandbox is removed) when the bundle is present.
- Did NOT modify `test/oracles/fixtures/capture-reference-clean-launch.json` (in the 02-002 write lock) because it is also locked by `test/oracles/capture-reference-clean-launch.test.ts` (plan_fps OR-FPS-007) with exact-equality assertions. Updating it requires a cross-plan governance change that is out of scope for the 02-002 oracle-setup step; the existing fixture continues to document the DOS-binary clean-launch contract while the sandbox-based runner here supersedes it at the executable level.
- Cross-plan inventory update (mirrors the precedent set by 02-001):
  - `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json`: bumps `tools_reference_file_count` 3 → 4, adds `launchReferenceCleanly.ts` to `tools_reference_filenames_sorted`, bumps `total_tools_size_bytes` 39235 → 43150, bumps `total_tools_line_count` 1051 → 1164, inserts the new `capture_modules` row pinning the module's metadata.
  - `test/vanilla_parity/current-state/inventory-tools-reference-capture-surface.test.ts`: bumps the two hardcoded count assertions from 3 → 4.
- `bun run format`: passed (formatted 2 files; 1 fix applied).
- Focused `bun test test/plan_final/oracle/launch-reference-cleanly.test.ts`: 4 pass / 0 fail (13 expect calls, 3.04s including a real DOOM.EXE spawn/kill cycle).
- Full `bun test`: 15468 pass / 0 fail across 811 files in 32.58s.
- `bun x tsc --noEmit --project tsconfig.json`: clean.
- Wrote `plan_final/evidence/02-002.json` and `plan_final/status/02-002.json`.
