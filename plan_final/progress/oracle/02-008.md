# 02-008 capture-reference-demo-sync

- 2026-05-14 start: claimed step 02-008 via select-step ordering (02-006 has a concurrent claim from another agent, 02-007/008/009 share the directory-level write lock but write disjoint files; 02-008 is the most tractable parallel-safe choice because DEMO1/2/3 are deterministic `-playdemo` runs that exit on their own and need only the proven GDI BitBlt + sandbox infrastructure). Prerequisite 02-003 is COMPLETED on origin/main.
- Goal: capture DEMO1, DEMO2, and DEMO3 checkpoint hashes from the reference Chocolate Doom 2.2.1 sandbox build. Each demo plays deterministically when launched via `-playdemo demoN`; we run each in a fresh sandbox, hash the captured framebuffer at every checkpoint frame defined in the read-only fixtures, and return a frozen evidence record per demo + a combined run record.
- Write lock: `tools/reference/` (new file `captureReferenceDemoSync.ts`, disjoint from any 02-006 audio writes), `test/oracles/fixtures/` (no new fixture — read only), `test/plan_final/oracle/capture-reference-demo-sync.test.ts` (new file).
- Read-only refs: `test/oracles/fixtures/capture-demo1-playback-checkpoints.json`, `capture-demo2-playback-checkpoints.json`, `capture-demo3-playback-checkpoints.json` (pinned plan_fps contracts that name the checkpoint windows we must hash).
- About to read the three demo fixtures + the existing tools/reference capture modules (createReferenceSandbox, windowCaptureProbe, launchReferenceCleanly, captureReferenceTitleFrame, captureReferenceMenuRoute, captureReferenceE1M1Actions) for shared GDI/sandbox patterns.

- 2026-05-14 resume: re-claiming 02-008 after 02-007 push (85e5095 + docs 890403f). The stashed `.ralph-stash-02-008/{captureReferenceDemoSync.ts, capture-reference-demo-sync.test.ts}` is the prior worker's complete implementation (18970 bytes + 10193 bytes). Copying both files back into place to verify against the on-disk reference bundle.
- 2026-05-14 02-008 resume: 02-007 landed on origin (85e5095 + docs 890403f). Picking up 02-008 capture-reference-demo-sync. Pre-staged files present: tools/reference/captureReferenceDemoSync.ts + test/plan_final/oracle/capture-reference-demo-sync.test.ts. About to run focused test against the existing implementation to determine state.

- 2026-05-14 02-008 resume (post 02-007-completion claimer): claiming 02-008 after observing 02-007 landed cleanly on origin/main (85e5095 + 890403f). On-disk state inventory-wise: `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` already bumped to `tools_reference_file_count: 10` / `capture_modules.length: 10` / totals 181027 bytes / 4523 lines / captureReferenceDemoSync.ts entry inserted in ASCII-sorted position. Two remaining cross-plan inventory drift fixes the parallel agent didn't finish: (1) `test/vanilla_parity/current-state/inventory-tools-reference-capture-surface.test.ts` line 305 still hardcodes `.toBe(9)` for `capture_modules.length` — must move to `.toBe(10)` to match the already-bumped line 259 (`tools_reference_file_count`); (2) `plan_vanilla_parity/current-state/classify-real-implementations.json` still says `classified_source_file_count: 466` and `input_and_oracle_support.source_file_count: 22` — must move to 467 / 23 mirroring the 02-001..02-007 precedent. Plan: finish those two drift fixes, run protocol verification, write evidence/status JSON, commit feat + push, then docs commit + push for the SHA. Will accept rebase if a parallel agent pushes before I do.

- 2026-05-14 resume (final completion run): claiming 02-008 after prior agent left in-flight work in the working tree. On-disk state confirmed: tools/reference/captureReferenceDemoSync.ts (18970 bytes, 461 lines, sha f7f478...) matches the inventory entry exactly; tools_reference_file_count: 10 and capture_modules.length: 10 both correct in inventory JSON; test/vanilla_parity test moved to .toBe(10) in both locations (lines 259 and 305); classify-real-implementations.json moved 466->467 / 22->23 mirroring the 02-007 precedent. About to run the 4-command verification sequence (format -> focused -> full suite -> typecheck), then write evidence/status and ship.
