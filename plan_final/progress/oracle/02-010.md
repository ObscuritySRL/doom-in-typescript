- 2026-05-14 02-010 start: claimed step after 02-009 push (9c7d077 + docs c833568). Prerequisites all complete on origin/main. Plan: 28 fixtures under test/oracles/fixtures/ have "pending" markers; build pending-oracle-fixtures-blocklist.json enumerating all 28 with reasons, and a focused test that walks every fixture, detects pending markers, and asserts they are present in the blocklist with blocks_acceptance=true.
- 2026-05-14 02-010 resume: prior agent left two untracked artifacts on disk: `test/oracles/fixtures/pending-oracle-fixtures-blocklist.json` (28 entries enumerating every pending fixture) and `test/plan_final/oracle/replace-pending-oracle-fixtures.test.ts` (5 cases pinning the blocklist contract). Ran the focused test as-is: 3/5 pass / 2 fail — the failing cases use regex `/"pending[a-z-]*"/i` to detect pending fixtures on disk, which matches the literal `"pendingLiveHashes"` key in 27 fixtures but does NOT match `capture-demo2-playback-checkpoints.json` (uses suffix value `"demo2-live-hashes-pending"` and null hashes). Per brief, the canonical detection rule is "any field whose value starts with `pending-`", paired with a sibling/ancestor `status` field of literal `"blocked-pending-future-step"` referencing a real plan_final step ID.
- 2026-05-14 02-010 plan: rewrite the focused test to enforce the brief's exact rule (recursive tree walk; any value starting with `pending-` requires an ancestor `status: "blocked-pending-future-step"` plus an ancestor `followUpStepId` field whose value matches `/^\d{2}-\d{3}$/` and points to a real `plan_final/steps/<id>-*.md` file). Annotate every fixture that contains a `pending-*` value with a top-level `blockedPendingFutureStep` object pointing at `02-011-gate-oracle-foundation` (the explicit gate). Keep the blocklist contract tests for the 28-entry META enumeration. Fixtures with only `null` placeholders or `pendingX` keys (no `pending-*` value) remain "live-evidence-bound" per the brief and are NOT failed by the test; the blocklist documents them as awaiting follow-up.
- 2026-05-14 02-010 pivot: the per-fixture annotation approach is blocked by lock tests outside this step's write lock. Adding a top-level `blockedPendingFutureStep` key to any of the 17 `pending-*`-bearing fixtures breaks the lock tests under `test/oracles/capture-*.test.ts` that use `expect(fixture).toEqual(expectedFixture)` against frozen literals. Adding a NEW fixture file (`pending-oracle-fixtures-blocklist.json`) under `test/oracles/fixtures/` breaks `test/vanilla_parity/current-state/inventory-oracle-fixtures-with-pending-status.test.ts` because (a) the inventory's `fixtures_directory.fixture_count`/`fixtures[]` array would drift, and (b) the test's hardcoded `ALLOWED_PENDING_TOP_LEVEL_KEYS_SORTED` and `ALLOWED_PENDING_SIGNALS_SORTED` enum reject the new file's classification. Both downstream test files are outside this step's write lock. Final approach: move the blocklist registry INTO the focused test file as an inline TypeScript constant. The test file itself becomes the structured "blocked-pending-future-step" authority — it pins (a) the set of fixtures with `pending-*` values, (b) their followUpStepId, (c) the rationale. The test enforces the bidirectional invariant: every on-disk fixture with a `pending-*` value MUST be enumerated in the inline `BLOCKED_FIXTURES` constant with `status: "blocked-pending-future-step"` and a `followUpStepId` referencing a real plan_final step. No new files under `test/oracles/fixtures/`, no fixture content changes — only the new focused test file is added. Deleted the pre-existing untracked `test/oracles/fixtures/pending-oracle-fixtures-blocklist.json` to avoid the inventory drift.
- 2026-05-14 02-010 verification: focused `bun test test/plan_final/oracle/replace-pending-oracle-fixtures.test.ts` passes 7/7 in 56 ms. Full `bun test` passes 15538/0 across 819 files in 172.17 s. `bun x tsc --noEmit --project tsconfig.json` exits 0 with no output. The 17 fixtures with `pending-*` values are now enumerated in the inline `BLOCKED_FIXTURES_AUTHORITATIVE_REGISTRY` constant inside the focused test, each with `status: "blocked-pending-future-step"` + `followUpStepId: "02-011"` (the gate-oracle-foundation step that depends on 02-010) + a rationale naming the live capture surface required to retire it. The 14 fixtures without `pending-*` values (capture-demo1, capture-demo2, capture-e1m1-start, capture-episode-menu-path, capture-implementation-clean-launch-expectations, capture-menu-open-close-behavior, capture-new-game-menu-path, capture-options-menu-path, capture-reference-clean-launch, capture-scripted-combat-path, capture-scripted-intermission-path, capture-scripted-pickup-path, capture-skill-menu-path, capture-startup-sequence) are classified as live-evidence-bound by the brief's exact rule (no `pending-*` value placeholders) and are not failed by the test; they remain documented as awaiting follow-up via the cross-plan inventory `plan_vanilla_parity/current-state/inventory-oracle-fixtures-with-pending-status.json` which classifies them by signal type.
- 2026-05-14 02-010 verification passes: focused 7/0, full 15538/0, tsc clean. Embedded BLOCKED_FIXTURES_AUTHORITATIVE_REGISTRY inside the test file (parallel agent design) so 17 pending fixtures are enumerated without modifying any fixture file (keeps inventory-oracle-fixtures-with-pending-status.test.ts contract stable). Staging + committing.
