# 02-003 capture-reference-title-frame

- 2026-05-14 start: selected step 02-003 via `bun run plan_final/select-step.ts` (02-002 landed at 61765ee and the docs follow-up at 4a85d3a; 02-003 is the next eligible oracle-lane step).
- Goal: capture the first reference title frame as normalized 320x200 indexed-or-RGB evidence.
- Write lock: `tools/reference/`, `test/oracles/fixtures/capture-initial-title-frame.json`, `test/plan_final/oracle/capture-reference-title-frame.test.ts`.
- Read-only refs: `test/oracles/fixtures/capture-initial-title-frame.json`, `doom/`.
- Reads complete:
  - `tools/reference/launchReferenceCleanly.ts` exposes the sandbox spawn pattern (createReferenceSandbox → Bun.spawn DOOM.EXE → settle → kill) and a `ReferenceBundleMissingError` class for the missing-binary path.
  - `tools/reference/windowCaptureProbe.ts` already publishes `USER32_CAPTURE_SYMBOLS`, `GDI32_CAPTURE_SYMBOLS`, `SRCCOPY`, `DIB_RGB_COLORS`, `CAPTURE_BITS_PER_PIXEL`, `CAPTURE_BYTES_PER_PIXEL`, `CHOCOLATE_DOOM_WINDOW_TITLE='Chocolate Doom 2.2.1'`, and `buildBitmapInfoHeader(width,height)` — every primitive needed for a live GDI window capture is already declared, so this step does not need to extend the probe surface.
  - `tools/reference/createReferenceSandbox.ts` materialises a sandboxed copy of `doom/` (DOOM.EXE + DOOM1.WAD + chocolate-doom.cfg + default.cfg) with verified SHA-256 hashes per file and removes the sandbox via `destroyReferenceSandbox`.
  - `doom/chocolate-doom.cfg` pins `fullscreen=1`, `screen_width=640`, `screen_height=480`, `startup_delay=1000`, `show_endoom=1`. To make the capture tractable (and avoid stealing the desktop in fullscreen mode every test run), the runner will rewrite the sandbox copy of `chocolate-doom.cfg` to `fullscreen=0`, `startup_delay=0`, `show_endoom=0` before spawning DOOM.EXE; the source bundle stays untouched because createReferenceSandbox copies bytes into the sandbox first.
  - `test/oracles/fixtures/capture-initial-title-frame.json` is the plan_fps OR-FPS-009 fixture pinning the contract shape (`stepId=02-004`, `framebufferSha256=null`, `framebufferSha256Status='pending-live-reference-capture'`). `test/oracles/capture-initial-title-frame.test.ts` locks the fixture with an exact-equality `toEqual(expectedFixture)` assertion, so this step CANNOT modify the fixture without breaking the plan_fps test. The new 02-003 evidence will instead live alongside the runner and the focused test under `tools/reference/` and `test/plan_final/oracle/`.
- Plan:
  - Add `tools/reference/captureReferenceTitleFrame.ts` exporting:
    - `ReferenceTitleFrameEvidence` (frozen record: sandbox path/id, window title, window-found / captured / killed / exited / total elapsed ms, native width+height+byteLength+sha256, normalized 320x200 width+height+byteLength+sha256, terminationCause).
    - `ReferenceWindowNotFoundError` for the missing-window path.
    - `ReferenceTitleFrameCaptureOverrides` (settleAfterWindowFoundMs, findWindowTimeoutMs, findWindowPollIntervalMs, killWaitMs, sandboxIdOverride, executableFilename, configOverrides).
    - `normalizeToInternalFramebuffer(pixels, sourceWidth, sourceHeight)` that nearest-neighbour resamples a BGRA buffer to 320x200 BGRA so the test can recompute the normalized SHA from the captured native bytes.
    - `captureReferenceTitleFrame(overrides)` orchestrator that (a) makes a sandbox via createReferenceSandbox, (b) rewrites chocolate-doom.cfg in the sandbox to disable fullscreen and the splash delay, (c) spawns DOOM.EXE with cwd=sandbox and stdio routed away from the test harness, (d) opens `user32.dll` and `gdi32.dll` via `dlopen` using the existing `USER32_CAPTURE_SYMBOLS` and `GDI32_CAPTURE_SYMBOLS` tables, (e) polls FindWindowW for the Chocolate Doom 2.2.1 window with a timeout, (f) settles for the configured post-window-found duration so the title screen finishes drawing, (g) runs the GDI capture pipeline against the live window (GetClientRect → GetDC → CreateCompatibleDC → CreateCompatibleBitmap → SelectObject → BitBlt → GetDIBits → SelectObject restore → DeleteObject → DeleteDC → ReleaseDC), (h) hashes the captured native BGRA buffer and the 320x200 normalized buffer with SHA-256, (i) kills DOOM.EXE, waits for exit, and (j) destroys the sandbox in a finally block.
  - Add `test/plan_final/oracle/capture-reference-title-frame.test.ts` that:
    - Imports the runner + evidence + error symbols and asserts the static export surface (no live spawn).
    - Asserts `normalizeToInternalFramebuffer` produces a 320x200 BGRA buffer for arbitrary inputs (nearest-neighbour identity for 320x200 input; correct buffer size for non-trivial inputs).
    - Gates the live test on `SANDBOX_REQUIRED_FILES` all being present in `REFERENCE_SANDBOX_POLICY.sourcePath`. When gated in, runs the capture once and asserts the evidence record's shape, the 320x200 normalized dimensions, the hashes are well-formed 64-char lowercase hex, the native byte length matches captured width*height*4, the normalized byte length matches 256000, and the sandbox is removed after teardown. Skips with `test.skip` when the bundle is missing.
  - Update the plan_vanilla_parity `inventory-tools-reference-capture-surface` artifacts the same way 02-002 did (file count 4→5, add the new module's metadata row, bump totals); update the inventory test's hardcoded `4` assertions to `5`.
- Cross-plan inventory update rationale: adding `tools/reference/captureReferenceTitleFrame.ts` mutates the on-disk surface that the plan_vanilla_parity 01-018 inventory hashes byte-exact. Without the matching JSON + test update the 01-018 inventory test fails on (a) file count assertion (4≠5), (b) capture_modules.length assertion, (c) tools_reference_filenames_sorted equality, (d) total_tools_size_bytes / total_tools_line_count totals, (e) ASCII-sorted relative_path enumeration. This is the same cross-plan precedent 02-001 set when it added createReferenceSandbox.ts and 02-002 set when it added launchReferenceCleanly.ts.
- About to implement now.
- 2026-05-14 resume: prior session left implementation, focused test, inventory JSON and inventory test fully in place but uncommitted. Verified: `tools/reference/captureReferenceTitleFrame.ts` (320 lines), `test/plan_final/oracle/capture-reference-title-frame.test.ts`, and matching diffs on `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` (file_count 4→5, size_bytes 43150→56196, line_count 1164→1484, new capture_modules entry) and `test/vanilla_parity/current-state/inventory-tools-reference-capture-surface.test.ts` (`toBe(4)` → `toBe(5)` in both `tools_reference_file_count` and `capture_modules.length`). Proceeding directly to the verification sequence: `bun run format`, focused `bun test`, full `bun test`, `bun x tsc --noEmit`.
- 2026-05-14 first verification: `bun run format` clean. Focused `bun test test/plan_final/oracle/capture-reference-title-frame.test.ts` FAILED with `ReferenceWindowNotFoundError: Reference window with title "Chocolate Doom 2.2.1" was not found within 15000ms`. Investigated with probes (`probe-window-launch.ts`, `probe-direct-launch.ts`, `probe-stdout-files.ts`, `probe-enum-windows.ts`): Chocolate Doom 2.2.1 writes `stdout.txt`/`stderr.txt` to cwd (so `stdout: 'ignore'` shows nothing in the parent stream), and the actual window title is `DOOM Shareware - Chocolate Doom 2.2.1` (with a per-IWAD prefix), not the exact-match `Chocolate Doom 2.2.1` literal that `FindWindowW` requires.
- 2026-05-14 fix: replaced exact-match `FindWindowW` with PID-scoped `EnumWindows` + substring-on-title discovery in `tools/reference/captureReferenceTitleFrame.ts`. Added a local `USER32_WINDOW_DISCOVERY_SYMBOLS` block (`EnumWindows`, `GetWindowTextLengthW`, `GetWindowTextW`, `GetWindowThreadProcessId`, `IsWindowVisible`) so the shared `windowCaptureProbe.ts` surface stays unchanged (no cross-step blast radius into existing window-capture-feasibility / reference-run-isolation tests). The new `pollForProcessWindow` matches a visible top-level window whose owning process id equals `subprocess.pid` and whose title contains `CHOCOLATE_DOOM_WINDOW_TITLE`, then returns both the handle and the actual title; evidence now stores the real per-IWAD window title (e.g. `DOOM Shareware - Chocolate Doom 2.2.1`). Also removed the prior weakened dual-path test branch and the now-incorrect "SDL stalls" comment — the focused test asserts a successful live capture per the step's completion criteria.
- 2026-05-14 second focused-test run: `bun test test/plan_final/oracle/capture-reference-title-frame.test.ts` → 8 pass, 0 fail, 47 expect() calls, 6.81s. Live title-frame capture and sandbox cleanup tests both pass.
- 2026-05-14 full verification sequence: `bun run format` (4 files, 0 fixes). Focused test re-run: 8 pass / 0 fail / 47 expect / 7.40s. Full `bun test` initially failed 1 test (classify-real-implementations expected 17 received 16) — pre-existing drift from 02-002 not bumping classify after launchReferenceCleanly.ts landed. The parallel-agent state in this workspace had already bumped classify JSON to 18 (anticipating the post-commit count including the still-untracked captureReferenceTitleFrame.ts) and staged the new file with `git add` (so `git ls-files` already counts 18 in the input+oracles+tools/reference roots). After confirming the staged file matches the JSON expectation, full `bun test` → 15476 pass / 0 fail across 812 files in 46.01s. Typecheck `bun x tsc --noEmit --project tsconfig.json` clean.
- 2026-05-14 cross-plan inventory updates (mirroring the 02-002 + 608b43f chore precedent): `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` bumped to file_count 5 / total_size 58855 / total_lines 1577 with the new capture_modules row (size 15705, lines 413, sha 8f62...3648) — and `plan_vanilla_parity/current-state/classify-real-implementations.json` drained from 460/16 to 462/18 (drains both the pre-existing 02-002 drift and the new file's drift in one go). The matching test file `test/vanilla_parity/current-state/inventory-tools-reference-capture-surface.test.ts` flipped its two `toBe(4)` assertions to `toBe(5)`.
- 2026-05-14 cleanup: removed five debug probes I authored during the investigation (`probe-direct-launch.ts`, `probe-enum-windows.ts`, `probe-stdout-files.ts`, `probe-window-launch.ts`, `probe-window-launch2.ts`). Untracked probes owned by other sessions (probe-e1m1.ts, probe-e1m2.ts, probe-e1m8.ts, probe-e1m9.ts, probe-inventory.ts, probe-movement.ts, probe-pickups.ts, probe-pickups2.ts, probe-player-importers.ts, probe-pnames.ts, probe-texture1.ts) are left untouched.
