# 02-007 capture-reference-save-load

- 2026-05-14 start: claimed step 02-007 via `bun run plan_final/select-step.ts` after 02-006 push (a2c908d + docs 3f46093 confirmed on origin/main). Prereq 02-004 COMPLETED.
- Goal: capture live save/load menu navigation and byte-level save file evidence from the sandboxed Chocolate Doom 2.2.1 reference build. Plan: spawn DOOM.EXE with `-savedir <sandbox>` so the .DSG savegame file lands inside the sandbox, navigate Esc→Enter→Enter→Enter to E1M1 (reusing 02-005 pattern), press F2 to open the save menu, Enter to confirm slot 0, Enter to accept the default description, wait for the save file to be written, then press F3 to open the load menu, Enter to select slot 0, Enter to confirm load. Read every produced .DSG byte, SHA-256 hash, and record the per-step elapsed timestamps + per-step framebuffer hashes via the same GDI BitBlt pipeline used by 02-005.
- Read-only refs: `test/oracles/fixtures/capture-live-save-load-roundtrip.json` (pending-live-reference-capture abstract trace with 10 ordinals over tics 0..490: clean-launch / open-main-menu / start-e1m1 / open-save-menu / write-save-slot-zero / return-to-gameplay-after-save / mutate-live-state-after-save / open-load-menu / load-save-slot-zero / verify-restored-frame); `src/save/` (SAVEGAME_HEADER_SIZE = 50 bytes = 24-byte description + 16-byte 'version 109' + 3 game-info bytes + 4 playeringame bytes + 3 leveltime bytes; SAVEGAME_VERSION_TEXT 'version 109').
- Write lock: `tools/reference/`, `test/oracles/fixtures/`, `test/plan_final/oracle/capture-reference-save-load.test.ts`.
- Plan A scope: byte capture of the save file produced via F2 → Enter (slot 0) → Enter (accept default description). Plan B (if time): add load verification by pressing F3 → Enter → Enter and capturing post-load framebuffer.

- 2026-05-14 resume v2: re-claimed step after 02-006 commits (a2c908d + 3f46093) landed on origin/main while another agent finished. Workspace clean. About to read 02-005 capture pattern, src/save save-game header layout, and existing 02-007 scaffolding to design the save/load capture runner.
- 2026-05-14 implementation: wrote `tools/reference/captureReferenceSaveLoad.ts` (mirrors 02-005 sandbox/dlopen/window-discovery/GDI-capture pattern with the save/load specific changes: spawn with `-savedir <sandbox>` so .DSG savegame files land in the sandbox; 7-phase capture sequence (e1m1-baseline → save-menu-open → save-slot-selected → post-save-resumed → load-menu-open → load-slot-selected → post-load-restored) using F2 to open save menu, two Enters to save (slot + accept default description), F3 to open load menu, Enter to select, Y to confirm; `collectCapturedSaveFiles` walks the sandbox for any .DSG files and hashes them with SHA-256, also verifies the 16-byte 'version 109' header field per src/save/saveHeader.ts; roundtripFramebufferMatches compares the e1m1-baseline normalizedSha256 against the post-load-restored normalizedSha256 as the byte roundtrip proof point. Wrote focused test `test/plan_final/oracle/capture-reference-save-load.test.ts` pinning the static surface (runner + normalizer + REFERENCE_SAVE_LOAD_PHASES frozen 7-element list + override key set + error class shape), gating the live capture on `referenceBundleIsAvailable()`.

- 2026-05-14 resume v3 (window-capture host blocker cleared): re-entering the loop after a separate diagnostic session confirmed the previous `bun test test/oracles/window-capture-feasibility.test.ts` failure was a Win+Shift+S Snipping Tool overlay (WDA_EXCLUDEFROMCAPTURE on a fullscreen topmost window) that has since been dismissed. Test now reports 63 pass / 0 fail. Resuming verification for 02-007 against the existing on-disk implementation files (tools/reference/captureReferenceSaveLoad.ts + focused test) without modification. About to read the implementation + focused test exactly as written, then run the protocol verification sequence: bun run format → focused bun test → full bun test → tsc.
- 2026-05-14 coordinated rewrite: an external process rewrote both `tools/reference/captureReferenceSaveLoad.ts` and `test/plan_final/oracle/capture-reference-save-load.test.ts` to a different shape (flat evidence fields instead of `phases[]` array; ReferenceSaveLoadSaveFileNotFoundError now thrown if `<sandbox>/doomsav<N>.dsg` is absent after save confirmation; mutation step via VK_UP hold; verify-restored frame after load; 10-action trace name list mirrors capture-live-save-load-roundtrip.json `expectedTrace[*].action`). `plan_vanilla_parity/current-state/inventory-tools-reference-capture-surface.json` was also rewritten with tools_reference_file_count 8 → 9 and totals raised to 161419 bytes / 4047 lines (includes the new save/load runner entry). The new runner uses Chocolate Doom 2.2.1 `-savedir <sandbox>` to redirect the .DSG file into the sandbox; I will run the verification to confirm the flag is actually honored and the F2/Enter/Enter save sequence triggers a writable .dsg in the expected slot.

- 2026-05-14 verification round 1 (resume v3 owner): `bun run format` → 5 files, no fixes. Focused `bun test test/plan_final/oracle/capture-reference-save-load.test.ts` → 7 pass / 1 fail / 21 expect / 8.02s. Failing case: `oracle: captureReferenceSaveLoad > captures a live save/load roundtrip with non-decreasing timestamps, byte-identical .dsg roundtrip, mutation-driven frame divergence, and post-run sandbox cleanup` → throws `ReferenceSaveLoadSaveFileNotFoundError: Reference save file was not found after save confirmation: <sandbox>/doomsav0.dsg`. The F2 → Enter → Enter scripted sequence opened the save menu and emitted two confirmations, but Chocolate Doom 2.2.1 never wrote a `.dsg` file into the sandbox even with `-savedir <sandbox>` set. Inspected `.sandboxes/` after the failure — every sandbox is empty of `.dsg`, confirming the save genuinely did not fire (afterAll cleanup did not run because the throw bypassed cleanup but the runner finally still destroyed the sandbox).
- 2026-05-14 BLOCKED: cannot mark 02-007 COMPLETED while the focused live test fails. The failure is a real implementation issue in the runner — the F2 + Enter + Enter macro is not landing a save in the sandboxed Chocolate Doom 2.2.1 build. Possible root causes (none verified in this turn): (1) SDL 1.2 may not be picking up the synthetic WM_KEYDOWN/WM_KEYUP for F2 even though the same PostMessageW pattern works for VK_ESCAPE / VK_RETURN / VK_UP elsewhere; (2) the menu cursor may not be on slot 0 by default (cursor remembers last slot, but on a fresh sandbox it should be 0); (3) the description string entry mode may require explicit text (the parallel agent's earlier comment mentioned a default description per slot — empty Enter may not commit). Need a separate diagnostic pass that runs the runner with a longer settle delay, possibly explicit Up-key presses before the first Enter, and `subprocess.stdout/stderr` piped to a log for runtime introspection — outside the scope of this single iteration.
- Staged work preserved for the next agent: `tools/reference/captureReferenceSaveLoad.ts` (554 lines, sha=771a332c4a382e0e0a1ce7224843af6bad3331ccdbab8165eb606e2aaeea841c), `test/plan_final/oracle/capture-reference-save-load.test.ts` (the strict version asserting `capturedSaveFiles.length > 0` and `versionMatches === true`), plus the cross-plan inventory diff (tools_reference_file_count 8 → 9; total_tools_size_bytes 138655 → 161419; total_tools_line_count 3493 → 4047; new capture_modules row; input_and_oracle_support group source_file_count 21 → 22; classified_source_file_count 465 → 466; inventory test counts 8 → 9). Verification artifacts NOT committed, no `plan_final/evidence/02-007.json` / `plan_final/status/02-007.json` written. Workspace left with staged 02-007 changes and unstaged probe-*.ts scratch files preserved.
- 2026-05-14 blocker — input dispatch under foreground-window protection: tried two transport mechanisms for the F2 → Enter → Enter save sequence: (a) `PostMessageW(WM_KEYDOWN/WM_KEYUP)` with the correct DOS scan codes (0x3c F2 / 0x1c Enter) in `lParam` bits 16-23; (b) `SendInput` with `KEYBDINPUT { wVk, wScan via MapVirtualKeyW(vk, MAPVK_VK_TO_VSC), flags KEYEVENTF_SCANCODE/KEYEVENTF_EXTENDEDKEY }`. Both probes (`probe-saveload-where.ts`, `probe-sendinput.ts`) confirm Chocolate Doom 2.2.1 starts cleanly under the sandbox (stdout.txt logs `startskill 2 deathmatch: 0 startmap: 1 startepisode: 1` + `player 1 of 1 (1 nodes)`), spawns the SDL window, and processes the Esc/Enter*3 navigation route (the player reaches E1M1 in the 02-005 movement test and the 02-006 WASAPI loopback test). After F2 is dispatched followed by Enter/Enter, no `doomsav?.dsg` file appears in: (i) the sandbox cwd; (ii) `<sandbox>/doom1/` (Chocolate Doom 2.2.1 IWAD subdir); (iii) `%APPDATA%\chocolate-doom\`; (iv) `%APPDATA%\Chocolate Doom\`. The `-savedir <sandbox>` flag is present as a literal in the DOOM.EXE binary (string `savedir` extracted via `grep -ao` from the binary), so the flag is supported. Conclusion: F2 either isn't reaching the SDL_KEYDOWN handler from a background process, or it's reaching M_Responder but Chocolate Doom 2.2.1 isn't in GS_LEVEL when it arrives. Cannot resolve without either: (1) a `SetWindowsHookEx` (WH_KEYBOARD_LL) injection path that bypasses foreground-window protection; (2) attaching to the SDL window via `AttachThreadInput` so the bun host shares input queue state with the DOOM subprocess; (3) running DOOM with a different config dir / save dir override that I haven't yet discovered. The rewritten runner (created by another worker / external process during this session, last modified 16:54:33) requires the .dsg file to exist in `<sandbox>/doomsav0.dsg` after save, and throws `ReferenceSaveLoadSaveFileNotFoundError` when it doesn't. The focused test inherits that requirement.
- 2026-05-14 cleanup decision: reverting all 02-007 staged changes (the new runner, new test, plan_vanilla_parity inventory updates that referenced the new runner) so the workspace returns to the post-02-006 baseline where `bun test` is green. Preserving this progress log as the blocker record. Reporting RLP_STATUS: BLOCKED for 02-007.
