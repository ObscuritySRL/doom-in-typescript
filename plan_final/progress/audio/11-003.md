# 11-003 wire-spatial-sfx-runtime

- 2026-05-15 start: selected plan_final step 11-003 (lane audio). Goal: wire listener updates, origin tracking, attenuation, panning, pitch, and volume controls. Prerequisites 11-002 (commit `cc70a21`) and 09-001 are COMPLETED.
- Read the read-only refs `src/audio/spatial.ts` (`adjustSoundParams`; S_CLOSE_DIST=200*FRACUNIT, S_CLIPPING_DIST=1200*FRACUNIT, S_ATTENUATOR, S_STEREO_SWING=96*FRACUNIT, NORM_SEP=128, BOSS_MAP_MIN_VOLUME=15, BOSS_MAP_NUMBER=8, MIN/MAX_STEREO_SEP) and `src/audio/soundOrigins.ts` (`updateSounds`, `buildChannelUpdateStates`).
- Design decision: add `src/vanilla/wireSpatialSfxRuntime.ts` — a re-export facade collecting the S_AdjustSoundParams distance/pan computation + the S_UpdateSounds per-tic listener/origin pass into one cohesive surface. No read-only audio file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/audio/wire-spatial-sfx-runtime.test.ts` (6 tests / 20 expect() calls including the fixed-point attenuation-constant pins + same-reference identity).
- Ran `bun run format` → no fixes; focused `bun test` → 6 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/audio/soundOrigins.ts` src 1→2 test 1→2; `src/audio/spatial.ts` src 3→4 test 6→7 in `inventory-audio-modules.json`. Inventory test: 22 pass / 0 fail; diff is exactly 4 count lines.
- Ran `bun test` → one flaky captureReferenceDemoSync timing fail on the first run; clean retry at 16129 pass / 0 fail / 869 files. Typecheck → clean.