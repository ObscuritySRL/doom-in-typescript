# 11-007 wire-music-selection

- 2026-05-15 start: selected plan_final step 11-007 (lane audio) via `bun run plan_final/select-step.ts`. Prerequisites 11-006 and 07-001 are COMPLETED. No prior 11-007 artifacts — fresh step. Goal: wire title, level, intermission, finale, pause, and volume music behavior. Read-only: `src/audio/musicSystem.ts`, `src/ui/frontEndSequence.ts`, `src/ui/intermission.ts`, `src/ui/finale.ts`. Write lock: `src/audio/`, `src/ui/`, `src/vanilla/`, `test/plan_final/audio/wire-music-selection.test.ts`.
- 2026-05-15 reads: musicSystem.ts = pure S_*/I_* music engine (createMusicSystem default volume DEFAULT_MUSIC_VOLUME=8 device SNDDEVICE_SB; resolveMusicNumber does MUS_INTRO→MUS_INTROA only on ADLIB/SB + hasIntroALump; changeMusic/startMusic/stopMusic/pauseMusic/resumeMusic/setMusicVolume/isMusicPlaying/advanceMusic return frozen MusicDeviceAction[]; setMusicVolume THROWS RangeError outside [0,127], does not clamp; pauseMusic/resumeMusic no-op when no song / wrong pause state; constants MUSIC_VOLUME_MIN 0 / MAX 127 / NUMMUSIC 68 / MUS_INTRO 29 / MUS_INTROA 32). frontEndSequence `ShowPageTickAction.musicLump` carries title/demo page music; intermission `IntermissionMusicCue='mus_inter'` (Doom1 only, commercial mus_dm2int not emitted); finale `FinaleMusicCue='mus_victor'|'mus_bunny'|'mus_read_m'` (ep3 swaps victor→bunny). Design: `src/vanilla/wireMusicSelection.ts` re-export barrel (musicSystem value+type surface + the three UI music-cue/page types; no const enums; split value/type for verbatimModuleSyntax) + frozen `VANILLA_MUSIC_SELECTION_INVARIANTS` (6 rules). Inventory-neutral test (line-anchored specifier set + dynamic same-reference identity + real pure-engine behavior on createMusicSystem/setMusicVolume/pauseMusic/resolveMusicNumber). frontEndSequence/intermission/finale are UI-inventory-tracked; expect src-side drift there + audio inventory for musicSystem; will probe + reconcile.
- 2026-05-15 verify run 1: format wrapped 2 files; focused 8 pass / 1 fail = the re-export-structure test. Cause: prettier wrapped the 27-name musicSystem `export {}` block multi-line (exceeds printWidth 240), so the line-anchored `^export...from` regex no longer saw `from` on the export line. Fixed the test to a multi-line-tolerant global `\bfrom\s+['"]([^'"]+)['"]` filtered to `../`-prefixed specifiers (also excludes the JSDoc `./self` @example import); still inventory-neutral (the `../x.ts` literals sit in a toEqual array, not after `from '`). Focused 9/9.
- 2026-05-15 verify run 2: full `bun test` 16222 pass, 3 fail = flaky captureReferenceDemoSync + inventory-audio-modules + inventory-ui-modules. Probed both with the test's exact algorithm: four src-only drifts from the new facade's re-exports — musicSystem src 11→12 (audio), finale src 1→2, frontEndSequence src 23→24, intermission src 1→2 (ui); all test counts unchanged. Reconciled all four (audio anchored on the unique musParser/musScheduler external_imports; ui finale/intermission anchored on their unique exported_types since they share identical []/1/2 blocks; frontEndSequence on its unique external_imports). Probe reconfirms zero drift; removed throwaway probe. Rerunning full verification from the beginning.
