# 11-004 wire-sound-callback-bridge

- 2026-05-15 start: selected plan_final step 11-004 (lane audio) as first eligible step. Prerequisites 11-003 + 09-004 + 10-005 all COMPLETED. Goal: connect player/monster/switch/door/pickup/teleport/UI sound callbacks to S_StartSound. Write lock: src/audio/, src/player/, src/ai/, src/specials/, src/vanilla/, test/plan_final/audio/wire-sound-callback-bridge.test.ts. Read-only: src/player/, src/ai/, src/specials/, src/audio/soundSystem.ts. Research: AGENTS.md (binding, all session), plan_final/README.md.
- 2026-05-15 reads: src/audio/soundSystem.ts (450 lines, the S_StartSound composition). Values: NUMSFX 109, SFX_ID_MIN 1, SFX_ID_MAX = NUMSFX (109; vanilla `<= NUMSFX` quirk reads S_sfx[NUMSFX]), START_SOUND_LINK_MIN_VOLUME 1, PITCH_CLAMP_MIN 0, PITCH_CLAMP_MAX 255, startSound(request) -> StartSoundResult. Types: SfxPitchClass 'saw'|'static'|'default', StartSoundRequest, StartSoundResultKind 'started'|'link-silenced'|'inaudible'|'no-channel', StartSoundStartedResult, StartSoundDroppedResult, StartSoundResult. No const enums; single read-only module → no name collisions. s_sound.c order: validate sfx_id; seed volume=snd_SfxVolume/pitch=NORM_PITCH; link branch adds sfx->volume + replaces pitch, early-return when volume<1, clamp down to snd_SfxVolume; remote-origin (origin && origin!=listener) adjustSoundParams + same-position NORM_SEP override before audibility; pitch perturbation saw(+8-(M_Random()&15))/static(none)/default(+16-(M_Random()&31)) then clamp [0,255]; stopSound(origin) then allocateChannel. Design: src/vanilla/wireSoundCallbackBridge.ts re-export barrel (split value/type per verbatimModuleSyntax) over ../audio/soundSystem.ts + frozen VANILLA_SOUND_CALLBACK_BRIDGE_INVARIANTS (5 rules). Inventory-neutral test (constants, startSound typeof, frozen manifest, ../-filtered specifier set ['../audio/soundSystem.ts'], dynamic identity; behavioral guarantees are pinned by the manifest + the SHA-pinned module's own focused tests, so the test stays inventory-neutral via typeof + same-reference identity rather than constructing a full StartSoundRequest). soundSystem.ts is in inventory-audio-modules — probe + reconcile + sync; pathspec commits.
- 2026-05-15 verify: `bun run format` fixed 2 (facade + test wrapping); grep ` as unknown| as any| as [A-Z]` clean. Focused `bun test test/plan_final/audio/wire-sound-callback-bridge.test.ts` = 6 pass / 0 fail. Probe replicating the inventory scan + countImporters showed only src/audio/soundSystem.ts src 4->5 (the new facade); test unchanged at 2 — reconciled inventory-audio-modules.json soundSystem.ts src_importer_count 4->5 (no other field touched). Ran mandatory `bun run plan_final/sync-master-checklist.ts`. plan_vanilla_parity inventory suite = 644 pass / 0 fail; full `bun test` = 16377 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean. Finalizing with the two-commit pathspec pattern.
