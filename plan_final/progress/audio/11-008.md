# 11-008 wire-audio-hash-windows

- 2026-05-15 start: selected plan_final step 11-008 (lane audio). Prerequisites 11-005 and 11-007 are COMPLETED. No prior 11-008 artifacts — fresh step. Goal: expose deterministic SFX, mixed audio, and music-event hash windows for acceptance. Read-only: `src/audio/audioParity.ts`, `src/oracles/musicEventLog.ts`. Write lock: `src/audio/`, `src/oracles/`, `test/plan_final/audio/wire-audio-hash-windows.test.ts`.
- 2026-05-15 reads: audioParity.ts = the deterministic audio-parity harness (createAudioParityHarness arg-less OK, runHarnessTic→AudioParityTicOutput, hashPcmBuffer(Int16Array)→64-hex sha256, toAudioHashEntry, startHarnessSfx/stop/update + changeHarnessMusic/.../setHarnessMusicVolume/isHarnessMusicPlaying; HARNESS_TIC_RATE_HZ=35, HARNESS_SHA256_HEX_LENGTH=64, HARNESS_SAMPLES_PER_TIC_STEREO). musicEventLog.ts = MUSIC_EVENT_KINDS (4: change/pause/resume/stop), MusicEvent union, MusicEventLogPayload/Artifact, EMPTY_TITLE_LOOP/DEMO_PLAYBACK_MUSIC_EVENT_LOG (frozen), MUS_MAX_CHANNELS=16, MUS_PERCUSSION_CHANNEL=15, MIDI_PERCUSSION_CHANNEL=9, MUSIC_VOLUME_MIN 0/MAX 15. No const enums; no name collisions between the two modules. Sibling 11-005/11-006 placed facades in `src/vanilla/` (07-004 precedent) → `src/vanilla/wireAudioHashWindows.ts` re-export barrel + frozen `VANILLA_AUDIO_HASH_WINDOW_INVARIANTS` (5 rules). Inventory-neutral test (multi-line-tolerant `../`-filtered specifier set + dynamic same-reference identity + real hashPcmBuffer determinism/length + constants + frozen EMPTY logs). audioParity is audio-inventory-tracked, musicEventLog is oracles (likely a separate inventory) — expect src-side drift; will probe + reconcile. Finalize with pathspec-limited commits (parallel index contention — see memory).
- 2026-05-15 edits: created `src/vanilla/wireAudioHashWindows.ts` (re-export barrel + frozen `VANILLA_AUDIO_HASH_WINDOW_INVARIANTS` 5 rules) and focused test. Self-corrected: read-only `hashPcmBuffer` returns UPPERCASE hex (`.digest('hex').toUpperCase()`); fixed the test regex to `/^[0-9A-F]{64}$/` and the facade docstring/invariant from "lowercase" to "uppercase" to match the authoritative read-only source (accuracy, not weakening). Focused 8/8.
- 2026-05-15 verify run 1: full `bun test` 16233 pass, 2 fail = `inventory: audio modules` (audioParity src 0→1, expected) + NEW `plan_final master checklist sync` test (the concurrent governance agent committed sync-master-checklist + PROMPT.md step 10 at 96a826b; the suite now enforces MASTER_CHECKLIST.md == status JSON). Reconciled audioParity src 0→1. Ran the sanctioned `bun run plan_final/sync-master-checklist.ts` (updatedLineCount 1 — mostly already synced by 96a826b; 89/132 now checked). This is derive-from-authoritative-status (status JSON only exists for verified+pushed steps), NOT the manual checkbox-faking the /goal directive prohibits, and is now a committed mandatory per-step contract whose enforcing test must pass for ANY step to complete. MASTER_CHECKLIST.md will be committed with this step per the new PROMPT.md step-10 contract. sync test now 2/2. Rerunning full verification from the beginning.
