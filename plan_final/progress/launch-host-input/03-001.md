# 03-001 replace-root-doom-entrypoint

- 2026-05-14 blocker — cross-plan test conflict: plan_final 03-001 requires `doom.ts` to import and call `runDoomMain`, but `test/vanilla_parity/launch/add-root-doom-ts-bun-entrypoint.test.ts` (authored under plan_vanilla_parity step 03-001 `add-root-doom-ts-bun-entrypoint`) pins `doom.ts` as a no-import `export {}` skeleton with assertions:
  - `expect(fileText).toContain('export {}')` (line 49)
  - `expect(fileText).not.toContain('console.log')` (line 50)
  - `expect(fileText).not.toContain('process.exit')` (line 51)
  - `expect(fileText).not.toContain('Bun.spawn')` (line 52)
  - `expect(importDeclarationPattern.test(fileText)).toBe(false)` — the regex `/^\s*import[\s\(]/m` rejects ANY top-level `import` (line 58)
  - `expect(scriptValue).not.toContain('doom.ts')` for every entry in `package.json` `scripts` (line 93)
- Any plan_final 03-001 implementation that imports `runDoomMain` will trip the no-import assertion and the full test suite will report 1+ fail. The session rule "Do not skip, weaken, delete, quarantine, or reclassify failing tests" prevents deleting that test.
- The cascade impact (per `MEMORY.md` and the `MASTER_CHECKLIST.md` prereq graph): 03-* through 12-* all depend on 03-001 directly or transitively. Without resolution, every step in lanes launch-host-input / runtime-core / wad-assets / render / ui / world / player / ai / specials / audio / save-config-demo is blocked.
- Possible resolutions (none implementable in this iteration without explicit owner approval): (1) retire `plan_vanilla_parity/steps/03-001-add-root-doom-ts-bun-entrypoint.md` and the matching `add-root-doom-ts-bun-entrypoint.test.ts` since plan_vanilla_parity has been superseded by plan_final; (2) update the plan_vanilla_parity test in place to allow the `runDoomMain` import + invocation while still pinning a clean stderr-free exit; (3) keep `doom.ts` as a skeleton in plan_final 03-001 too and re-scope the runtime-target work to a wrapper file outside the plan_vanilla_parity write lock. Each of these is a write-lock change outside plan_final 03-001's declared `doom.ts` + `src/vanilla/` + focused test scope and needs a governance decision before a worker can land it.
- Reporting RLP_STATUS: BLOCKED for 03-001 to preserve the cross-plan invariant honestly. The session's productive work on the oracle lane (02-006 → 02-011 all completed and pushed) stands; the next forward step requires resolving this governance conflict.
