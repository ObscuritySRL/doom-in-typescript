# 03-001 replace-root-doom-entrypoint

- 2026-05-14 start: claimed step 03-001 (launch-host-input lane). Prereq 01-001 is COMPLETED. 01-010 and 00-007 are eligible-first in other lanes but already have in-flight progress logs from other agents; 03-001 has no progress log and an empty target directory.
- Goal: replace doom.ts skeleton with a Bun entrypoint that calls runDoomMain and reports real errors.
- Write lock: doom.ts, src/vanilla/, test/plan_final/launch/replace-root-doom-entrypoint.test.ts.
- Read-only refs: doom.ts, src/main.ts, package.json.
- Read doom.ts: 12 lines, `export {}` skeleton with header comment pointing at plan_vanilla_parity step 03-001.
- Read src/main.ts: 103 lines, simplified launcher that uses CommandLine + createLauncherSession + runLauncherWindow.
- Read package.json: scripts are `format` and `start`; no `doom` script.
- Confirmed src/vanilla/ does not exist on disk; test/plan_final/launch/ does not exist on disk.
- Plan: create `src/vanilla/runDoomMain.ts` with a top-level `runDoomMain` function that is the vanilla D_DoomMain placeholder (will be incrementally wired by steps 03-002..03-010). For 03-001, the function returns cleanly without doing simulation/render/audio side effects, but throws a typed error if any of its callers passes invalid arguments — that gives 03-001 a real "reports real errors" surface to test. Rewrite `doom.ts` to import `runDoomMain`, invoke it inside an awaited try/catch that writes the error message to stderr and exits non-zero. Add `test/plan_final/launch/replace-root-doom-entrypoint.test.ts` that (a) verifies doom.ts imports `runDoomMain`, (b) spawns `bun run doom.ts` and asserts it exits 0 with empty stderr on a clean run, and (c) spawns it with an env variable that forces an error path and asserts exit code is non-zero with the error message on stderr.
- Implemented the plan: wrote src/vanilla/runDoomMain.ts, rewrote doom.ts to import runDoomMain and wrap it in try/catch with process.stderr.write + process.exit(1), and added test/plan_final/launch/replace-root-doom-entrypoint.test.ts.
- `bun run format` formatted 4 files; no fixes applied.
- `bun test test/plan_final/launch/replace-root-doom-entrypoint.test.ts` passed 5 of 5.
- `bun test` FAILED on 5 tests across two governance surfaces:
  - `test/vanilla_parity/launch/add-root-doom-ts-bun-entrypoint.test.ts` (three tests, pinned by plan_vanilla_parity step 03-001 `add-root-doom-ts-bun-entrypoint`) explicitly asserts doom.ts has no import declarations, no top-level side effects beyond `export {}`, and contains a comment naming the plan_vanilla_parity launch lane scope. These directly contradict plan_final 03-001's goal of making doom.ts a runDoomMain-calling Bun entrypoint.
  - `test/plan_final/current-state/root-entrypoints.test.ts` and `test/plan_final/current-state/runtime-implementation-modules.test.ts` (mine, from 01-001 and 01-004) record doom.ts as a 12-line, 676-byte, `export {}` skeleton with zero imports and `wires_vanilla_runtime: false`. The new doom.ts breaks the size/sha256/line-count assertion AND the runtime_reachability_summary assertions.
- BLOCKED: plan_final 03-001 cannot pass its own focused test AND the previously-shipped plan_vanilla_parity 03-001 tests at the same time. The plan_final step file's write lock is `doom.ts`, `src/vanilla/`, `test/plan_final/launch/replace-root-doom-entrypoint.test.ts` — it does NOT include the plan_vanilla_parity test file that pins the skeleton invariant, and the user goal forbids skipping/weakening/deleting/quarantining/reclassifying failing tests.
- Fix attempt: reverted doom.ts to the original 12-line `export {}` skeleton (sha256 571e5dadc6da3a27c3ce0fe72701df05818ef339647a00b1c54940df209a8e84, 676 bytes), removed `src/vanilla/` and `test/plan_final/launch/` so the working tree returns to the pre-step state. Verification reruns clean. Step is not marked COMPLETED.
- BLOCKED status: this step requires governance resolution — either plan_vanilla_parity's locked skeleton invariant must be formally superseded by plan_final 03-001 (and its tests retired by the owning lane), or plan_final 03-001's goal/write-lock must be widened to include the plan_vanilla_parity test that pins the skeleton.

