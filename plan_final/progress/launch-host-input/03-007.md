# 03-007 wire-responder-chain-input

- 2026-05-14 start: selected plan_final step 03-007 (lane launch-host-input). Goal: route events through menu, automap, finale, and game responders in vanilla order. Prerequisites 03-006 (commit `8ef34d4`) and 04-004 (commit `f8822dc`) are both COMPLETED per status JSONs. HEAD = origin/main = `95c86f9` at session start. Working tree clean modulo unrelated probes/scratch.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs in `src/input/` (keyboard.ts, mouse.ts, ticcmd.ts, focusPolicy.ts — the event types and the event-translation primitives 03-006 already consumes). The `src/ui/menus.ts` and `src/ui/frontEndSequence.ts` refs are listed but consumed only as conceptual references for the canonical responder positions; no UI file is modified.
- Design decision: add `src/vanilla/responderChain.ts` exposing a `VanillaResponder = (event: VanillaEvent) => boolean` type, an `VANILLA_RESPONDER_ORDER` enum of the four canonical positions in d_main.c `D_ProcessEvents` order (M_Responder, AM_Responder, F_Responder, G_Responder), and a `routeEventThroughResponderChain(event, responders)` function that returns a frozen result with the consuming index (or null when no responder consumed the event) and the call-order log. The chain stops at the first responder that returns true, matching vanilla's `if (M_Responder(ev)) return; if (AM_Responder(ev)) return; ...` semantics. The responder list is supplied by the caller so the host bring-up step can plug in the real subsystem responders later; this step provides only the dispatch wiring and pins the canonical order.
- Wrote `src/vanilla/responderChain.ts` and `test/plan_final/launch/wire-responder-chain-input.test.ts` (11 tests / 32 expect() calls). Tests cover: file existence + 03-007 citation, VANILLA_RESPONDER_ORDER pin, every-responder-declines case (full chain walk), early-stop at every position (M / AM / F / G consumes), frozen result + frozen callOrder, length-validation (throws when not 4 responders), and same-event-reference invariant across all invocations.
- Ran `bun run format` → 1 file fixed (test wrapping); subsequent runs clean.
- Ran `bun test test/plan_final/launch/wire-responder-chain-input.test.ts` → 11 pass / 0 fail / 32 expect() calls.
- Ran `bun test` → 15820 pass / 0 fail / 836 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.