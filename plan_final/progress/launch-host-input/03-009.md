# 03-009 wire-clean-quit-and-endoom

- 2026-05-15 start: selected plan_final step 03-009 (lane launch-host-input) via `bun run plan_final/select-step.ts`. Prerequisites 03-005 (commit b1817fd) and 07-010 (commit 9bed8ce) are COMPLETED. No prior 03-009 artifacts — fresh step. Goal: wire I_Quit, close button, quit confirmation, fatal errors, and ENDOOM behavior. Read-only: `src/bootstrap/quitFlow.ts`, `src/ui/endoom.ts` (both already read in full during 07-010). Write lock: `src/bootstrap/`, `src/ui/`, `src/vanilla/`, `test/plan_final/launch/wire-clean-quit-and-endoom.test.ts`.
- 2026-05-15 reads: read sibling launch-lane facade `src/vanilla/win32WindowHost.ts` (03-005) for the established launch-host convention (pure unit-testable primitives + cited step + frozen manifest; FFI left to src/launcher/win32.ts). Inspected 03-005..03-008 evidence: launch facades live in `src/vanilla/` with tests in `test/plan_final/launch/`; neither is a plan_vanilla_parity source_roots/test_roots entry, but `countImporters` in the UI inventory scans `test/**/*.ts` globally, so a `from '...endoom.ts'` literal anywhere drifts endoom counts. Design: 03-009 facade re-exports the quit/ENDOOM surface from the 07-010 facade `./wireQuitAndEndoomUi.ts` (same src/vanilla dir, in write lock) — NOT directly from the read-only `../ui/endoom.ts` — so no new direct importer is added to the read-only UI module and no UI-inventory rewrite is required (matching the 03-006/03-007 "no inventory rewrite" precedent). The path string `./wireQuitAndEndoomUi.ts` does not match the case-sensitive `\bendoom` countImporters regex.
- 2026-05-15 edits: created `src/vanilla/cleanQuitAndEndoom.ts` (re-exports the quit/ENDOOM surface through `./wireQuitAndEndoomUi.ts`; adds `QuitTrigger`, `QuitPlan`, `resolveQuitPlan`, `runQuitPlan`, and the frozen `VANILLA_CLEAN_QUIT_INVARIANTS` pinning 4 launch-host rules: close-button→clean I_Quit, ENDOOM only on clean quit, fatalError→executeErrorQuit + exit 1, confirmed menu quit→I_Quit) and focused test `test/plan_final/launch/wire-clean-quit-and-endoom.test.ts` (exists/cites-step, routes-through-07-010-facade + asserts it does NOT import the read-only modules, frozen 4-rule manifest, resolveQuitPlan for all 4 triggers, runQuitPlan LIFO drain for clean triggers + error drain for fatalError, same-reference identity vs the 07-010 facade). No read-only file modified; no `from '...endoom.ts'`/`from '...quitFlow.ts'` literal in either new file, so no UI-inventory drift expected. Running verification.
- 2026-05-15 verify run 1: format fixed 1 file (export-list wrap in cleanQuitAndEndoom.ts); focused 6 pass; full `bun test` 16191 pass, 1 fail = `inventory-ui-modules.test.ts:331` endoom test_importer_count. Cause: the focused test's `expect(fileText).not.toContain("from '../ui/endoom.ts'")` assertion literally embedded the `from '../ui/endoom.ts'` substring, which the UI inventory's whole-file countImporters heuristic matched. Manifest left UNCHANGED.
- 2026-05-15 fix: rewrote that test to parse the facade's line-anchored `^(?:import|export)...from '...'` specifiers and assert the deduped set equals `['./wireQuitAndEndoomUi.ts']` (no embedded read-only literal; JSDoc @example self-import excluded by the line anchor). Keeps the launch-lane change inventory-neutral (03-006/03-007 precedent) instead of thrashing the shared cross-plan manifest. Confirmed `plan_vanilla_parity/current-state/inventory-ui-modules.json` is unmodified by this step.
- 2026-05-15 verify run 2 PASS (canonical order): `bun run format` clean -> focused `bun test test/plan_final/launch/wire-clean-quit-and-endoom.test.ts` 6 pass / 0 fail -> full `bun test` 16192 pass / 0 fail across 878 files -> `bun x tsc --noEmit --project tsconfig.json` exit 0. Wrote `plan_final/evidence/03-009.json` (knownFailures: [], crossPlanInventoryRewriteRequired: false). Staging explicit paths for the feat commit (status JSON deferred to the post-push docs commit).
