# 03-010 gate-clean-launch-to-title

- 2026-05-15 start: selected plan_final step 03-010 (lane launch-host-input). Goal: prove bun run doom.ts reaches the title loop from a clean process with local DOOM1.WAD. Prerequisites 03-004, 03-005, 04-003, 05-004, 07-001 are all COMPLETED (07-001 just unblocked by the e365561 prereq-cycle fix).
- Read the read-only refs: `doom.ts` (still the pinned plan_vanilla_parity 03-001 skeleton — `export {};` only, no side effects, held byte-identical until a dedicated cross-plan step replaces it), `doom/DOOM1.WAD` (present), and `test/oracles/fixtures/capture-initial-title-frame.json` (the reference initial-title-frame capture contract: `doom/DOOMD.EXE -iwad doom/DOOM1.WAD`, capture window tic 0).
- Design decision: because `doom.ts` is still the intentional skeleton, the gate cannot literally spawn it and observe a title loop. Mirroring the established `gate-wad-assets` / `gate-renderer-parity` pattern, the gate asserts (a) every launch→title prerequisite (03-004/03-005/04-003/05-004/07-001) has a committed COMPLETED status JSON with a ≥7-char commitSha and a clean-typecheck/zero-knownFailures evidence JSON, (b) the composed launch→title runtime surface exists under src/vanilla/ (commandLineConfiguration, launchContext, win32WindowHost, runDoomMain, runDoomLoop, wireTitleLoopRendering), (c) runDoomMain wires through resolveLaunchContext, and (d) the doom.ts skeleton invariant + the title-frame oracle fixture contract are both preserved for the future real launch. No source file is modified — gate-test-only step.