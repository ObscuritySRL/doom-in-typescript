# 03-003 wire-vanilla-command-line

- 2026-05-14 start: selected plan_final step 03-003 (lane launch-host-input). Goal: wire vanilla command-line parsing into `runDoomMain` for IWAD, config, warp, playdemo, timedemo, skill, savedir. Prerequisite 03-001 is COMPLETED at commit `df1882d`; 03-002 is BLOCKED on a separate cross-plan SHA pin against `src/main.ts` that does not affect this step.
- Read `plan_final/PROMPT.md`, `plan_final/steps/03-003-wire-vanilla-command-line.md`, the read-only refs `src/bootstrap/cmdline.ts` and `src/bootstrap/implement-vanilla-command-line-parsing.ts`, the existing wrapper `src/vanilla/runDoomMain.ts`, the prior step's evidence `plan_final/evidence/03-001.json`, status JSONs for 03-001/03-002, and the 03-001 progress log.
- Verified cross-plan SHA pin status: `test/vanilla_parity/current-state/inventory-src-bootstrap-surface.test.ts` line 203 enforces `moduleEntry.sha256` for every committed bootstrap module against the on-disk sha256, and the inventory JSON pins the canonical sha256 values. Both `src/bootstrap/cmdline.ts` and `src/bootstrap/implement-vanilla-command-line-parsing.ts` are inside that pinned set. Treating both as strict read-only and consuming the existing `CommandLine` class API (no modification).
- Design decision: keep the existing single-argument `runDoomMain(argumentVector?: unknown): Promise<void>` signature byte-stable (plan_final 03-001 evidence JSON lists it as a stable public surface), and introduce a separate exported helper `parseCommandLineConfiguration(argv: readonly string[]): CommandLineConfiguration` that wraps the bootstrap `CommandLine` class. Wire `runDoomMain` to invoke it on the validated argv so the configuration flows through. On parse failure, throw a new typed `CommandLineParseError extends Error` with the offending argument in the message.
- Wrote `src/vanilla/commandLineConfiguration.ts`: exports `CommandLineConfiguration`, `WarpTarget`, `CommandLineParseError`, `parseCommandLineConfiguration`. Wires the seven flags `-iwad`, `-config`, `-warp`, `-playdemo`, `-timedemo`, `-skill`, `-savedir` via the existing `CommandLine` class (no bootstrap modifications). `-skill` is validated against the vanilla `[1, 5]` range. `-warp` accepts both `-warp <episode> <map>` (episodic) and `-warp <map>` (Doom 2) forms via `CommandLine.getParameterValues`. Numeric arguments are validated with `^-?\d+$` then range-checked. Empty trailing values are rejected.
- Extended `src/vanilla/runDoomMain.ts` to import and invoke `parseCommandLineConfiguration` on the validated argv. The single-argument public signature is preserved byte-stable; on parse failure the typed `CommandLineParseError` propagates from `runDoomMain` to the caller (this is what the focused test asserts).
- Wrote `test/plan_final/launch/wire-vanilla-command-line.test.ts` with 27 tests covering: wrapper file existence + 03-003 comment, bootstrap-CommandLine import string, runDoomMain-imports-parseCommandLineConfiguration, public surface arity, `CommandLineParseError` constructor + fields, all-null configuration for program-name-only argv, individual happy paths per wired flag (`-iwad`, `-config`, `-warp` two-arg + one-arg, `-playdemo`, `-timedemo`, `-skill`, `-savedir`), composite-argv flow-through, case-insensitive matching, frozen-result invariant, and failure modes (`-skill banana`, `-skill 9`, `-iwad` tail, `-warp` no args, `-warp` non-numeric episode), plus runDoomMain integration: happy-path string-array argv, `InvalidArgumentError` preserved from 03-001, `CommandLineParseError` propagated from invalid argv, and `--iwad` (double-dash) ignored to preserve the 03-001 baseline argv shape.
- Ran `bun run format` → formatted 3 files (1 fix to the new focused test for line-length wrapping; final file size identical otherwise).
- Ran `bun test test/plan_final/launch/wire-vanilla-command-line.test.ts` → 27 pass, 0 fail, 74 expect() calls.
- Ran `bun test` → 15586 pass, 0 fail across 823 files (up from the 03-001 baseline of 15555 pass / 821 files: this step adds 1 new test file with 27 tests; the remaining +4 tests come from other in-flight work landed since 03-001).
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean (no output).
- No plan_vanilla_parity inventory rewrites required: `src/vanilla/` is not a tracked `source_roots` entry, and the bootstrap directory inventory at `plan_vanilla_parity/current-state/inventory-src-bootstrap-surface.json` is untouched (sha256 pins for `src/bootstrap/cmdline.ts` and `src/bootstrap/implement-vanilla-command-line-parsing.ts` remain valid because neither file was modified).
- Verified `git fetch origin main` against the pre-commit HEAD: origin remained at `21fcbee` (the 03-002 BLOCKED docs commit), so no rebase was required.
- Staged explicit paths: `src/vanilla/commandLineConfiguration.ts`, `src/vanilla/runDoomMain.ts`, `test/plan_final/launch/wire-vanilla-command-line.test.ts`, `plan_final/evidence/03-003.json`, `plan_final/progress/launch-host-input/03-003.md`. Committed `feat(plan_final): wire vanilla command-line parsing into runDoomMain for 03-003` as `b44e2ad36f6bd4d76621d2cb0f537d41c61b1d70` and pushed to `origin/main`; verified remote via `git log origin/main -1`.
- Wrote `plan_final/status/03-003.json` with `COMPLETED` status and commitSha `b44e2ad36f6bd4d76621d2cb0f537d41c61b1d70`.
- RLP_STATUS: COMPLETED for 03-003 under the wrapper-file interpretation (extends the same precedent set by 03-001, with the bootstrap surface preserved byte-stable).
