# 03-002 retire-or-redirect-src-main

- 2026-05-14 start entry. read AGENTS.md, plan_final/PROMPT.md, the 03-002 step file, the prior 03-001 progress log, src/main.ts (103 lines), test/launcher/cli.test.ts, test/launcher/session.test.ts, src/vanilla/runDoomMain.ts, and the cli test harness at test/_harness/cli.ts.
- intent under the step contract: reduce src/main.ts to a thin compatibility wrapper that re-exports from src/vanilla/runDoomMain.ts (or a helper imported by it) while preserving `bun run src/main.ts`-style entrypoint behavior for test/launcher/cli.test.ts and the `bun run start` script.

- 2026-05-14 blocker — cross-plan write-lock conflict on src/main.ts. The plan_final 03-002 write lock is src/main.ts, but several tests outside the plan_final lane pin src/main.ts byte-for-byte and would fail on any modification. Specifically:
  - `test/plan_fps/01-013-audit-missing-save-load-ui.test.ts:171` pins `expect(await sha256Hex('src/main.ts')).toBe('019ea4be868270e62d17d3557679dbecf0cbf838c10f07c393d1d9cd54300f44')`. Verified the current file SHA matches that hash (`bun -e "createHash('sha256').update(await Bun.file('src/main.ts').bytes()).digest('hex')"` returned `019ea4be868270e62d17d3557679dbecf0cbf838c10f07c393d1d9cd54300f44`). Any byte change to src/main.ts trips that assertion.
  - `test/plan_fps/01-013-audit-missing-save-load-ui.test.ts:175-189` requires src/main.ts to contain the exact strings `'const resources = await loadLauncherResources(iwadPath);'`, `'const session = createLauncherSession(resources, {'`, `'await runLauncherWindow(session, {'` in that order, plus `'  Tab: toggle gameplay view and automap'` and `'  Esc: quit'`.
  - `test/plan_fps/01-014-audit-missing-config-persistence.test.ts:336-355` and 357-372 require src/main.ts to contain `"commandLine.getParameter('--map') ?? DEFAULT_MAP_NAME"`, `"parseIntegerParameter(commandLine, '--scale', DEFAULT_SCALE)"`, `"parseIntegerParameter(commandLine, '--skill', DEFAULT_SKILL)"`, and `'const localIwad = Bun.file(DEFAULT_LOCAL_IWAD_PATH);'`, and to NOT contain `'Bun.write'`, `'chocolate-doom.cfg'`, `'default.cfg'`, `'keyBindings'`, `'loadConfig'`, `'saveConfig'`.
  - `test/plan_fps/01-015-audit-missing-side-by-side-replay.test.ts:317-329` pins multiple substrings present in the current src/main.ts launcher body and asserts ordering between `'if (commandLine.parameterExists(\'--list-maps\'))'`, `'const session = createLauncherSession(resources, {'`, and `'await runLauncherWindow(session, {'`. Lines 347-356 lock the help usage lines verbatim against the live launcher source via `manifest.commandContracts.currentLauncher.helpUsageLines`.
  - Approximately 60+ additional tests under `test/oracles/**` and `test/playable/**` reference `'src/main.ts'` as the current entrypoint path, the `'bun run src/main.ts'` package start command, or pin manifest evidence paths containing `'src/main.ts'`. While many of those tests gate on string equality (and would still pass if the file exists with the same path), the SHA pin + ordered-substring assertions in plan_fps 01-013/01-014/01-015 alone are sufficient to block any wrapper-shrink edit.

- the situation is structurally identical to the plan_final 03-001 blocker on doom.ts (where plan_vanilla_parity 03-001 pinned the skeleton invariant via a no-imports/no-side-effects assertion). Here, plan_fps 01-013..01-015 pin the current launcher body exactly as it stands. The plan_final 03-002 write lock does NOT include `test/plan_fps/`, `test/playable/`, `test/oracles/`, or any of the plan_fps manifest files used by those tests (e.g. `plan_fps/manifests/01-013-audit-missing-save-load-ui.json`, `plan_fps/manifests/01-014-audit-missing-config-persistence.json`, `plan_fps/manifests/01-015-audit-missing-side-by-side-replay.json`). The plan_fps tests assert against both manifests and the file contents simultaneously, so updating the manifests without touching the tests is not enough either.

- additional constraint: the step instructions explicitly require "no skipping/weakening failing tests" and forbid modifying `test/vanilla_parity/**` or `plan_vanilla_parity/**`. By the same governance principle that produced the 03-001 wrapper-file interpretation, this worker cannot delete or rewrite plan_fps tests that fall outside the declared 03-002 write lock. Doing so would weaken a test that is currently passing.

- possible resolutions (none implementable in this iteration without explicit owner approval, mirroring the 03-001 governance pattern):
  1. Retire `plan_fps/**` and the corresponding `test/plan_fps/**` tests as superseded prior art (CLAUDE.md already says "`plan_engine/` is prior art only"; the 03-001 evidence log states plan_fps was superseded by plan_vanilla_parity, which itself is being superseded by plan_final). This would require an explicit governance decision before a worker can land it.
  2. Update each plan_fps SHA / substring assertion in test/plan_fps/01-013-audit-missing-save-load-ui.test.ts (line 171, 175-189), test/plan_fps/01-014-audit-missing-config-persistence.test.ts (lines 336-372), and test/plan_fps/01-015-audit-missing-side-by-side-replay.test.ts (lines 317-329, 347-356) to track the new thin-wrapper src/main.ts content, plus update the matching plan_fps/manifests/01-01{3,4,5}-*.json files. This expands the write lock beyond 03-002's declared scope.
  3. Keep src/main.ts byte-identical to the current state under 03-002, leaving the "retire or redirect" goal unsatisfied and re-scoping the work to a future step that resolves the cross-plan write-lock conflict on src/main.ts separately. Equivalent in spirit to the 03-001 wrapper-file deferral.

- reporting RLP_STATUS: BLOCKED for 03-002 to preserve the cross-plan invariant honestly. Working tree left clean of any 03-002 staging beyond this progress log.
- staged plan_final/progress/launch-host-input/03-002.md explicitly by path and committed it as `docs(plan_final): record 03-002 cross-plan test conflict blocker` (sha `cb8bc81`). Pushed to origin/main and verified the remote head via `git fetch origin main` + `git log origin/main -1`. No src/main.ts, package.json, test/launcher/, or test/plan_final/launch/ files were touched.
- no plan_final/evidence/03-002.json or plan_final/status/03-002.json was written: the focused test was not authored and the verification sequence was not run because the implementation is blocked, and writing a COMPLETED status against a blocked step would violate the step's completion criteria ("The step is not marked complete, committed, or pushed while any verification command is failing.").

- 2026-05-14 selector-skip resolution start entry. Read AGENTS.md, plan_final/PROMPT.md, plan_final/select-step.ts, plan_final/planData.ts (FinalPlanStep shape), plan_final/validate-plan.ts (confirmed findStatusSchemaViolations short-circuits when status.status !== 'COMPLETED', so a BLOCKED status JSON is schema-clean), test/plan_final/select-step.test.ts, and test/plan_final/validate-plan.test.ts. Verified 03-002 is not referenced as a prerequisite by any other FINAL_PLAN_STEPS entry (so skipping does not strand any downstream step). Plan: (1) extend plan_final/select-step.ts to skip BLOCKED steps at selection time without satisfying their prerequisites, (2) write plan_final/status/03-002.json with status BLOCKED documenting the cross-plan src/main.ts SHA pin, (3) write plan_final/evidence/03-002.json documenting blockerDetails, (4) add a focused test test/plan_final/governance/selector-skips-blocked.test.ts covering both the skip-and-advance case and the prerequisite-blocking case. Plan PROMPT.md already lists BLOCKED as a valid RLP_STATUS and does not describe selector internals, so no doc update is required.

- 2026-05-14 resolution committed and pushed. Pushed SHA `fb29aad` `feat(plan_final): selector skips BLOCKED status JSONs and 03-002 marked blocked by plan_fps src/main.ts SHA pins` to origin/main (verified `git fetch origin main` shows fb29aad as origin/main head). Verification sequence ran in order: `bun run format` (2 files formatted, no fixes applied), focused `bun test test/plan_final/governance/selector-skips-blocked.test.ts` (4 pass, 11 expect calls), full `bun test` (15559 pass on the clean run; one unrelated live-reference-capture flake observed under suite-load on prior runs in test/plan_final/oracle/capture-reference-demo-sync.test.ts at line 181 timing assertion — passes in isolation; not touched by selector changes), `bun x tsc --noEmit --project tsconfig.json` (clean). `bun run plan_final/select-step.ts` now returns step `03-003` (`wire-vanilla-command-line` in lane launch-host-input), confirming the BLOCKED skip advances the cascade past 03-002. 03-002 stays uncompleted (it is BLOCKED, not COMPLETED); the plan_final 13-011 final acceptance gate will correctly continue to report 03-002 as not-COMPLETED until governance resolves the cross-plan src/main.ts SHA pin.

- 2026-05-15 OWNER-UNBLOCK resume. Owner authorized plan supersession via AskUserQuestion (recorded in memory plan_final_governance_blockers.md): plan_final supersedes plan_vanilla_parity & plan_fps; retiring their doom.ts/src/main.ts pinning suites (test/plan_fps/01-013..01-015, test/vanilla_parity/launch/add-root-doom-ts-bun-entrypoint.test.ts, sibling skeleton/SHA pins) and wiring the real engine into doom.ts is now the sanctioned path; the no-manifest-only-final-gate rule still stands. This directly satisfies the blockerReason's stated resolution ("retire the plan_fps audit tests as part of plan supersession"). Re-read AGENTS.md, plan_final/PROMPT.md, the 03-002 step file, this progress log, src/main.ts (103 lines: CommandLine -> loadLauncherResources -> createLauncherSession -> runLauncherWindow launcher entrypoint). Plan for this iteration: (1) clear BLOCKED in plan_final/status/03-002.json; (2) reduce src/main.ts to a thin BEHAVIOR-PRESERVING wrapper that re-invokes the same launcher path so test/launcher/ behavioral tests stay green; (3) retire (delete) the owner-named pin suites test/plan_fps/01-013-audit-missing-save-load-ui.test.ts, 01-014, 01-015, test/vanilla_parity/launch/add-root-doom-ts-bun-entrypoint.test.ts; (4) write focused test test/plan_final/launch/retire-or-redirect-src-main.test.ts; (5) run full verification, empirically discover the cascade, retire ONLY further pins that are provably skeleton/SHA assertions the owner sanctioned (do NOT weaken genuine behavioral/parity tests — if a behavioral test breaks, fix the wrapper instead); (6) evidence+status COMPLETED, sync checklist, pathspec commit+push. If the cascade requires weakening genuine behavioral tests, STOP and report honestly (no-fake rule stands).

- 2026-05-15 DECISION + EXECUTION (owner: "make decisions for me"). Decided option (A): DEFER 03-002 (src/main.ts + the 64-file SHA backbone + package.json left byte-identical — non-destructive, reversible, 03-002 blocks nothing) and instead WIRE doom.ts (the actual headline-goal linchpin). Cascade scoped empirically: only 3 files assert the doom.ts skeleton; 179 oracle tests pin `bun run src/main.ts` (left untouched). Changes: doom.ts now `import { runDoomMain } from './src/vanilla/runDoomMain.ts'; await runDoomMain(Bun.argv.slice(2));` (722B/13L/sha bc2404bc...) — `bun run doom.ts` exits 0 cleanly (with/without args) since runDoomMain's current body parses/validates/resolves and returns; deeper D_DoomMain/D_DoomLoop is progressive launch-lane work. Retired the 3 owner-sanctioned doom.ts skeleton pins: test/vanilla_parity/launch/add-root-doom-ts-bun-entrypoint.test.ts (owner-named explicitly), test/plan_final/launch/replace-root-doom-entrypoint.test.ts + test/plan_final/launch/gate-clean-launch-to-title.test.ts (plan_final's own skeleton-interpretation tests, superseded by the owner's "wire doom.ts" authorization). Reconciled 3 inventory records to TRUTH (not faked): plan_final/current-state/root-entrypoints.json + .test.ts (doom.ts size/line/sha + current_behavior vanilla-runtime-runner + wires_vanilla_runtime true + gap_summary doom_ts_is_runtime_entrypoint true / skeleton_only false); plan_final/current-state/runtime-implementation-modules.json + .test.ts (doom_ts_imports_count 1, doom_ts_is_skeleton false, subsystems_reachable_from_doom_ts ["src/bootstrap/"] computed by transitive import trace, not_reachable 22, test now asserts live-computed wired invariant + internal consistency); plan_vanilla_parity/current-state/classify-real-implementations.json (bootstrap_runtime_contracts test_file_count 44->43 for the one deleted vanilla_parity/launch test, classified_test_file_count 274->273). Ran mandatory sync-master-checklist. Verify: bun run format (no fixes), bun x tsc --noEmit (clean), full bun test = 16395 pass / 0 fail. No genuine behavioral test weakened; no-fake honored (all inventory records made TRUE). src/main.ts/package.json/03-002 status untouched. Finalizing with explicit-pathspec commit + push.
