# 03-004 wire-iwad-and-config-discovery

- 2026-05-14 start: selected plan_final step 03-004 (lane launch-host-input). Goal: wire IWAD, default.cfg, chocolate-doom.cfg, and save directory discovery into the launch context. Prerequisite 03-003 is COMPLETED at commit `b44e2ad` (push SHA recorded in status JSON commit `3b2465c`); origin/main is in sync at session start.
- 2026-05-14 resume: re-reading `plan_final/PROMPT.md`, the step file, the existing wrapper `src/vanilla/runDoomMain.ts`, `src/vanilla/commandLineConfiguration.ts`, the read-only refs `src/bootstrap/implement-iwad-discovery-order.ts` and the `src/config/` parsers, plus `src/bootstrap/gameMode.ts` for the GameMode/GameMission types. Working tree is clean (only unrelated probe-*.ts and .claude scratch files untracked); 03-003 status JSON is committed at `3b2465c`.
- Design decision: add a new `src/vanilla/launchContext.ts` wrapper that consumes a `CommandLineConfiguration` plus an environment probes object and produces a frozen `LaunchContext` carrying the resolved IWAD (path, source = 'iwad-flag' | 'discovery', game mode, probe sequence, mission), the resolved `default.cfg` path (honoring `-config <path>` per Chocolate Doom 2.2.1 `M_SetConfigFilenames` main-config slot), the resolved `chocolate-doom.cfg` path (no override flag wired in 03-003, so always the literal `chocolate-doom.cfg`), and the save directory (`-savedir <path>` or vanilla CWD fallback). The bootstrap SHA-pinned files remain untouched; the wrapper imports the existing `VANILLA_IWAD_CANDIDATES` list and `VANILLA_DEFAULT_SEARCH_DIRECTORY` constant from the audit module, plus `identifyMission` from `gameMode.ts`. The environment probes are injected (no direct filesystem calls) so the step is deterministic and unit-testable.
- Wrote `src/vanilla/launchContext.ts`: exports `LaunchContext`, `ResolvedIwad`, `LaunchContextEnvironment`, `VANILLA_DEFAULT_CONFIG_FILENAME`, `HOST_EXTRA_CONFIG_FILENAME`, and `resolveLaunchContext(configuration, environment)`. The IWAD resolver short-circuits when `-iwad <path>` is supplied (does not call the probe) and case-insensitively recognizes one of the seven canonical candidates as the matched filename. Otherwise walks `VANILLA_IWAD_CANDIDATES` in canonical order and stops at the first existence-probe match. The default-config slot honors `-config <path>` or returns the literal `default.cfg`. The host-config slot is the literal `chocolate-doom.cfg`. The savedir slot honors `-savedir <path>` or returns the literal `.`.
- Wrote `test/plan_final/launch/wire-iwad-and-config-discovery.test.ts` with 27 tests covering wrapper file existence + 03-004 comment, the bootstrap import lines, the public surface arity, frozen result invariants, every iwad-flag branch (canonical lower/upper, non-canonical, Windows path separator), every discovery branch (empty filesystem, single-IWAD match per gamemode slot, first-match-wins, DOOMWADDIR override, length=7 invariant), defaultConfigPath/hostConfigPath/saveDirectory branches, full-composite argv, and the two probe-invocation invariants (iwad-flag short-circuits the probe; discovery stops after first match).
- Ran `bun run format` → 3 files formatted, no fixes applied.
- Ran `bun test test/plan_final/launch/wire-iwad-and-config-discovery.test.ts` → 27 pass, 0 fail, 87 expect() calls.
- Ran `bun test` → 15613 pass, 0 fail across 824 files (+27 tests, +1 file vs the 03-003 baseline of 15586/823).
- First `bun x tsc --noEmit` attempt failed: 2 TS7006 implicit-any errors at the test file's environment-object literal where the inline `doesBasenameExistInWadDirectory` callback parameter was implicitly typed. Bun-test's inference is more permissive than tsc strict-mode. Fix: explicitly annotate the inline `basename` parameter as `VanillaIwadCandidate['filename']` in both `buildEnvironmentWithPresentBasenames` and the tracking environment literal in the "stops invoking … after the first match" test.
- Verified cross-plan SHA pin status before writing implementation:
  - test/vanilla_parity/current-state/inventory-src-bootstrap-surface.test.ts:203 pins sha256 for every committed bootstrap module including implement-iwad-discovery-order.ts. Treat src/bootstrap/ as strict read-only.
  - test/vanilla_parity/current-state/inventory-save-and-config-modules.test.ts pins sha256 for src/bootstrap/config.ts plus 5 save modules and the src/save/ directory file count (24), total size, line count, and exact 24-filename list. Adding any new file to src/save/ would break that inventory test. Treat src/save/ and src/bootstrap/config.ts as strict read-only.
  - src/config/ has no SHA pin in any plan_vanilla_parity inventory and src/vanilla/ has no inventory entry (per 03-001/03-003 precedent). Both are safe surfaces; landing all new module code under src/vanilla/launchContext.ts is the minimum-surface option.
