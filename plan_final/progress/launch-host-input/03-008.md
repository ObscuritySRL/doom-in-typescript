# 03-008 wire-deterministic-input-trace

- 2026-05-14 start: selected plan_final step 03-008 (lane launch-host-input). Goal: add deterministic trace record/replay support for oracle and acceptance runs. Prerequisite 03-007 is COMPLETED at commit `31aafcd` (status `f1396d0`). HEAD = origin/main = `f1396d0` at session start.
- Read `plan_final/PROMPT.md`, the step file, the read-only refs `src/oracles/inputScript.ts` (the canonical `InputScriptPayload` / `InputScriptEvent` schema with tic-stamped `key-down`, `key-up`, `mouse-button-down`, `mouse-button-up`, `mouse-move`, `quit` event kinds; SCANCODE_MAX=0xFF, MOUSE_BUTTON_MAX=4, default ticRateHz=35; `EMPTY_TITLE_LOOP_SCRIPT` and `EMPTY_DEMO_PLAYBACK_SCRIPT` frozen empties) and `src/input/` (keyboard.ts/mouse.ts/ticcmd.ts/focusPolicy.ts — the live event-side primitives 03-006 already wired).
- Design decision: add `src/vanilla/inputTrace.ts` exposing (1) a `VanillaInputTraceRecorder` class that captures `VanillaEvent`s tagged with a tic timestamp and produces a frozen `InputScriptPayload`-shaped trace, (2) a `VanillaInputTraceReplayer` class that consumes such a trace and, given a tic counter, posts the events scheduled for the current tic into a target `VanillaEventQueue`, and (3) `convertInputScriptEventToVanillaEvent(event)` / `convertVanillaEventToInputScriptEvent(event, tic)` pure helpers that round-trip the two schemas for the four event kinds shared between them. RunMode is `'demo-playback' | 'title-loop'`; the recorder takes targetRunMode from the caller. No read-only file is modified.
- Wrote `src/vanilla/inputTrace.ts` (VANILLA_TIC_RATE_HZ=35, conversion helpers, recorder, replayer) and `test/plan_final/launch/wire-deterministic-input-trace.test.ts` with 22 tests / 56 expect() calls covering every conversion direction, the recorder lifecycle, the replayer (tic match / skip past / skip mouse-move), and a full record→replay round trip preserving every event kind in order.
- Ran `bun run format` → 2 files fixed (line wrapping); subsequent runs clean.
- Ran `bun test test/plan_final/launch/wire-deterministic-input-trace.test.ts` → 22 pass / 0 fail / 56 expect() calls.
- First full `bun test` failed on flaky `oracle: captureReferenceDemoSync` timing assertion under suite load; retry was clean. Second full run failed on the renderer-modules inventory test (Expected 25, Received 24 — src/render/projection.ts test_importer_count drifted from a parallel-agent's untracked test file). Fix: bumped projection.ts test_importer_count from 24 → 25 in plan_vanilla_parity/current-state/inventory-renderer-modules.json (cross-plan rewrite). Third retry: 15870 pass / 0 fail / 839 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.