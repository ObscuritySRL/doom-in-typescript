# 09-004 wire-weapon-psprites

- 2026-05-15 start: selected plan_final step 09-004 (lane player-weapons-items). Goal: wire weapon ownership, selection, raise/lower, psprite state ticking, flash, and attack gating. Prerequisite 09-002 is COMPLETED.
- Read the read-only refs `src/player/weapons.ts` (`giveAmmo`, `giveWeapon`, `checkAmmo`, `giveBackpack`) and `src/player/weaponStates.ts` (`fireWeapon`, `dropWeapon`, `aWeaponReady`, `aLower`, `aRaise`, `aReFire`, `aCheckReload`, `aGunFlash`, `setWeaponStateContext`, `getWeaponStateContext`).
- Design decision: add `src/vanilla/wireWeaponPsprites.ts` — a re-export facade collecting the weapon ownership/selection + psprite-state-machine entry points into one cohesive surface. No read-only player file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/player/wire-weapon-psprites.test.ts` (5 tests / 26 expect() calls including a same-reference identity check).
- Ran `bun run format` → no fixes; focused `bun test` → 5 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/player/weaponStates.ts` src 0→1 test 2→3; `src/player/weapons.ts` src 3→4 test 3→4 in `inventory-player-modules.json`. Inventory test: 23 pass / 0 fail; diff is exactly 4 count lines.
- Ran `bun test` → 16096 pass / 0 fail / 864 files. Typecheck → clean.