# 09-001 wire-player-spawn-state

- 2026-05-15 start: selected plan_final step 09-001 (lane player-weapons-items). Goal: wire player spawn, reborn, inventory defaults, view height, bob, cards, ammo, and weapons into runtime. Prerequisite 08-001 is COMPLETED.
- Read the read-only ref `src/player/playerSpawn.ts` (`createPlayer`, `playerReborn`, `setupPsprites`, `movePsprites`, `bringUpWeapon`, `setPsprite`; constants NUMWEAPONS=9, NUMAMMO=4, NUMCARDS=6, NUMPOWERS=6, INITIAL_HEALTH=100, INITIAL_BULLETS=50, MAX_AMMO=[200,50,300,50], WEAPONTOP/WEAPONBOTTOM/LOWERSPEED/RAISESPEED).
- Design decision: add `src/vanilla/wirePlayerSpawnState.ts` — a re-export facade collecting the player spawn/reborn/psprite functions + inventory-default constants into one cohesive surface. No read-only player file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/player/wire-player-spawn-state.test.ts` (7 tests / 27 expect() calls). Fixed one assertion: `createPlayer()` returns `health: 0` (zero-init); `playerReborn` sets INITIAL_HEALTH=100 — the test now asserts both.
- Ran `bun run format` → no fixes.
- Ran focused `bun test` → 7 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/player/playerSpawn.ts` src 14→15, test 14→15 in `inventory-player-modules.json`. Inventory test: 23 pass / 0 fail; diff is exactly 2 count lines.
- Full suite first run had the flaky captureReferenceDemoSync + the player inventory drift; after the bump the retry was clean at 16052 pass / 0 fail / 858 files.
- Ran `bun x tsc --noEmit --project tsconfig.json` → clean.