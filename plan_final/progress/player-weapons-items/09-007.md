# 09-007 wire-pickups

- 2026-05-15 start: selected plan_final step 09-007 (lane player-weapons-items). Goal: wire health, armor, ammo, weapon, backpack, key, bonus, and powerup pickup side effects. Prerequisites 09-001 and 08-003 are COMPLETED.
- Read the read-only refs `src/player/pickups.ts` (`giveBody`, `giveArmor`, `giveCard`, `givePower`, `touchSpecialThing`, `setPickupContext`, `getPickupContext`, `clearPickupContext`) and `src/player/powerups.ts` (`tickPowerups`, `computeFixedColormap`, `computePalette`).
- Design decision: add `src/vanilla/wirePickups.ts` — a re-export facade collecting the pickup side-effect functions + per-tic powerup functions into one cohesive surface. No read-only player file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/player/wire-pickups.test.ts` (5 tests / 23 expect() calls including a same-reference identity check).
- Ran `bun run format` → no fixes; focused `bun test` → 5 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/player/pickups.ts` src 2→3 test 6→7; `src/player/powerups.ts` src 0→1 test 1→2 in `inventory-player-modules.json`. Inventory test: 23 pass / 0 fail; diff is exactly 4 count lines.
- Ran `bun test` → 16116 pass / 0 fail / 867 files. Typecheck → clean.