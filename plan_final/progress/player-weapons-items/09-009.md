# 09-009 wire-vanilla-cheats

- 2026-05-15 start: selected plan_final step 09-009 (lane player-weapons-items). Goal: wire vanilla-scope cheats only if they exist in the reference target and do not affect demo parity unless entered. Prerequisite 09-002 is COMPLETED.
- Investigated the cheat surface: there is no dedicated `cheats.ts`; the vanilla cheat-flag bits live in the read-only `src/player/implement-god-mode-and-powerup-flags.ts` — `VANILLA_CF_NOCLIP=1`, `VANILLA_CF_GODMODE=2`, `VANILLA_CF_NOMOMENTUM=4` plus the predicates `isGodMode`, `isNoclip`, `isNoMomentum`. These mirror `doomdef.h` `cheat_t`. The cheat-code keystroke recognition is vanilla `ST_Responder` / `m_cheat.c`, which only runs on live keyboard input; demo playback carries ticcmds (no raw keystrokes), so a demo NEVER sets the cheat flags — that is the parity invariant this step pins.
- Design decision: add `src/vanilla/wireVanillaCheats.ts` — a re-export facade collecting the vanilla cheat-flag bits + predicates, plus a frozen `VANILLA_CHEAT_FLAG_BITS` manifest and the `VANILLA_CHEATS_AFFECT_DEMO_PARITY=false` invariant. No read-only player/input file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/player/wire-vanilla-cheats.test.ts` (7 tests / 26 expect() calls including bitfield-decode + same-reference identity + the demo-parity invariant).
- Ran `bun run format` → no fixes; focused `bun test` → 7 pass / 0 fail.
- Ran `bun test` → one flaky captureReferenceDemoSync timing fail on the first run; clean retry at 16123 pass / 0 fail / 868 files. No cross-plan inventory drift. Typecheck → clean.