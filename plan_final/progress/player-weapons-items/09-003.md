# 09-003 wire-player-use-action

- 2026-05-15 start: selected plan_final step 09-003 (lane player-weapons-items) via `bun run plan_final/select-step.ts`. Prerequisite 09-002 is COMPLETED (commit dd679d5). No prior 09-003 artifacts — fresh step. Goal: wire the use button to P_UseLines, no-way sounds, and use-down debounce. Read-only: `src/player/implement-player-use-action.ts`, `src/world/useLines.ts`. Write lock: `src/player/`, `src/world/`, `src/vanilla/`, `test/plan_final/player/wire-player-use-action.test.ts`.
- 2026-05-15 reads: read read-only `src/player/implement-player-use-action.ts` (`classifyVanillaPlayerUseAction` BT_USE/usedown debounce; `VANILLA_USERANGE`=64*FRACUNIT, `VANILLA_SFX_NOWAY`=20; `PlayerUseInput`/`PlayerUseOutcome`) and `src/world/useLines.ts` (`useLines` P_UseLines ray cast; `USERANGE`=64*FRACUNIT, `MAXSPECIALCROSS`/`MAXSPECIALCROSS_ORIGINAL`/`DEFAULT_SPECHIT_MAGIC`, `spechitOverrun`; `UseLineCallbacks`/`UseSpecialLineFunction`/`NoWaySoundFunction`/`SpechitOverrunState`). Inspected 09-001/09-002 evidence: player-lane facades live at `src/vanilla/wirePlayerXxx.ts` with tests in `test/plan_final/player/`, and both required a plan_vanilla_parity inventory importer-count bump. Design: `src/vanilla/wirePlayerUseAction.ts` re-export barrel of both read-only modules + an `applyPlayerUseAction` wiring fn (classify debounce → conditional `useLines` on the rising edge; uses `Parameters<typeof useLines>` so no extra Mobj/MapData imports widen the inventory blast radius) + frozen `VANILLA_PLAYER_USE_INVARIANTS`. Expect inventory drift for the two read-only modules; will probe + reconcile per the 09-001/09-002 precedent.
- 2026-05-15 edits: created `src/vanilla/wirePlayerUseAction.ts` (re-export barrel + `applyPlayerUseAction(input, useLinesAction)` thunk-gated wiring + frozen `VANILLA_PLAYER_USE_INVARIANTS` 4 rules) and focused test `test/plan_final/player/wire-player-use-action.test.ts`. Self-corrected: first draft used banned `as unknown as`/`as` casts to fake Mobj/MapData sentinels (no test factories exist in the repo) — AGENTS.md/CLAUDE.md forbid casts unconditionally. Redesigned `applyPlayerUseAction` to gate a zero-arg `useLinesAction: () => void` thunk (caller binds the re-exported `useLines`), which is the minimal honest wiring primitive and fully unit-testable with no casts and no heavy fixtures. Test uses line-anchored import-specifier parsing + dynamic `await import(...)` identity checks so the test file adds no inventory importer; only the unavoidable facade src import drifts the two read-only modules. Running verification.
- 2026-05-15 verify run 1: format clean; focused 7 pass / 0 fail; no banned casts; full `bun test` 16198 pass, 1 fail = `inventory-map-and-world-modules.test.ts:358` `src/world/useLines.ts` src_importer_count manifest 1 vs live 2. `src/player/implement-player-use-action.ts` is not tracked by any plan_vanilla_parity manifest (grep-confirmed). Probed the map-world manifest with the test's exact scan+countImporters: only useLines drifts (src 1→2 from the facade `export ... from '../world/useLines.ts'`; test stays 1 — line-anchored/dynamic-import test design held). Reconciled `plan_vanilla_parity/current-state/inventory-map-and-world-modules.json` useLines src 1→2 (manifest==reality maintained, documented bump-on-drift, matches 09-001/09-002 precedent). Probe reconfirms zero drift; removed throwaway probe. Rerunning full verification from the beginning.
