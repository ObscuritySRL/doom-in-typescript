# 12-008 wire-long-run-drift-checks

- 2026-05-15 start (claim): selected plan_final step 12-008 (lane save-config-demo) — `bun run plan_final/select-step.ts` returns it as first eligible. Prerequisites 12-006 + 04-007 + 06-009 + 11-008 all COMPLETED (verified by status JSON). A concurrent worker held a brief progress-log claim on 12-008 earlier (collision-analysis claim entry) but RELEASED it: no plan_final/progress/*/12-008.md and no plan_final/status/12-008.json exist now; the probe of the live frontier confirms 12-008 progressLog=false/claimed=false and the only active worker is 13-001 (acceptance lane, write lock test/plan_final/acceptance/ + plan_final/final-gates/ — disjoint from 12-008). Reclaiming via progress-log-first protocol. Goal: add replay drift checks for the long attract loop, demos, and scripted E1M1 paths. Write lock: src/oracles/, test/plan_final/save/wire-long-run-drift-checks.test.ts. Read-only: src/oracles/ (existing modules), test/oracles/fixtures/. NO src/vanilla/ in write lock → the aggregator is a NEW file in src/oracles/ (existing src/oracles/ files stay read-only), using same-directory ./ specifiers. Research: AGENTS.md (binding, all session), plan_final/README.md.
- 2026-05-15 reads: src/oracles/framebufferHash.ts — FRAMEBUFFER_WIDTH 320, FRAMEBUFFER_HEIGHT 200, FRAMEBUFFER_SIZE 64000, PALETTE_COUNT 14, DEFAULT_FRAMEBUFFER_SAMPLING_INTERVAL_TICS 35, interfaces FramebufferHashEntry/FramebufferHashPayload, type FramebufferHashArtifact = OracleArtifact<FramebufferHashPayload>, frozen baselines EMPTY_TITLE_LOOP_FRAMEBUFFER_HASH / EMPTY_DEMO_PLAYBACK_FRAMEBUFFER_HASH. src/oracles/stateHash.ts — type StateHashComponent 'automap'|'combined'|'player'|'rng'|'sectors'|'thinkers', STATE_HASH_COMPONENTS (6, frozen), DEFAULT_SAMPLING_INTERVAL_TICS 35, INDIVIDUAL_COMPONENT_COUNT 5, interfaces StateHashEntry/StateHashPayload, type StateHashArtifact, frozen baselines EMPTY_TITLE_LOOP_STATE_HASH / EMPTY_DEMO_PLAYBACK_STATE_HASH. No const enums (string-union + OracleArtifact<> type aliases only). framebufferHash.ts / stateHash.ts are NOT referenced by any plan_vanilla_parity inventory (only audioHash/inputScript/referenceRunManifest/referenceSandbox are) → expected inventory-neutral; will probe ALL inventory-*.json to confirm. Design: src/oracles/longRunDriftChecks.ts aggregator re-exporting the framebuffer (pixel-drift) + state (tic/state-drift) comparison contracts and the long-attract-loop / demo-playback baseline payloads, + frozen VANILLA_LONG_RUN_DRIFT_CHECK_INVARIANTS (5 rules). Inventory-neutral test (constants, frozen baselines + manifest, ./-filtered specifier set ['./framebufferHash.ts','./stateHash.ts'], dynamic identity). Test lives in test/plan_final/save/.
- 2026-05-15 verify: `bun run format` fixed 1 (multiline wrap). Focused first run failed 1/6 — the ./-filter caught the JSDoc @example's own `./longRunDriftChecks.ts` self-reference; fixed by stripping the leading `/** ... */` block before scanning specifiers (same robust technique as the door const-enum test), not a weakening (the assertion still verifies the real export region is exactly ./framebufferHash.ts + ./stateHash.ts). Re-ran: focused = 6 pass / 0 fail. grep ` as unknown| as any| as [A-Z]` clean. All-inventory probe (every plan_vanilla_parity/current-state/inventory-*.json) showed ZERO drift — framebufferHash.ts/stateHash.ts and the new aggregator are not inventory-tracked → fully inventory-neutral, no reconcile. Ran mandatory `bun run plan_final/sync-master-checklist.ts`. Full `bun test` = 16419 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean (zero error TS). Finalizing with the two-commit pathspec pattern.
