# 12-005 wire-savegame-load

- 2026-05-15 start: selected plan_final step 12-005 (lane save-config-demo). Prerequisite 12-004 is COMPLETED. No prior 12-005 artifacts — fresh step. Goal: wire savegame version checks, corrupted save errors, state restore, render/audio/input refresh. Read-only: `src/save/loadgame.ts`, `src/save/restore-post-load-render-audio-input-state.ts`. Write lock: `src/save/`, `src/vanilla/`, `test/plan_final/save/wire-savegame-load.test.ts`.
- 2026-05-15 reads: loadgame = SAVEGAME_EOF 0x1d, readLoadGame(source,layout,startOffset)→SaveGameReadResult<LoadGameRestore>|null (null on version-mismatch / corrupted save), LoadGameLayout/LoadGameRestore interfaces. restore-post-load = VANILLA_POST_LOAD_SUBSYSTEM_RESET_ORDER frozen 6-step list [R_FillBackScreen, R_ExecuteSetViewSize, R_SetupFrame, S_Start, I_ResetKey, D_ResetMouseDeltas] (render→audio→input), VANILLA_POST_LOAD_RESET_GROUPS, vanillaPostLoadResetSubsystemFor / vanillaPostLoadResetOrderIndex. No const enums; no name collisions (SaveGameReadResult is imported by loadgame, not its own export — not re-exported). Design: `src/vanilla/wireSavegameLoad.ts` re-export barrel + frozen `VANILLA_SAVEGAME_LOAD_INVARIANTS` (5 rules). Inventory-neutral test (real reset-order index/subsystem fns + constants + frozen order + `../`-filtered specifier set + dynamic identity). save modules ARE importer-tracked (12-004 finding) — expect drift in inventory-save-and-config-modules.json; probe + reconcile + sync MASTER_CHECKLIST; pathspec-limited commits.
- 2026-05-15 edits: created `src/vanilla/wireSavegameLoad.ts` + focused test. Format wrapped 1 file; no banned casts; focused 7/7. Full run 16262 pass, 1 fail = save-config inventory. Probe: single src-only drift loadgame.ts src 6→7 (restore-post-load module is NOT importer-tracked). Reconciled loadgame.ts src 6→7. Rerunning full verification + sync checklist.
