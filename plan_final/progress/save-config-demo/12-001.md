# 12-001 wire-config-load-and-persist

- 2026-05-15 start: selected plan_final step 12-001 (lane save-config-demo). Goal: wire default.cfg and chocolate-doom.cfg load, precedence, write-back, and isolation policy. Prerequisite 03-004 is COMPLETED.
- Read the read-only `src/config/` refs: `defaultCfg.ts` (`parseDefaultCfg`, `createDefaultVanillaCfg`), `hostConfig.ts` (`parseHostExtraCfg`, `createDefaultHostExtraCfg`), `write-config-back-in-vanilla-format.ts` (`writeVanillaDefaultCfg`), `isolate-tests-from-user-local-config.ts` (`isUserLocalConfigPath`, `assertConfigPathIsTestSafe`, `buildInMemoryDefaultCfgFixture`).
- Design decision: add `src/vanilla/wireConfigLoadAndPersist.ts` — a re-export facade collecting the default.cfg + chocolate-doom.cfg parse/create, the vanilla-format write-back, and the test-isolation policy into one cohesive surface. No read-only config file is modified — pure barrel + frozen manifest.
- Wrote the facade + `test/plan_final/save/wire-config-load-and-persist.test.ts` (7 tests / 66 expect() calls including the disjoint-namespace invariant + a fixture round-trip + same-reference identity).
- Ran `bun run format` → 1 fix (manifest wrapping); focused `bun test` → 7 pass / 0 fail.
- Cross-plan inventory diagnostic: `src/bootstrap/config.ts` src 4→5 test 4→5 in `inventory-save-and-config-modules.json`. Inventory test: 22 pass / 0 fail; diff is exactly 2 count lines.
- Ran `bun test` → one flaky captureReferenceDemoSync timing fail on the first run; clean retry at 16136 pass / 0 fail / 870 files. Typecheck → clean.