# 12-003 wire-save-slot-ui

- 2026-05-15 start: selected plan_final step 12-003 (lane save-config-demo). Prerequisite 07-003 is COMPLETED. No prior 12-003 artifacts — fresh step. Goal: wire save/load slot descriptions, slot selection, quicksave, quickload, and messages. Read-only: `src/save/define-save-directory-policy.ts`, `src/save/implement-save-slot-descriptions.ts`, `src/ui/menus.ts`. Write lock: `src/save/`, `src/ui/`, `src/vanilla/`, `test/plan_final/save/wire-save-slot-ui.test.ts`.
- 2026-05-15 reads: define-save-directory-policy = VANILLA_SAVE_SLOT_COUNT 6, doomsav prefix / .dsg ext / temp.dsg staging, VANILLA_SAVEGAME_NAME_SIZE 24, vanillaSaveSlotFilename/isVanillaSaveSlotFilename/vanillaSaveDirectoryPathSegments. implement-save-slot-descriptions = VANILLA_SAVE_STRING_SIZE 24 / USER_INPUT_MAX 23 / printable ASCII 0x20..0x7e, VANILLA_EMPTY_SAVE_SLOT_MENU_LABEL 'EMPTY', encode/decodeVanillaSaveDescription, isVanillaSaveSlotEmpty, vanillaSaveSlotMenuLabel, isVanillaSaveDescriptionCharacter. menus.ts = SAVESTRINGSIZE 24 + MenuAction (selectLoadSlot/selectSaveSlot/beginSaveStringEntry/commit/cancel from 07-003). No const enums; no export-name collisions. Design: `src/vanilla/wireSaveSlotUi.ts` re-export barrel + frozen `VANILLA_SAVE_SLOT_UI_INVARIANTS` (5 rules). Inventory-neutral test (real encode/decode round-trip + slot filename + constants + `../`-filtered specifier set + dynamic identity). menus.ts is UI-inventory-tracked; the save modules likely save-config inventory; will probe + reconcile + sync MASTER_CHECKLIST per the mandatory contract; pathspec-limited commits.
- 2026-05-15 edits: created `src/vanilla/wireSaveSlotUi.ts` + focused test. Format wrapped lines; cast-check false positive on the "as EMPTY" test-title string (no real casts); focused 7/7.
- 2026-05-15 verify run 1: full `bun test` 16248 pass, 1 fail = `inventory: ui modules` only (save modules not importer-tracked). Probe: single drift menus.ts src 24→25 (test unchanged at 24). Reconciled menus.ts src 24→25. Rerunning full verification from the beginning + sync checklist.
