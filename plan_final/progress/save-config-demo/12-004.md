# 12-004 wire-savegame-write

- 2026-05-15 start: selected plan_final step 12-004 (lane save-config-demo). Prerequisites 12-003 and 08-002 are COMPLETED. No prior 12-004 artifacts — fresh step. Goal: wire savegame header, players, world, thinkers, specials, terminators, and size limit writes. Read-only: `src/save/saveHeader.ts`, `src/save/coreSerialization.ts`, `src/save/specialSerialization.ts`. Write lock: `src/save/`, `src/vanilla/`, `test/plan_final/save/wire-savegame-write.test.ts`.
- 2026-05-15 reads: saveHeader = SAVEGAME_VERSION_CODE 109 / fixed SAVEGAME_HEADER_SIZE, writeSaveGameHeader/readSaveGameHeader, SaveGameHeader{description,gameepisode,gamemap,gameskill,leveltime,playeringame[4]}. coreSerialization = fixed record sizes (SAVEGAME_MOBJ_SIZE 154, SAVEGAME_PLAYER_SIZE 280, SECTOR 14, SIDE 10, LINE_BASE 6), SAVEGAME_THINKER_CLASS_END 0 / _MOBJ 1, writeArchivedPlayers/Mobjs/World + read*, SaveGameWorld/Mobj/Player/Thinker/Layout/ReadResult. specialSerialization = SAVEGAME_SPECIAL_* sizes + class tags (CEILING 0/DOOR 1), writeArchivedSpecials/readArchivedSpecials, SaveGameSpecialArchive/Thinker. NOTE collision: `SAVEGAME_PLAYER_COUNT` + `SaveGamePlayerPresence` exported by BOTH saveHeader and coreSerialization (same values) — surface from saveHeader only to avoid TS duplicate-export. specialSerialization names all SAVEGAME_SPECIAL_*/SaveGameSpecial* (distinct). No const enums. Design: `src/vanilla/wireSavegameWrite.ts` re-export barrel (value/type split) + frozen `VANILLA_SAVEGAME_WRITE_INVARIANTS` (5 rules: fixed header w/ version 109; fixed mobj/player record sizes; world writes sectors/lines/sides then thinkers; thinker list terminated by CLASS_END 0; specials serialize doors/floors/ceilings by class). Inventory-neutral test (real writeSaveGameHeader round-trip + constants + `../`-filtered specifier set + dynamic identity). save modules not importer-tracked (per 12-002/12-003 finding) — likely no inventory drift; will probe + sync MASTER_CHECKLIST per the mandatory contract; pathspec-limited commits.
- 2026-05-15 edits: created `src/vanilla/wireSavegameWrite.ts` + focused test. Format wrapped lines; no banned casts; focused 7/7.
- 2026-05-15 verify run 1: full `bun test` 16255 pass, 1 fail = `inventory: save and config modules`. CORRECTION to the start-note assumption: the SERIALIZATION modules (saveHeader/coreSerialization/specialSerialization) ARE importer-tracked by inventory-save-and-config-modules.json (the persist-*/policy modules from 12-002/12-003 are not). Probe: three src-only drifts — coreSerialization 2→3, saveHeader 12→13, specialSerialization 1→2 (all test counts unchanged). Reconciled all three (anchored on the unique exported_types line / external_imports closing per module). Probe reconfirms zero drift. Rerunning full verification from the beginning + sync checklist.
