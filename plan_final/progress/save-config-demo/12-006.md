# Progress Log: 12-006 wire-demo-playback-runtime

## 2026-05-15 — start

- Selected step 12-006 (save-config-demo lane) via `bun run plan_final/select-step.ts --lane save-config-demo`.
- Default global selector returned 11-004; a parallel `claude` agent is racing the default selector (commits dbbc89f/f816742 for 10-008 at 14:32). Taking the deepest eligible step (12-006) to avoid write-lock collision with the parallel worker on the front of the queue. Verify-then-yield strategy.
- Confirmed fresh: progress log, `plan_final/status/12-006.json`, and `test/plan_final/save/wire-demo-playback-runtime.test.ts` all MISSING.
- Read: AGENTS.md, plan_final/PROMPT.md, plan_final/MASTER_CHECKLIST.md, plan_final/steps/12-006-wire-demo-playback-runtime.md.
- Goal: wire DEMO lump playback from title loop and -playdemo through runtime ticcmds.
- Next: read research source plan_final/README.md, then read-only src/demo/demoParse.ts, src/demo/demoPlayback.ts, and the src/vanilla/ facade pattern.

## 2026-05-15 — reads complete, plan fixed

- Read plan_final/README.md, src/demo/demoParse.ts, src/demo/demoPlayback.ts, and the established facade+test pattern (src/vanilla/wireRngAndDemoStreams.ts, src/vanilla/wireSavegameLoad.ts, test/plan_final/save/wire-savegame-load.test.ts).
- Inventory-drift analysis: test/vanilla_parity/current-state/inventory-demo-and-replay-modules.test.ts pins `src_importer_count` for the demo modules from plan_vanilla_parity/current-state/inventory-demo-and-replay-modules.json. Currently passes 22/22 with demoParse.ts src=2 and demoPlayback.ts src=12.
- A re-export barrel facade in src/vanilla/ adds one src importer to each demo module it re-exports. Precedent: plan_final step 04-006 (wireRngAndDemoStreams.ts) recorded `crossPlanInventoryRewriteRequired: true` and bumped inventory-demo-and-replay-modules.json importer counts even though the inventory JSON was outside its declared write lock. Following that established, evidence-documented pattern is mandatory to keep the full headless suite green.
- Plan: create src/vanilla/wireDemoPlaybackRuntime.ts (pure re-export barrel from ../demo/demoParse.ts + ../demo/demoPlayback.ts, value/type split for verbatimModuleSyntax, no const enums, frozen VANILLA_DEMO_PLAYBACK_INVARIANTS); create inventory-neutral focused test test/plan_final/save/wire-demo-playback-runtime.test.ts (static-import only the facade; dynamic-import + text-parse for demo modules; real synthetic-demo playback behavior assertion); bump inventory-demo-and-replay-modules.json demoParse.ts src 2->3 and demoPlayback.ts src 12->13.

## 2026-05-15 — edits complete

- Created src/vanilla/wireDemoPlaybackRuntime.ts (re-export barrel + 5 frozen invariants).
- Created test/plan_final/save/wire-demo-playback-runtime.test.ts (8 tests: structure, ../-specifier set, frozen invariants, pinned constants, parseDemo decode, attract-loop playback, singledemo quit, version-mismatch rejection, dynamic same-reference identity).
- Edited plan_vanilla_parity/current-state/inventory-demo-and-replay-modules.json: demoParse.ts src_importer_count 2->3, demoPlayback.ts src_importer_count 12->13 (crossPlanInventoryRewriteRequired, per 04-006 precedent).
- Next: run verification sequence (format, focused test, full headless, typecheck).

## 2026-05-15 — verification round 1

- `bun run format`: OK (reformatted the new facade + test; functionally identical).
- `bun test test/plan_final/save/wire-demo-playback-runtime.test.ts`: 9 pass, 0 fail.
- `bun test --only-failures`: 16400 pass, 11 pre-existing skip, 0 fail (inventory-demo-and-replay-modules.test.ts green with bumped counts).
- `bun x tsc --noEmit`: FAILED — TS2769 at wire-demo-playback-runtime.test.ts:77. `EXPECTED_INVARIANT_IDS` widened to `string[]` is not assignable to the literal-union `ids` array in `toEqual`.
- Fix: map invariant ids through `String()` so `ids` is `string[]` matching `EXPECTED_INVARIANT_IDS` (documented String() field-narrowing pattern, cast-free). Rerunning full sequence from the beginning.



