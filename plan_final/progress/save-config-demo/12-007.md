# 12-007 wire-demo-record-and-timedemo

- 2026-05-15 start: selected plan_final step 12-007 (lane save-config-demo) as first eligible step. Prerequisite 12-006 COMPLETED (concurrent agent landed it at 842ae3a). Goal: wire -record/-playdemo/-timedemo/demo termination/deterministic output. Write lock: src/demo/, src/vanilla/, test/plan_final/save/wire-demo-record-and-timedemo.test.ts. Read-only: src/demo/demoRecord.ts, src/demo/demoFile.ts. Research: AGENTS.md (binding, all session), plan_final/README.md.
- 2026-05-15 reads: src/demo/demoRecord.ts (364 lines): DEMO_RECORD_DEFAULT_MAXIMUM_SIZE 0x20000, DEMO_RECORD_WRITE_HEADROOM 16, interfaces DemoRecordCommand / DemoRecorderOptions, class DemoRecorder (ctor validates maximumSize >= headerBytes+1, exactly DEMO_MAX_PLAYERS 4 slots with >=1 active, freezes playersInGame; getters commandByteLength/format/headerByteLength/maximumSize/playersInGame/ticCount/vanillaDemoLimit/versionByte; recordCommand(cmd)->Readonly<DemoTicCommand>|null; finish()->Buffer; header 13 bytes versioned / 7 old). src/demo/demoFile.ts (197 lines): DEMO_VERSION_19 109, DEMO_HEADER_SIZE 13, DEMO_TIC_SIZE 4, DEMO_END_MARKER 0x80, DEMO_MAX_PLAYERS 4, DEMO_TIC_RATE 35, interfaces DemoTicCommand / DemoFile, parseDemoLump(buf)->Readonly<DemoFile> (validates >= DEMO_HEADER_SIZE, reads 9 header bytes + 4 playeringame, durationSeconds = ticCount / DEMO_TIC_RATE). No const enums in either; no name collision (demoRecord imports DEMO_MAX_PLAYERS/DEMO_END_MARKER from demoParse but does not re-export them; only demoFile's are surfaced). Design: src/vanilla/wireDemoRecordAndTimedemo.ts re-export barrel (split value/type) over ../demo/demoRecord.ts + ../demo/demoFile.ts + frozen VANILLA_DEMO_RECORD_AND_TIMEDEMO_INVARIANTS (5 rules). Inventory-neutral test (constants, typeof parseDemoLump + DemoRecorder class, frozen manifest, ../-filtered specifier set ['../demo/demoFile.ts','../demo/demoRecord.ts'], dynamic identity). Both modules in inventory-demo-and-replay — probe + reconcile + sync; pathspec commits.
- 2026-05-15 verify: `bun run format` fixed 1 (test multiline wrap); grep ` as unknown| as any| as [A-Z]` clean. Focused `bun test test/plan_final/save/wire-demo-record-and-timedemo.test.ts` = 6 pass / 0 fail. Probe replicating the inventory scan + countImporters showed src/demo/demoFile.ts src 1->2 and src/demo/demoRecord.ts src 1->2 (the new facade is the only added importer of each); tests unchanged at 2 — reconciled inventory-demo-and-replay-modules.json both src_importer_count 1->2 (no other field touched). Ran mandatory `bun run plan_final/sync-master-checklist.ts`. Full `bun test` = 16413 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean (zero error TS). Finalizing with the two-commit pathspec pattern.
