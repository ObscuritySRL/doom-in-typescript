# 12-009 gate-save-config-demo

- 2026-05-15 start: selected plan_final step 12-009 (lane save-config-demo) as first eligible step. Prerequisites 12-001..12-008 all COMPLETED (verified via status JSON; 12-007 + 12-008 finalized this session). Goal: gate config/save/demo behavior. Write lock: test/plan_final/save/gate-save-config-demo.test.ts, plan_final/evidence/, plan_final/status/. Read-only: src/config/, src/save/, src/demo/, test/oracles/fixtures/. Research: AGENTS.md (binding, all session), plan_final/README.md.
- 2026-05-15 reads: step file 12-009; canonical cast-free lane-gate pattern (gate-ui-parity / gate-ai-specials / gate-map-world-parity). Step-file->title map: 12-001 wire-config-load-and-persist, 12-002 wire-settings-menus-to-config, 12-003 wire-save-slot-ui, 12-004 wire-savegame-write, 12-005 wire-savegame-load, 12-006 wire-demo-playback-runtime, 12-007 wire-demo-record-and-timedemo, 12-008 wire-long-run-drift-checks. All eight focused tests present under test/plan_final/save/. Design: mirror the cast-free gate (parse into Record<string, unknown>, String()/Array.isArray narrowing, poison sentinel for malformed knownFailures) over the eight save-config-demo prerequisites; no src import → inventory-neutral, no probe needed.
- 2026-05-15 verify: `bun run format` no fixes; grep ` as unknown| as any| as [A-Z]` clean. Focused `bun test test/plan_final/save/gate-save-config-demo.test.ts` = 7 pass / 0 fail. First full run surfaced 2 failures (NOT from the gate test): `plan_final current-state runtime-implementation-modules` oracles tracked_file_count 9 vs git-ls-files 10, and `vanilla_parity classify-real-implementations` group [src/input/,src/oracles/,tools/reference/] source_file_count 24 vs observed 25. Root cause: 12-008's legitimately new, now-git-tracked src/oracles/longRunDriftChecks.ts (committed a3000b3) — these file-count records only drift once the file is git-tracked, which post-dates 12-008's own full run. Per the procedure (any failure fixed in this same step, rerun from the beginning) and the established count-reconcile pattern (bookkeeping, NOT test weakening): updated plan_final/current-state/runtime-implementation-modules.json src/oracles/ tracked_file_count 9->10, and plan_vanilla_parity/current-state/classify-real-implementations.json the input/oracles/tools-reference group source_file_count 24->25 + classification_summary.classified_source_file_count 469->470 (keeps the summary-vs-group-sum assertion consistent). Probe-replicated both test count algorithms before editing; touched only the drifted numeric fields. Re-ran full sequence from the beginning: `bun run format` no fixes; full `bun test` = 16426 pass / 0 fail; `bun x tsc --noEmit --project tsconfig.json` = clean. Finalizing with the two-commit pathspec pattern (the two reconcile files are staged in the 12-009 feat commit since the failures manifested during, and were fixed within, this step).
