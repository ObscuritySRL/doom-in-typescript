# 12-002 wire-settings-menus-to-config

- 2026-05-15 start: selected plan_final step 12-002 (lane save-config-demo). Prerequisites 12-001 and 07-003 are COMPLETED. No prior 12-002 artifacts — fresh step. Goal: connect sound, screen, mouse, key binding, messages, and compatibility settings to menus and config. Read-only: `src/config/` (whole dir), `src/ui/menus.ts`. Write lock: `src/config/`, `src/ui/`, `src/vanilla/`, `test/plan_final/save/wire-settings-menus-to-config.test.ts`.
- 2026-05-15 reads: surveyed src/config/. Per-category persist modules with non-colliding category-prefixed surfaces: persist-sound-settings (VANILLA_SOUND_DEVICE_* 0-9, VANILLA_SOUND_VOLUME_MIN 0/MAX 15, VANILLA_SOUND_SETTING_NAMES, create/serializeVanillaSoundSettings, VanillaSoundSettings), persist-screen-settings (show_messages/screenblocks/detaillevel/usegamma; SCREENBLOCKS 3..11, DETAIL 0/1, GAMMA 0..4 count 5), persist-mouse-settings, persist-vanilla-key-bindings (DOS scancode defaults, name col width 30), persist-chat-macros (10 slots 0-9), hostConfig (HOST_EXTRA_CFG_VARIABLE_COUNT 113, createDefaultHostExtraCfg/parseHostExtraCfg, VanillaExtendedCfg = compatibility/extended). No const enums; no cross-module export-name collisions among the chosen surfaces. menus.ts exports `MenuAction` (string-union type, the settings-menu action vocabulary pinned by 07-003). Design: `src/vanilla/wireSettingsMenusToConfig.ts` re-export barrel of the five category modules + hostConfig (compatibility) + `MenuAction` from menus.ts; value/type split for verbatimModuleSyntax; frozen `VANILLA_SETTINGS_CONFIG_INVARIANTS` (6 rules) connecting each settings category to its vanilla cfg variables. Inventory-neutral test (real create/serialize behavior + constants + `../`-filtered specifier set + dynamic identity). Expect src-side drift: config modules in inventory-save-and-config-modules.json + menus.ts in inventory-ui-modules.json; will probe + reconcile, then sync MASTER_CHECKLIST per the mandatory contract.
- 2026-05-15 edits: created `src/vanilla/wireSettingsMenusToConfig.ts` (re-export barrel of the 5 persist-* category modules + hostConfig + MenuAction type; frozen `VANILLA_SETTINGS_CONFIG_INVARIANTS` 6 rules) + focused test. Format wrapped lines; no banned casts; focused 7/7.
- 2026-05-15 verify run 1: full `bun test` 16241 pass, 1 fail = `inventory: ui modules` only. The config persist-* modules + hostConfig are NOT importer-tracked by inventory-save-and-config-modules.json (no save-config inventory failure). Probe: single drift `src/ui/menus.ts` src 23→24 (test unchanged at 24) from `export type { MenuAction } from '../ui/menus.ts'`. Reconciled menus.ts src 23→24. sync-master-checklist test did NOT fail (checklist already current for prior steps; 12-002 correctly unchecked, no status yet). Will run the sanctioned sync as part of finalization per the mandatory contract. Rerunning full verification from the beginning.
