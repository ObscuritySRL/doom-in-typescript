<!-- Generated by plan_final/generate-plan-files.ts. Edit planData.ts, then regenerate. -->

# [ ] STEP 01-002: inventory-simplified-launcher-imports

## id

01-002

## lane

current-state

## title

inventory-simplified-launcher-imports

## goal

Record every src/main.ts and src/launcher import edge that bypasses the vanilla launch path.

## prerequisites

- 01-001

## parallel-safe-with

- all lanes except steps sharing the write lock

## write lock

- plan_final/current-state/simplified-launcher-imports.json
- test/plan_final/current-state/simplified-launcher-imports.test.ts

## read-only paths

- src/main.ts
- src/launcher/

## research sources

- AGENTS.md
- plan_final/README.md

## expected changes

- plan_final/current-state/simplified-launcher-imports.json
- test/plan_final/current-state/simplified-launcher-imports.test.ts

## test files

- test/plan_final/current-state/simplified-launcher-imports.test.ts

## verification commands

- `bun run format`
- `bun test test/plan_final/current-state/simplified-launcher-imports.test.ts`
- `bun test`
- `bun x tsc --noEmit --project tsconfig.json`

## progress log

- Append before the first read: `plan_final/progress/current-state/01-002.md`.
- Append after every read group, edit group, failed command, fix attempt, passing command sequence, commit, pull/rebase, and push.
- Preserve the log on block or failure so the next agent can resume without guessing.

## completion criteria

- The focused test proves the behavior or inventory result for this exact step.
- `bun run format`, focused `bun test test/plan_final/current-state/simplified-launcher-imports.test.ts`, `bun test`, and `bun x tsc --noEmit --project tsconfig.json` all pass in that order.
- Any failure is fixed in this same step and the full verification sequence is rerun from the beginning.
- The step is not marked complete, committed, or pushed while any verification command is failing.
- Files are staged explicitly by path, committed with a Conventional Commit, and pushed directly with local git commands.

## final evidence

- Write `plan_final/evidence/01-002.json` with commands, changed paths, and zero known failures.
- Write `plan_final/status/01-002.json` only after verification passes; status must not claim completion for failing tests.
- Include the pushed commit SHA in the progress log after `git push` verifies the remote contains it.
